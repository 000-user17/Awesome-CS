# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：139
- **题目名称**：单词拆分 Word Break
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/word-break/
- **刷题日期**：2026\08\14
- **标签**：动态规划 / 记忆化DFS / 字符串

## 📝 题目大意
给定字符串 `s` 和单词字典 `wordDict`，判断 `s` 是否可以被拆分成字典中一个或多个单词；
字典单词可重复使用，不需要全部用完。

> 两条主流可行思路：
> 方案1：**记忆化DFS（自顶向下DP）**
> 从位置 `idx` 向后枚举，缓存每个下标能否成功拆分，避免重复递归；
> 方案2：**迭代DP（自底向上）**
> `\(dp[i]\)` 代表前 `i` 个字符能否拆分，从小到大依次推导。

## 💡 解题思路
### 思路1：记忆化 DFS（自顶向下）
定义 `dfs(idx)`：从下标 `idx` 开始，`s[idx:]` 是否能够成功拆分。
优化点：
1. 字典转为 `set`，查询 $\(O(1)\)$；
2. 预先求出字典最长单词长度 `maxLen`，截取子串不会超出范围；
3. `memo` 缓存下标结果：**核心收益是缓存失败(`False`)分支，避免多条路径重复计算同一个位置**；
4. 一旦找到可行路径直接逐层返回True，提前终止。

递归终止条件：
- `idx == len(s)`：匹配到字符串末尾，返回 True；
- `idx` 在 memo 中：直接读取缓存结果。

### 思路2：迭代 DP（自底向上）
状态定义：
$\(dp[i]\)$：字符串前 $i$ 个字符 `s[0:i]` 是否可以拆分。
初始化：$dp[0] = True$（空串合法）。
转移：
向前枚举分割点，若存在合法位置 $j$，满足 $\(dp[j]=True\)$ 且 `s[j:i]` 在字典内，则 $\(dp[i]\)=True$。
搭配 `maxLen` 剪枝，不需要一直向前遍历到0。

> ⚠️ 你写的DP版本存在一处逻辑Bug，下面标出修正！

## ⚙️ 复杂度分析
设字符串长度 $n$，最长单词长度 $L$
- 记忆化DFS：时间 $O(n\cdot L)$，空间 $\(O(n)\)$（递归栈 + memo）
- 迭代DP：时间 $O(n\cdot L)$，空间 $\(O(n)\)$

## 💻 AC 代码
### ① 记忆化DFS（标准可AC版本）
```python
from typing import List
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        sLen = len(s)
        memo = {}
        wordSet = set(wordDict)
        maxLen = max(len(w) for w in wordDict)

        def dfs(idx):
            if idx == sLen:
                return True
            if idx in memo:
                return memo[idx]
            # 枚举截取长度
            for length in range(1, maxLen + 1):
                end = idx + length
                if end > sLen:
                    break
                substr = s[idx:end]
                if substr in wordSet:
                    if dfs(end):
                        memo[idx] = True
                        return True
            memo[idx] = False
            return False
        return dfs(0)
```

### ② 迭代DP（修正你原来代码的错误）
你原版DP代码 `j + 1 > maxLen` 逻辑写反了，分割下标处理混乱，下面是规范写法：
```python
from typing import List
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        sLen = len(s)
        wordSet = set(wordDict)
        maxLen = max(len(w) for w in wordDict)
        dp = [False] * (sLen + 1)
        dp[0] = True

        for i in range(1, sLen + 1):
            # j 是分割起点，只向前最多查找 maxLen 个位置
            start = max(0, i - maxLen)
            for j in range(start, i):
                if dp[j] and s[j:i] in wordSet:
                    \(dp[i]\) = True
                    break
        return dp[sLen]
```

## 🧩 关键知识点 & 技巧
1. **memo作用重点**
   成功路径一旦找到直接逐层return，几乎不会重复访问；
   大量重复计算来自**失败分支**，`memo[idx]=False` 是优化核心；
2. 字符串切片 `s[j:i]` 左闭右开，和DP状态定义完美匹配；
3. 列表转集合 `set(wordDict)`，极大提升子串存在性查询速度；
4. `maxLen` 剪枝：不用无限制向前枚举，减少循环次数；
5. 两种方案本质等价：记忆化DFS = 自顶向下DP，迭代DP = 自底向上DP。

## ❌ 错题反思 / 踩坑记录
### DFS常见坑
1. 多余参数拼接字符串 `curS`，产生大量临时字符串，浪费性能；
2. 不加记忆化，暴力回溯指数级复杂度，长用例直接超时；
3. 忘记 `break`，截取长度超出字符串边界。

### DP常见坑
1. 忘记初始化 `\(dp[0] = True\)`，全部结果为False；
2. 分割下标 `i,j` 混淆，切片区间写错；
3. 剪枝条件写反，达不到优化效果甚至逻辑错误（你原版DP问题）；
4. 不使用集合，直接在list中查找子串，时间超时。

### 容易混淆的概念
- `dfs(idx)`：从下标 idx **往后**能否拼完
- `\(dp[i]\)`：前 i 个字符 **前面**能否拼成功
两个状态定义视角不一样，不要混用下标！

## ✅ 两种方案简单对比
| 方式 | 优点 | 缺点 |
|------|------|------|
| 记忆化DFS | 代码直观，思路顺着题意正向思考，找到解立刻返回 | 存在递归栈深度风险（超长字符串） |
| 迭代DP | 无递归栈溢出风险，空间更稳定 | 需要理清前后下标关系，下标更容易写错 |
---
