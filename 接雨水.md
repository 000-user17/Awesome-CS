# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：42
- **题目名称**：接雨水 Trapping Rain Water
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/trapping-rain-water/
- **刷题日期**：2026\08\31
- **标签**：动态规划 / 双指针 / 单调栈
## 📝 题目大意
给定 `n` 个非负整数表示每个宽度为 1 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。
> 重点难点：某一位置能接的雨水，不由相邻柱子决定，由该位置**左侧最高柱子、右侧最高柱子**的短板决定；
> 公式：`当前位置雨水 = min(左侧最大高度，右侧最大高度) − 当前柱子高度`；结果小于0时接不到雨水，取0。
> 三种思路：DP、双指针为**纵向计算**，统计每根柱子上方存水量；单调栈为**横向计算**，一层一层统计凹槽面积。

## 💡 解题思路
### 思路一：动态规划 DP
定义：
- `left_max[i]`：下标 `i` **左边（不含i）**的柱子最大高度
- `right_max[i]`：下标 `i` **右边（不含i）**的柱子最大高度

步骤拆解：
1. 边界：数组长度小于等于2，直接返回0，无法存雨水；
2. 初始化 `left_max`、`right_max` 两个等长数组；
3. 从左向右遍历填充 `left_max`：`left_max[i] = max(left_max[i‑1], height[i‑1])`；
4. 从右向左遍历填充 `right_max`：`right_max[i] = max(right_max[i+1], height[i+1])`；
5. 跳过首尾柱子，遍历中间每一个位置，累加每一处雨水 `max(0, min(left_max[i], right_max[i])‑height[i])`；
6. 返回总雨水。

示例推演：`height = [2,1,1,2]`
n=4
left_max = [0,2,2,1]
right_max = [1,2,2,0]
i=1：min(2,2)-1 =1；i=2：min(2,2)-1=1；总和=2。

### 思路二：双指针（面试最优，空间\(O(1)\)）
定义：
- `l_max`：left指针**左侧**见过的最大高度，不含当前 `height[left]`
- `r_max`：right指针**右侧**见过的最大高度，不含当前 `height[right]`

核心逻辑：**短板效应，哪边最大高度更小，就先计算该侧位置的雨水**。
步骤拆解：
1. 边界：数组长度小于等于2返回0；
2. 初始化 `left=0`，`right = n‑1`；`l_max=0，r_max=0，res=0`；
3. `while left <= right`：
    - 如果 `l_max < r_max`：计算left位置雨水；再用当前柱子更新`l_max`；left右移；
    - 否则：计算right位置雨水；再用当前柱子更新`r_max`；right左移；
4. 返回res。

> ⚠️顺序不能乱：**先算水量，再更新最大墙高，再移动指针**。

示例推演：`height = [2,1,1,2]`
left=0,right=3,l_max=0,r_max=0
l_max不小于r_max，走else：res += max(0,0‑2)=0；r_max=2；right=2
l_max=0 < r_max=2：res += max(0,0‑2)=0；l_max=2；left=1
l_max=2不小于r_max=2：res += max(0,2‑1)=1；r_max=max(2,1)=2；right=1
l_max=2不小于r_max=2：res += max(0,2‑1)=1；r_max=max(2,1)=2；right=0
循环结束 res=2。

### 思路三：单调递减栈（横向层面积）
栈中保存柱子**下标**，栈对应高度从栈底到栈顶单调递减。
当遇到一根高于栈顶的柱子，代表找到凹槽右边界，弹出栈顶作为凹槽底部，计算这一层的雨水面积。

步骤拆解：
1. 初始化空栈，res=0；
2. 遍历每一个下标i：
    - while栈不为空，且`height[i] > height[stack[-1]]`：
        - pop得到凹槽底部bottom；
        - 弹出后栈为空，代表没有左墙，break；
        - left = stack[-1] 左墙下标；
        - width = i‑left‑1；
        - water_h = min(height[left], height[i]) ‑ height[bottom]
        - res += width * water_h
    - 将i压入栈；
3. 返回res。

> ⚠️必须是`while`不是`if`，同一个i可以连续弹出多层低洼；栈存下标不是高度；pop后判栈空；不要忘记乘以宽度。

示例推演：`height = [2,1,1,2]`
i=3(height=2)触发循环：
第一次pop bottom=2，h=1，left=1，water_h=min(1,2)-1=0，贡献0；
继续while条件成立，第二次pop bottom=1，h=1，left=0，width=2，water_h=2‑1=1，res +=2*1=2。

## ⚙️ 复杂度分析
1. **动态规划DP**
- 时间复杂度：$\(O(n)\)$，三次线性遍历
- 空间复杂度：$\(O(n)\)$，两个辅助数组
2. **双指针**
- 时间复杂度：$\(O(n)\)$，一次遍历
- 空间复杂度：$\(O(1)\)$，仅常数变量，面试最优
3. **单调递减栈**
- 时间复杂度：$\(O(n)\)$，每个元素最多入栈、出栈各一次
- 空间复杂度：$\(O(n)\)$，最坏情况栈存储全部下标

## 💻 AC 代码
### ① DP版本（left_max/right_max不含自身）
```python
from typing import List
class Solution:
    def trap(self, height: List[int]) -> int:
        n = len(height)
        if n <= 2:
            return 0
        left_max = [0] * n
        right_max = [0] * n
        for i in range(1, n):
            left_max[i] = max(left_max[i-1], height[i-1])
        for i in range(n-2, -1, -1):
            right_max[i] = max(right_max[i+1], height[i+1])
        
        res = 0
        for i in range(1, n-1):
            res += max(0, min(left_max[i], right_max[i]) - height[i])
        return res
```

### ② 双指针版本（面试优先）
```python
from typing import List
class Solution:
    def trap(self, height: List[int]) -> int:
        n = len(height)
        if n <= 2:
            return 0
        left, right = 0, n - 1
        l_max, r_max = 0, 0
        res = 0
        while left <= right:
            if l_max < r_max:
                res += max(0, l_max - height[left])
                l_max = max(l_max, height[left])
                left += 1
            else:
                res += max(0, r_max - height[right])
                r_max = max(r_max, height[right])
                right -= 1
        return res
```

### ③ 单调递减栈版本
```python
from typing import List
class Solution:
    def trap(self, height: List[int]) -> int:
        stack = []
        res = 0
        n = len(height)
        for i in range(n):
            while stack and height[i] > height[stack[-1]]:
                bottom = stack.pop()
                if not stack:
                    break
                left = stack[-1]
                width = i - left - 1
                h = min(height[left], height[i]) - height[bottom]
                res += width * h
            stack.append(i)
        return res
```

## 🧩 关键知识点 & 技巧
1. DP两套定义：`left_max`可以定义为「包含自身」或「不包含自身」，**两套定义不能交叉混用**；
2. 双指针记住顺序：**先算雨水，再更新墙高，再移动指针**；`l_max < r_max`代表左边是短板，处理左边；
3. 单调栈是横向按层算面积，DP与双指针是纵向按柱子算水量；
4. 单调栈要点：栈存下标；用`while`；pop后判断栈空；计算面积必须乘宽度；使用单调递减栈；
5. 雨水不能为负数，统一使用`max(0, xxx)`做保护；
6. 首尾柱子无法存雨水，长度≤2直接返回0。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. DP混用两套`left_max`定义，一部分包含自身，一部分不包含；
2. 双指针更新顺序颠倒：先更新max，再计算雨水，变量语义错乱；
3. 单调栈把`while`写成`if`，只能处理一层凹槽，连续低洼计算不全；
4. 单调栈栈内存高度而不是下标，无法计算凹槽宽度；
5. 单调栈pop之后不判断栈空，直接取`stack[-1]`触发索引越界；
6. 单调栈忘记乘以宽度width，只累加水位高度，结果偏小。

**避坑要点**
- DP数组定义自洽；
- 双指针严格遵守：算水 → 更新max → 移动指针；
- 单调栈：while循环、存下标、判栈空、乘宽度四件套缺一不可；
- 所有计算雨水的地方都要和0取max，防止负数。

## ✅ 总结
接雨水是经典困难题。
DP逻辑直观，适合初学理解；双指针空间最优，面试首选；单调栈思维角度不同，横向计算凹槽，也是84.柱状图中最大矩形的前置模板。
> 面试建议：优先写双指针；面试官追问再讲DP；单调栈作为拓展解法。
