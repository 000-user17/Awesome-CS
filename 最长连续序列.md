# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：128
- **题目名称**：最长连续序列 Longest Consecutive Sequence
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/longest-consecutive-sequence/
- **刷题日期**：2026\08\14
- **标签**：哈希表 / 集合

## 📝 题目大意
给定未排序整数数组 `nums`，找出数字连续的最长序列长度；序列元素不需要在原数组中连续。
要求实现时间复杂度 $\(O(n)\)$ 的算法。

> 可行思路对比
> 方案1：排序后遍历，时间 $O(n\log n)$，不满足题目时间限制；
> 方案2：暴力双层循环枚举，时间 $O(n^2)$，容易超时；
> 方案3：**哈希集合寻找序列起点（标准解法）**，达到 $\(O(n)\)$ 时间复杂度。

## 💡 解题思路
1. 将数组全部转为集合，实现 $\(O(1)\)$ 快速查询元素是否存在，同时自动去重；
2. 判断连续序列起点：如果 `num - 1` 不在集合中，说明当前 `num` 是一段连续序列的起始数字；
3. 从起点开始不断向后查找 `num、num+1、num+2……`，统计当前连续序列长度；
4. 更新全局最大长度，遍历结束后返回。

> 核心优化逻辑：只从序列起点开始向后拓展，**每个数字只会被访问一次**，保证整体线性时间。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，数组元素仅会被遍历两次（外层for循环、内层while循环）
- **空间复杂度**：$\(O(n)\)$，哈希集合存储全部数组元素

## 💻 AC 代码
```python
from typing import List
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        numSet = set(nums)
        maxCnt = 0
        for num in numSet:
            cnt = 0
            if num - 1 not in numSet:
                while num in numSet:
                    cnt += 1
                    num += 1
                maxCnt = max(maxCnt, cnt)
        return maxCnt
```

## 🧩 关键知识点 & 技巧
1. `set()` 集合查询 `in` 操作效率远高于列表 list；
2. `num - 1 not in numSet` 是保证线性复杂度的核心条件，避免重复扫描；
3. 数组存在重复数字无需额外处理，集合自动完成去重；
4. 空数组场景下，集合为空，循环不会执行，直接返回 `maxCnt=0`，天然兼容边界。

## ❌ 错题反思 / 踩坑记录
1. 使用列表进行 `in` 判断，查询为 $\(O(n)\)$，整体超时；
2. 缺少起点判断条件，每个数字都向后枚举，复杂度退化至 $O(n^2)$；
3. 在外层遍历原始数组 nums（而非集合），重复数字触发多次无效计算；
4. 忘记不断更新全局最长长度。

## ✅ 总结
经典哈希表应用题。
核心口诀：**找到连续序列起点，再向后延伸统计长度**。
