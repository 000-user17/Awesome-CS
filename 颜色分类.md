# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：75
- **题目名称**：颜色分类 Sort Colors
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/sort-colors/](https://leetcode.cn/problems/sort-colors/)
- **刷题日期**：2026\09\14
- **标签**：数组 / 双指针 / 三路快排

## 📝 题目大意
数组只有 0、1、2 三种数字，原地排序，0放最前面，1中间，2最后。
要求：**不能调用内置sort函数，原地修改数组**。
> 俗称荷兰国旗问题。

示例：
`nums = [2,0,2,1,1,0]` → 输出 `[0,0,1,1,2,2]`

> 简单思路：先统计0、1、2各自出现多少次，再覆盖回数组。统计法能做，但不是最优；面试更推荐**三路双指针（一次遍历）**。

## 💡 解题思路
### 三路快排思想（一次遍历，\(O(n)\)时间，\(O(1)\)空间）
定义三个区域：
- `p0`：**0区域的右边界**，p0左边全部是0，p0指向第一个不是0的位置
- `p2`：**2区域的左边界**，p2右边全部是2，p2指向第一个不是2的位置
- `cur`：当前遍历指针，用来扫描元素

规则：
1. `nums[cur] == 0`：交换 `nums[cur]` 和 `nums[p0]`，p0右移，cur右移
2. `nums[cur] == 1`：已经在正确区域，直接 cur右移
3. `nums[cur] == 2`：交换 `nums[cur]` 和 `nums[p2]`，**p2左移，cur不动**
> 为什么cur不动？交换过来的元素是从p2位置拿过来的，还没有检查，需要下一轮循环判断。

终止条件：`cur > p2`，p2右边已经全部是2，不用再看。

### 统计计数法（简单好写，两次遍历）
1. count0 count1 count2 统计0、1、2个数
2. 从头开始，先写count0个0，再count1个1，最后count2个2
优点：简单不容易写错；缺点：需要两次遍历数组。

## ⚙️ 复杂度分析
三路双指针版本：
- **时间复杂度**：$\(O(n)\)$，数组只遍历一遍
- **空间复杂度**：$\(O(1)\)$，原地修改，常数变量

计数法：
- **时间复杂度**：$\(O(n)\)$，两次遍历
- **空间复杂度**：$\(O(1)\)$

## 💻 AC 代码（三路双指针，面试推荐）
```python
class Solution:
    def sortColors(self, nums: list[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        p0 = 0
        p2 = len(nums) - 1
        cur = 0
        while cur <= p2:
            if nums[cur] == 0:
                nums[cur], nums[p0] = nums[p0], nums[cur]
                p0 += 1
                cur += 1
            elif nums[cur] == 1:
                cur += 1
            else: # ==2
                nums[cur], nums[p2] = nums[p2], nums[cur]
                p2 -= 1
```

计数法版本：
```python
class Solution:
    def sortColors(self, nums: list[int]) -> None:
        count0 = count1 = count2 = 0
        for num in nums:
            if num == 0:
                count0 +=1
            elif num ==1:
                count1 +=1
            else:
                count2 +=1
        idx = 0
        for _ in range(count0):
            nums[idx] = 0
            idx +=1
        for _ in range(count1):
            nums[idx] = 1
            idx +=1
        for _ in range(count2):
            nums[idx] = 2
            idx +=1
```

## 🧩 关键知识点 & 技巧
1. 荷兰国旗问题，本质是三路划分，快排partition的优化版本；
2. cur碰到2交换后**不能cur++**，交换过来的元素还没校验；
3. cur碰到0交换后可以cur++：因为p0位置一定是cur之前扫描过的位置，只能是1；
4. p0 ≤ cur ≤ p2 是区间不变量。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 交换2之后写了cur +=1，导致漏掉刚交换过来的元素；
2. while循环条件写成 cur < p2，漏掉p2位置元素；
3. 分不清p0、p2含义，指针移动方向写反；
4. 试图return数组，题目要求原地修改，不需要返回值。

**避坑要点**
> 交换 0：p0++ cur++
> 交换 2：p2-- cur保持不变

## ✅ 总结
75是经典双指针/三路划分题。计数法适合快速写对；三路双指针一次遍历，是面试考点。
关联题目：快排原理、215数组中的第K个最大元素。
