# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：98
- **题目名称**：验证二叉搜索树 Validate Binary Search Tree
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/validate-binary-search-tree/](https://leetcode.cn/problems/validate-binary-search-tree/)
- **刷题日期**：2026\09\09
- **标签**：二叉树 / DFS / 中序遍历

## 📝 题目大意
给你一个二叉树的根节点 root ，判断其是否是一个有效的二叉搜索树。
有效二叉搜索树定义如下：
1. 节点的左子树只包含 **严格小于** 当前节点的数。
2. 节点的右子树只包含 **严格大于** 当前节点的数。
3. 所有左子树和右子树自身必须也是二叉搜索树。

> 分析：
> ❗ 最容易踩坑：不能只判断「左孩子.val < 当前节点.val && 右孩子.val > 当前节点.val」。
> BST 的规则不是只约束直接子节点，而是**整棵左子树所有节点都小于当前节点；整棵右子树所有节点都大于当前节点**。
> 示例反例：`[5,4,6,null,null,3,7]`。局部父子关系满足，但 3 在根5的右子树，3 < 5，整体不合法。
> 两种主流解法：
> 1. DFS 递归，给每个节点带上合法取值上下界（推荐）；
> 2. 二叉树中序遍历，BST 的中序遍历结果一定是**严格递增序列**。

## 💡 解题思路（上下界递归版，优先掌握）
### 核心架构
递归函数传入当前节点，以及该节点允许取值范围 `(low, high)`：
1. 当前节点的值必须满足 `low < node.val < high`；不满足直接返回False。
2. 递归左子树：左子树全部必须小于当前节点值 → 上界更新为 `node.val`，下界保持不变。
3. 递归右子树：右子树全部必须大于当前节点值 → 下界更新为 `node.val`，上界保持不变。
4. 递归终止条件：节点为空，代表该分支合法，返回 True。

初始调用：根节点没有上下限制，区间 `(-inf, +inf)`。

### 中序遍历思路（迭代版，防止递归深度过大栈溢出）
二叉搜索树中序遍历顺序：左 → 根 → 右，遍历序列严格递增。
1. 使用栈迭代实现中序遍历；
2. 保存上一个访问节点的值 `prev`；
3. 每次弹出节点时，判断 `prev < 当前节点.val`；一旦不满足，返回False；
4. 更新 `prev`，继续遍历右子树；遍历完成返回True。

## ⚙️ 复杂度分析
**递归上下界版本**
- **时间复杂度**：$\(O(n)\)$，每个节点访问一次
- **空间复杂度**：$\(O(h)\)$，h为树高度；最坏链式二叉树 $\(O(n)\)$（递归栈开销）

**迭代中序版本**
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(h)\)$，栈存储节点

## 💻 AC 代码（递归上下界版本，面试首选）
```python
# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def isValidBST(self, root: TreeNode) -> bool:
        def dfs(node, low, high):
            # 空节点合法
            if not node:
                return True
            # 当前节点超出区间，不是BST
            if not (low < node.val < high):
                return False
            # 左子树：上限变成node.val；右子树：下限变成node.val
            return dfs(node.left, low, node.val) and dfs(node.right, node.val, high)
        
        return dfs(root, float("-inf"), float("inf"))
```

### AC 代码（迭代中序遍历版本）
```python
class Solution:
    def isValidBST(self, root: TreeNode) -> bool:
        stack = []
        prev = float("-inf")
        cur = root
        while stack or cur:
            # 一路走到最左节点
            while cur:
                stack.append(cur)
                cur = cur.left
            cur = stack.pop()
            # 严格递增，等于也不合法
            if cur.val <= prev:
                return False
            prev = cur.val
            cur = cur.right
        return True
```

## 🧩 关键知识点 & 技巧
1. BST 核心性质：**中序遍历严格递增**，这是BST最本质特征；
2. 上下界递归：把全局约束层层向下传递，保证子树所有节点满足范围限制；
3. 严格小于/大于：题目要求严格，`val ==` 的情况直接判定非法；
4. 递归深度问题：树极度倾斜时递归会栈溢出，工程/面试稳妥写法用迭代中序；
5. 边界值：节点值可以是 int 极值，所以用 `float('-inf')`、`float('inf')` 作为初始边界。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 只比较当前节点与左右孩子，忽略子树全部节点约束，反例 `[5,4,6,null,null,3,7]` 会误判True；
2. 判断条件写成 `low <= node.val <= high`，没有严格大小比较，相等值没有拦截；
3. 递归传参写反：左子树传入 high=low，右子树传入 low=node.val，区间搞反；
4. 中序遍历使用 `prev <= cur.val`，把相等当成合法；
5. 没有考虑节点值是 int 边界，使用固定极大/极小数字作为初始上下界，遇到边界样例报错。

**避坑要点**
- BST约束是**整棵子树**，不是父子局部；
- 记住关键词：**严格小于、严格大于**；
- 递归函数参数：左缩上界，右缩下界；
- 中序迭代模板是二叉树通用模板，需要熟练默写。

## ✅ 总结
验证二叉搜索树是二叉树经典面试题，考察对BST定义的理解，不是单纯二叉树遍历。
两种解法：
1. DFS上下界：直观翻译定义，代码简短，优先写；
2. 中序迭代：利用BST数学性质，规避递归栈溢出。
面试一定要主动提那个经典反例 `[5,4,6,null,null,3,7]`，说明不能只判断父子节点。

---
