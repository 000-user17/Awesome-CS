# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：31
- **题目名称**：下一个排列
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/next-permutation/
- **刷题日期**：2026\09\02
- **标签**：数组 / 双指针

## 📝 题目大意
整数数组的下一个排列，是字典序比当前更大的最近排列。
如果已经是字典序最大排列，将数组变为字典序最小（升序）。
要求：**原地修改数组，仅允许常数额外空间，不能返回新数组**。
示例：
`[1,2,3]` → `[1,3,2]`
`[2,3,1]` → `[3,1,2]`
`[3,2,1]` → `[1,2,3]`

> 重点难点：
> 1. 禁止暴力枚举全部排列，阶乘复杂度会超时；
> 2. 严格原地修改，\(O(1)\)额外空间；
> 3. 交换拐点元素后，后缀依然保持降序，可以直接反转，不需要sort排序；
> 4. 数组完全降序时整体反转，得到最小升序排列；
> 5. 需要处理存在重复数字的场景。

## 💡 解题思路
**核心思想：字典序排列四步算法**
1. **从后向前寻找拐点 i‑1**
从数组末尾向前遍历，找到第一个下标 `i‑1`，满足 `nums[i‑1] < nums[i]`。
`i` 位置往后的后缀子数组是**非递增（降序）**，后缀已经是该部分最大字典序；`i‑1` 是唯一可以增大的位置。
循环结束如果没有找到拐点，说明整个数组降序，直接反转整个数组得到最小排列。

2. **在i右侧寻找交换点 j‑1**
从i位置向后扫描，找到第一个不大于 `nums[i‑1]` 的下标j；`j‑1` 即为i右侧大于`nums[i‑1]`的最靠右元素。

3. **交换拐点 i‑1 和 j‑1**
交换后，`[i, n‑1]` 区间仍然维持降序，不会打乱原有降序性质。

4. **反转i到末尾的后缀区间**
降序反转直接变为升序，拿到后缀最小字典序，完成下一个排列。

**步骤拆解**
1. n = len(nums)，数组长度小于等于1直接返回；
2. i从n‑1向前扫描，定位拐点`i‑1`；
3. 找到拐点后，向后扫描确定j，交换`nums[i‑1]`与`nums[j‑1]`；
4. 使用双指针反转`[i, n‑1]`区间，完成后return；
5. 未找到拐点，说明数组是最大排列，循环反转整个数组。

示例推演：`nums = [2,3,1]`
1. i初始=2，`nums[1]=3>nums[2]=1`，i减为1；
2. `nums[0]=2 < nums[1]=3`，拐点`i‑1=0`；后缀`[3,1]`降序；
3. j从1向后扫描，`nums[1]=3>2`，`nums[2]=1`不大于2，j=2，`j‑1=1`；
4. 交换`nums[0]`和`nums[1]`得到`[3,2,1]`；
5. 反转i=1到末尾`[2,1]`，得到`[3,1,2]`。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，两次线性扫描 + 一次区间反转
- **空间复杂度**：$\(O(1)\)$，原地双指针操作，常数额外空间

## 💻 AC 代码（你提供版本）
```python
from typing import List
class Solution:
    def nextPermutation(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        if n <= 1:
            return
        i = n - 1
        while i > 0:
            if nums[i-1] < nums[i]:
                j = i
                while j < n and nums[j] > nums[i-1]:
                    j += 1
                nums[j-1], nums[i-1] = nums[i-1], nums[j-1]
                left, right = i, n-1
                while left < right:
                    nums[left], nums[right] = nums[right], nums[left]
                    left += 1
                    right -= 1
                return
            i -= 1
        for i in range(n // 2):
            nums[i], nums[n-i-1] = nums[n-i-1], nums[i]
```

## 🧩 关键知识点 & 技巧
1. 字典序性质：后缀降序，代表该后缀已经是最大排列，无法再变大；
2. 交换`i‑1`与`j‑1`之后，`[i,n‑1]`依旧保持降序，**直接双指针反转即可，不需要sort排序**；
3. j取i右侧大于`nums[i‑1]`的最靠右元素，这是保证交换后后缀维持降序的前提；
4. 找到拐点、交换、反转后缀完成后才return，不能交换完直接return而跳过反转；
5. 完全降序场景，执行数组整体反转，得到最小升序排列。

## ❌ 错题反思 / 踩坑记录
**曾经踩过的错误（早期错误版本）**
1. 交换i‑1、j‑1之后，额外再做一次错误交换，篡改数组元素；
2. 交换完成直接return，**没有反转i之后的后缀**，后缀依旧是降序，样例大量出错；
3. j的选取逻辑错误，随便选取大于nums[i‑1]的元素，交换之后后缀不再是降序，反转失效；
4. 试图使用sort排序后缀，时间复杂度退化$O(n\log n)$；
5. 混淆return位置，最大排列场景没有走到整体反转逻辑。

**避坑要点**
- 找拐点i‑1从后往前扫描；找j从i向后扫描；
- 交换拐点元素之后，**必须反转i到末尾区间，再return**；
- 后缀本身是降序，反转得到升序，不要调用sort；
- 全部降序时，执行整体数组反转；
- 函数无返回值，原地修改输入nums。

## ✅ 总结
31下一个排列属于数组双指针经典中等题。
核心四步：找拐点i‑1 → 定位交换点j‑1 → 交换元素 → 反转后缀。
不需要暴力生成全部排列，利用字典序降序后缀的性质原地完成，满足常数空间限制。
> 拓展：也可以采用从后向前找j的写法，逻辑等价。
