# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：300
- **题目名称**：最长递增子序列 Longest Increasing Subsequence
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/longest-increasing-subsequence/
- **刷题日期**：2026\08\20
- **标签**：动态规划 / 贪心 / 二分查找

## 📝 题目大意
给你一个整数数组 `nums`，找到其中**最长严格递增子序列**的长度。
子序列由数组派生，可以删除若干元素，不改变剩余元素相对顺序，不需要连续。
> 重点难点：严格递增，不能相等；子序列不要求连续；只需要求长度，不需要输出子序列。
> 示例：`nums = [10,9,2,5,3,7,101,18]`，最长严格递增子序列 `[2,3,7,101]`，输出4。

## 💡 解题思路
### 思路1：动态规划 O(n²)
**状态定义**
`dp[i]`：以 `nums[i]` **结尾**的最长严格递增子序列长度。

**状态转移**
遍历i前面所有j，`j < i`：
如果 `nums[j] < nums[i]`，说明`nums[i]`可以接在j结尾的子序列后面。
$$dp[i]=\max(dp[i],\; dp[j]+1)$$

**初始化**
每个元素自身构成长度为1子序列，`dp[i] = 1`。

**答案**
最长子序列不一定以数组最后一个元素结尾，取整个dp数组最大值。

### 思路2：贪心 + 手写二分 O(n log n)⭐面试最优
维护数组`tails`
- `tails[k]`：长度为`k+1`的递增子序列，**末尾元素尽可能小**。
> 贪心核心：相同长度子序列，末尾值越小，后面越容易接更大数字。

流程：
遍历每个数字`num`
1. 如果`num > tails[-1]`，直接append，得到更长子序列；
2. 否则，二分查找`tails`中第一个≥`num`的下标，替换该位置的值。

> ⚠️重要：`tails`数组本身不一定是真实子序列；**tails的长度才是LIS答案**。
> 严格递增：二分条件`target <= tails[mid]`；允许相等（非严格）需要修改二分判断。

## ⚙️ 复杂度分析
### DP O(n²)版本
- **时间复杂度**：$O(n^2)$，两层循环
- **空间复杂度**：$\(O(n)\)$，dp数组

### 贪心+手写二分 O(n log n)版本
- **时间复杂度**：$O(n\log n)$，外层遍历n次，每次二分$\log n$
- **空间复杂度**：$\(O(n)\)$，tails数组

## 💻 AC 代码
### ① DP O(n²)版本
```python
from typing import List
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        n = len(nums)
        dp = [1] * n
        res = 1
        for i in range(n):
            for j in range(0, i):
                if nums[i] > nums[j]:
                    dp[i] = max(dp[i], dp[j] + 1)
            res = max(res, dp[i])
        return res
```

### ② 贪心+手写二分 O(n log n)版本
```python
from typing import List
class Solution:
    def lengthOfLIS(self, nums: List[int]) -> int:
        n = len(nums)
        tails = [nums[0]]

        def binarySearch(target):
            l, r = 0, len(tails)
            while l < r:
                mid = (l + r) // 2
                if target <= tails[mid]:
                    r = mid
                else:
                    l = mid + 1
            return l

        for num in nums:
            if tails[-1] < num:
                tails.append(num)
            else:
                tails[binarySearch(num)] = num
        return len(tails)
```

## 🧩 关键知识点 & 技巧
1. DP：`dp[i]`定义是以`nums[i]`结尾，答案取全局最大值，**不是dp[-1]**；
2. 贪心二分：tails只保存最小末尾，数组本身不是真实子序列，只看`len(tails)`；
3. 严格递增 vs 非严格递增：
   - 严格递增：二分条件 `target <= tails[mid]`，bisect_left；
   - 允许相等：`target < tails[mid]`，bisect_right；
4. DP适合理解，数据量大（n>1000）会超时；面试优先写贪心+二分；
5. 手写二分区间`[0, len(tails)]`，左闭右开，是插入位置模板。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. DP直接返回`dp[-1]`，最长子序列不一定以数组末尾元素结束；
2. 误解tails数组，直接把tails当成输出子序列；
3. 严格递增题目，二分判断写成`<`，变成非严格逻辑；
4. 二分右边界写成`len(tails)-1`，插入位置计算错误；
5. dp数组忘记初始化为1，子序列至少包含自己，长度最小为1。

**避坑要点**
- DP状态定义一定要明确“以i结尾”；
- 贪心二分：append条件是`num > tails[-1]`，不要写`>=`；
- 手写二分左闭右开`l=0，r=len(tails)`，处理尾部插入场景。

## ✅ 总结
1. DP解法逻辑直观，适合理解子序列最优子结构，时间$O(n^2)$，大数据会超时；
2. 贪心+二分是本题最优解$O(n\log n)$，但tails数组不是真实子序列，只能求长度；
3. 区分严格/非严格递增，对应不同二分判断条件；
4. 该题是子序列DP经典模板，很多子序列变种题目以此为基础扩展。
