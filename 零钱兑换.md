# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：322
- **题目名称**：零钱兑换 Coin Change
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/coin-change/
- **刷题日期**：2026\08\14
- **标签**：动态规划 / 完全背包

## 📝 题目大意
给定不同面额的硬币数组 `coins`，每种硬币数量不限；给定总金额 `amount`，求出凑成该总金额所需**最少硬币数量**。若无法凑出目标金额，返回 -1。

> 思路对比
> 暴力DFS回溯：枚举所有硬币组合，存在大量重复子问题，效率低下容易超时；
> 最优方案：一维动态规划，属于**完全背包求最小值**经典模型。

## 💡 解题思路
1. 状态定义：`dp[i]` 代表凑出金额 `i` 需要的最少硬币数目；
2. 初始化：dp数组全部填充无穷大，表示初始状态不可达；`dp[0] = 0`，凑0元不需要任何硬币；
3. 状态转移方程：
$$dp[i] = \min(dp[i], dp[i-coin] + 1) \quad (i \ge coin)$$
含义：尝试选取当前硬币，凑出 `i-coin` 的最优方案再加上当前这一枚硬币；
4. 完全背包特性：金额从小到大正向遍历，允许硬币重复多次选取；
5. 遍历结束后，若 `dp[amount]` 依旧为无穷大，代表无法组合出目标金额，返回 -1。

## ⚙️ 复杂度分析
设硬币种类数量为 $m$，目标金额为 $amount$
- **时间复杂度**：$O(amount \times m)$
- **空间复杂度**：$\(O(amount)\)$，一维DP数组占用空间

## 💻 AC 代码
```python
from typing import List
class Solution:
    def coinChange(self, coins: List[int], amount: int) -> int:
        dp = [float('inf')] * (amount + 1)
        dp[0] = 0

        for coin in coins:
            for i in range(coin, amount + 1):
                if coin > i:
                    continue
                dp[i] = min(dp[i], dp[i - coin] + 1)
        
        return -1 if dp[amount] == float('inf') else dp[amount]
```

## 🧩 关键知识点 & 技巧
1. 硬币可以无限选取 → 完全背包；硬币只能选一次为01背包，二者遍历顺序不同；
2. 完全背包一维写法：金额正向遍历；01背包需要金额反向遍历；
3. 使用无穷大标记“不可达状态”，只有 `dp[0]` 初始化为0；
4. 本题目标是求最少数量，使用 `min`；同类求组合方案数题目使用累加 `+=`。

## ❌ 错题反思 / 踩坑记录
1. dp数组全部初始化为0，状态定义错误；
2. 混淆01背包与完全背包的遍历顺序，导致硬币重复选取/无法选取；
3. 未判断最终金额是否可达，直接返回 `dp[amount]`；
4. 循环区间设置错误，出现数组下标越界。

## ✅ 总结
完全背包经典模板题。
核心要点：**无限物品、一维DP、正向遍历、min求取最小数量**。

> 拓展区分：
> 硬币每种最多选1枚（01背包）：内层循环金额改为从大到小逆序遍历，不需要对硬币数组排序。
