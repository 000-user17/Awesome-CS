```
# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：538
- **题目名称**：把二叉搜索树转换为累加树
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/convert-bst-to-greater-tree/
- **刷题日期**：2026\09\02
- **标签**：二叉搜索树、树、DFS、逆中序遍历

## 📝 题目大意
给定二叉搜索树，节点值各不相同。将每个节点的值更新为：原节点值 + 树中所有比该节点大的节点值之和。
BST性质：右子树全部大于根，左子树全部小于根。

> 重点难点：
> 1. 利用BST有序特性，不需要收集全部节点再二次遍历；
> 2. 使用逆中序遍历（右‑根‑左），按从大到小访问节点；
> 3. 维护累加和，边走边原地修改节点值；
> 4. 递归实现代码简洁；树深度很大时递归会栈溢出，需要栈迭代版本；
> 5. 注意累加变量的作用域，递归过程需要持续保存状态。

## 💡 解题思路
**核心思想：逆中序遍历（右 → 根 → 左）**
普通中序 左‑根‑右：BST从小到大输出。
逆中序 右‑根‑左：BST从大到小输出。

维护累加变量 preSum：保存已经遍历完成、比当前节点更大的节点总和。
1. 递归优先访问右子树（数值更大的节点）
2. 当前节点：节点值 = 节点原值 + preSum；更新preSum为新的节点值
3. 递归访问左子树（数值更小的节点）

迭代（栈）思路：
1. 使用栈模拟递归，cur指针游走，只把非空节点压入栈；
2. 循环一直走向最右节点，沿途压栈；
3. pop取出节点进行数值更新；
4. cur转向左子树，继续循环；
5. 空树可以天然兼容，不需要额外处理。

步骤拆解
1. preSum初始为0；
2. 递归dfs(node)：node为空直接返回；dfs(node.right)；更新当前节点；dfs(node.left)；
3. 迭代版：cur从root出发，while cur or stack 循环，完成逆中序；
4. 原地修改树节点，返回root。

示例推演
原BST序列 [0,1,2,3,4,5,6,7,8]
逆中序访问顺序：8,7,6,5,4,3,2,1,0
preSum=0
8 →8+0=8 preSum=8
7 →7+8=15 preSum=15
6 →6+15=21 preSum=21
依次完成所有节点更新。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n)$，每个节点遍历恰好一次
- **空间复杂度**：$O(h)$，h为树的高度；递归调用栈 / 迭代栈开销。平衡树h=logn，最坏斜树h=n。

## 💻 AC 递归代码
```python
from typing import Optional
# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def convertBST(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        self.preSum = 0
        def dfs(node):
            if not node:
                return
            dfs(node.right)
            node.val += self.preSum
            self.preSum = node.val
            dfs(node.left)
        dfs(root)
        return root
```

## 💻 AC 迭代（栈）代码
```python
from typing import Optional
# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def convertBST(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        preSum = 0
        stack = []
        cur = root
        while cur or stack:
            while cur:
                stack.append(cur)
                cur = cur.right
            cur = stack.pop()
            cur.val += preSum
            preSum = cur.val
            cur = cur.left
        return root
```

## 🧩 关键知识点 & 技巧
1. BST中序遍历得到升序；**逆中序（右根左）得到降序序列**；
2. 不需要把全部节点存数组再二次遍历；遍历同时原地更新；
3. preSum保存已经处理过的更大节点总和；递归版本使用实例变量保存状态；
4. 树很深的时候递归会栈溢出，可以用栈迭代逆中序；
5. 直接修改原树，不需要新建节点；
6. 迭代模板：`cur = root; while cur or stack:`，只压入非None节点，不要手动提前append(root)。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 写成普通中序（左根右），从小到大遍历，累加逻辑完全颠倒；
2. preSum写在dfs内部，每次递归重置，累加状态丢失；递归要用外层/实例变量；
3. 先处理左子树再处理右子树，遍历顺序错误；
4. 迭代错误手动提前append(root)，栈内存留旧节点，造成无限循环超时；
5. 迭代把None压入栈，访问node.val触发空指针异常；
6. 函数返回preSum数字，题目要求返回树root节点；
7. 手动append左子节点，破坏栈迭代模板逻辑。

**避坑要点**
- 遍历顺序必须：右 → 当前节点 → 左；
- preSum是跨递归的累加状态，不能放在dfs函数内部；
- 迭代标准模板：cur变量游走，只压非空节点，pop出来处理，处理后不要残留节点在栈中；
- BST的性质：右边的值全部大于根，左边全部小于根。

## ✅ 总结
538累加树是BST经典题。
利用逆中序遍历从大到小访问节点，维护累加和原地修改节点。
BST遇到有序相关问题，优先思考中序/逆中序遍历。
递归写法简洁；深度大的树优先使用栈迭代，避免递归栈溢出。
```
