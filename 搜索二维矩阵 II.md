# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：240
- **题目名称**：搜索二维矩阵 II
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/search-a-2d-matrix-ii/
- **刷题日期**：2026\08\29
- **标签**：数组 / 二分查找 / 矩阵
## 📝 题目大意
编写一个高效的算法来搜索 m x n 矩阵 matrix 中的一个目标值 target。该矩阵具有以下特性：
1. 每行的元素从左到右升序排列。
2. 每列的元素从上到下升序排列。
若矩阵中存在目标值返回true，不存在则返回false。
> 重点难点：不能直接使用普通二维二分；左上角起点无法唯一确定搜索方向，需要选择合适起点，每次直接排除一整行或一整列，保证高效。
## 💡 解题思路
### 核心思想：选择右上角作为起点线性查找
起点选右上角元素（也可以选左下角），利用行列有序特性：
- 如果当前元素 == target：直接找到目标，返回True
- 如果当前元素 > target：当前列下方所有数字都更大，直接**左移一列**
- 如果当前元素 < target：当前行左侧所有数字都更小，直接**下移一行**
> 为什么不选左上角：左上角数字偏小，当小于target时，既可以右移也可以下移，无法排除区域，不能直接缩小搜索范围。
**步骤拆解**
1. 边界处理：矩阵为空或者矩阵第一行为空，直接返回False；
2. 初始化坐标：`row = 0`（第一行），`col = len(matrix[0]) -1`（最后一列，右上角）；
3. 在坐标不越界的前提下循环判断当前位置数值；
4. 根据当前数值和target大小关系，移动行或者列指针；
5. 指针越界仍未找到，返回False。
示例推演：matrix = [
 [1,4,7,11,15],
 [2,5,8,12,19],
 [3,6,9,16,22],
 [10,13,14,17,24],
 [18,21,23,26,30]
]，target=5
初始：row=0，col=4，cur=15
15 >5 → col=3，cur=11
11>5 → col=2，cur=7
7>5 → col=1，cur=4
4<5 → row=1，cur=5
cur == target ✅，返回True
最终答案：True
## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(m+n)\)$，行指针最多移动m次，列指针最多移动n次
- **空间复杂度**：$\(O(1)\)$，仅使用常数变量，不开辟额外数组
## 💻 AC 代码
```python
from typing import List
class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        if not matrix or not matrix[0]:
            return False
        m = len(matrix)
        n = len(matrix[0])
        row, col = 0, n - 1
        while row < m and col >= 0:
            cur = matrix[row][col]
            if cur == target:
                return True
            elif cur > target:
                col -= 1
            else:
                row += 1
        return False
```
## 🧩 关键知识点 & 技巧
1. 有序矩阵搜索的经典贪心思路，核心是选**唯一分支的起点**；
2. 右上角/左下角是理想起点，每次比较可以直接舍弃一整行或者一整列；
3. 边界判断不能忽略空矩阵、空行的case，否则会索引报错；
4. 该思路优于逐行二分（O(m logn)），是本题最优解法。
## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 选择左上角作为起始点，遇到小于target的情况无法确定移动方向，逻辑失效；
2. 越界条件写错，出现数组索引越界报错；
3. 直接暴力遍历矩阵，时间复杂度O(mn)大数据超时；
4. 混淆【74.搜索二维矩阵】，直接使用整体二分（74是每行首元素大于上一行末尾，和本题条件不一样）。
**避坑要点**
- 本题和LeetCode74矩阵条件不同，不能套用74的一维二分思路；
- 循环终止条件必须同时满足行不超下界、列不小于0；
- 移动指针时一次只能移动一行或者一列，不能同时移动。
## ✅ 总结
本题属于有序矩阵经典贪心搜索题型，核心是巧妙选择右上角起点，利用行列升序特性每次缩减搜索范围，时间复杂度优于二分逐行查找，是面试高频最优解法。
