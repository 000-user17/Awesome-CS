# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：581
- **题目名称**：最短无序连续子数组 Shortest Unsorted Continuous Subarray
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/shortest-unsorted-continuous-subarray/](https://leetcode.cn/problems/shortest-unsorted-continuous-subarray/)
- **刷题日期**：2026\09\14
- **标签**：数组 / 双指针

## 📝 题目大意
给整数数组`nums`，找到**最短连续子数组**：只要把这个子数组升序排序，整个数组就变成升序。返回这个子数组长度。

示例1：
`nums = [2,6,4,8,10,9,15]`
输出：5
解释：子数组`[6,4,8,10,9]`排序后，整个数组有序。

示例2：
`nums = [1,2,3,4]`
输出：0（本身有序）

> 核心理解：
> 假设数组分为三段：`[左有序段] [中间无序段] [右有序段]`
> 左段全部 ≤ 中间段最小值；右段全部 ≥ 中间段最大值。
> 我们要找到中间段**最左边界left、最右边界right**，答案 = right-left+1

## 💡 解题思路
### 思路1：排序对比（简单好写，面试可以先说）
1. 复制数组并排序，得到`sorted_nums`
2. 从左往右找第一个 `nums[i] != sorted_nums[i]` → left
3. 从右往左找第一个 `nums[j] != sorted_nums[j]` → right
4. 没有找到任何不等元素返回0；否则返回 `right-left+1`
- 优点：逻辑简单，不容易想错
- 缺点：需要额外数组，时间\(O(nlogn)\)，不是最优

### ✅ 思路2：一次遍历，双指针（最优解法 \(O(n)\)）
核心：从左向右遍历，记录遍历到的**最大值max_val**；一旦当前数字 < max_val，说明这个位置一定在无序区间内，更新右边界`right`。
从右向左遍历，记录遍历到的**最小值min_val**；一旦当前数字 > min_val，说明这个位置一定在无序区间内，更新左边界`left`。

步骤：
1. 初始化：
    - max_val = nums[0], right = 0
    - min_val = nums[-1], left = len(nums)-1
2. 左→右遍历：
    - 如果 `nums[i] > max_val`，更新max_val
    - 否则，更新`right=i`（当前元素位置属于无序段）
3. 右→左遍历：
    - 如果 `nums[j] < min_val`，更新min_val
    - 否则，更新`left=j`（当前元素位置属于无序段）
4. 如果right >= left：返回 `right-left+1`；否则返回0（数组完全有序）

> 原理：
> 从左向右，max是已经走过部分的最大值；只要当前数比max小，代表它位置不对，必须被包含在待排序区间。
> 从右向左，min是已经走过部分的最小值；只要当前数比min大，代表它位置不对，必须被包含在待排序区间。

## ⚙️ 复杂度分析
一次遍历最优版本
- **时间复杂度**：$\(O(n)\)$，两次遍历数组
- **空间复杂度**：$\(O(1)\)$，常数变量

排序版本
- **时间复杂度**：$\(O(nlogn)\)$
- **空间复杂度**：$\(O(n)\)$

## 💻 AC 代码（\(O(n)\)双指针，面试推荐）
```python
class Solution:
    def findUnsortedSubarray(self, nums: List[int]) -> int:
        max_value = nums[0]
        n = len(nums)
        right = 0
        for i in range(n):
            if nums[i] < max_value:
                right = i
            else:
                max_value = nums[i]
        min_value = nums[-1]
        left = n - 1
        for j in range(n - 1, -1, -1):
            if nums[j] > min_value:
                left = j
            else:
                min_value = nums[j]
        if right <= left:
            return 0
        return right - left + 1 

```

排序对比版本（简易版）
```python
class Solution:
    def findUnsortedSubarray(self, nums: list[int]) -> int:
        sorted_nums = sorted(nums)
        left, right = 0, len(nums)-1
        while left <= right and nums[left] == sorted_nums[left]:
            left +=1
        while left <= right and nums[right] == sorted_nums[right]:
            right -=1
        return right-left+1
```

## 🧩 关键知识点 & 技巧
1. 不要直接找第一个下降点和最后一个下降点！例如`[1,3,2,2,2]`，单纯找拐点会出错；必须记录最大/最小值修正边界。
2. 双指针最优解法的本质：**确定哪些元素位置不对，被卷入无序区间**
3. 边界情况：严格升序数组，返回0；整个数组都无序，返回数组长度。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 只找第一个逆序位置、最后一个逆序位置，忽略中间更大/更小元素，边界偏小，答案错误；例`[1,3,2,2,2]`
2. 初始化left、right写反；
3. 忘记判断数组完全有序的情况，算出负数长度。

**避坑要点**
> 逆序拐点只是候选边界，**必须结合遍历过程的最大值、最小值修正左右边界**

## ✅ 总结
581数组双指针题。两种方案：排序对比（好写但慢）；一次遍历维护max/min，锁定无序子数组左右边界（最优\(O(n)\)）。
关联题目：978 最长湍流子数组
