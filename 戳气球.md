# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：312
- **题目名称**：戳气球
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/burst-balloons/
- **刷题日期**：2026\08\19
- **标签**：动态规划 / 区间DP

## 📝 题目大意
有 `n` 个气球，编号为 `0 ~ n‑1`，数组 `nums` 保存每个气球上的数字。
戳破第 `i` 个气球，可以获得硬币：`nums[i‑1] * nums[i] * nums[i+1]`。
- 如果 `i‑1` 或 `i+1` 越出数组边界，则视作该位置气球数字为 `1`。
- 戳破气球后，原本左右相邻的气球会直接挨在一起，改变后续戳气球的相邻关系。
求戳破全部气球能够拿到的最大硬币数量。

> 重点难点：正向模拟先戳哪个气球，戳完后左右合并，相邻边界动态改变，子问题无法独立复用，常规思路很难处理。
> 样例：`nums = [3,1,5,8]`，全部戳破最大硬币为 `167`。

## 💡 解题思路
### 核心思想：区间DP，逆向思考，枚举**最后戳破**的气球
正向思路：枚举最先戳破的气球，戳破后左右两段合并，边界发生变化，子问题状态互相干扰，无法拆分。

逆向转换思路：
考虑一段区间，不去想先戳谁；假设在区间内部，**k 是最后一个被戳破的气球**。
1. 定义 `dp[left][right]`：**开区间 (left, right)**，只戳中间 `left+1 ~ right‑1` 的气球；`left` 和 `right` 两个气球保留不戳，充当区间的左右边界。
2. 因为k是开区间`(left, right)`内部最后戳破的气球：区间内其余气球已经全部戳爆，但边界`left`、`right`仍然保留存活。此时戳k，k的左右邻居就是边界`left`与`right`，得到硬币：`new_nums[left] * new_nums[k] * new_nums[right]`。
3. 原问题被拆成两个独立子问题：先把左边开区间`(left,k)`全部戳完，再把右边开区间`(k,right)`全部戳完。

为统一处理原数组的越界边界为1，对原数组前后补虚拟气球：
`new_nums = [1] + nums + [1]`，数组长度变为 `n+2`。
虚拟边界下标：`0` 和 `n+1`，永远不被戳破。

状态转移公式：
$$
dp[left][right] = \max_{k=left+1}^{right-1}\big\{\;
dp[left][k] + dp[k][right] + new\_nums[left] * new\_nums[k] * new\_nums[right]
\;\big\}
$$

**初始化**
当 `right = left + 1`，开区间`(left,right)`中间没有气球，`dp[left][right] = 0`，没有硬币收益。

**步骤拆解**
1. 边界处理：复制原数组，前后补虚拟边界`1`得到`new_nums`；
2. 初始化二维dp数组全部为0；
3. `length`代表开区间`(left,right)`**内部气球的数量**，从小到大枚举 `length`：`1 ~ n`；
4. 枚举左端点`left`，计算右端点 `right = left + length + 1`，保证`right ≤ n+1`；
5. 在区间内部枚举k（最后戳破的气球），更新`dp[left][right]`取最大值；
6. 最终答案：`dp[0][n+1]`，虚拟边界0与n+1之间所有气球全部戳破的最大收益。

示例推演：`nums = [3,1,5,8]`
补虚拟边界：`new_nums = [1,3,1,5,8,1]`，n=4，求`dp[0][5]`
- length=1：处理内部只有1个气球的小区间，例如`dp[0][2]`、`dp[1][3]`等；
- length=2：处理内部2个气球区间，依赖已经算好的length=1子区间；
- length逐步增大直到length=4，计算最大区间`dp[0][5]`得到结果167。

## ⚙️ 复杂度分析
- **时间复杂度**：$\boldsymbol{O(n^3)}$，三层循环：枚举区间长度、枚举左边界left、枚举区间内最后戳破的k；n为原始气球数量。
- **空间复杂度**：$\boldsymbol{O(n^2)}$，二维dp数组大小为 $(n+2)\times(n+2)$。

## 💻 AC 代码
```python
from typing import List
class Solution:
    def maxCoins(self, nums: List[int]) -> int:
        n = len(nums)
        # 前后补充虚拟边界气球1
        new_nums = [1] + nums + [1]
        # dp[left][right]：开区间(left, right)内部气球全部戳破最大硬币
        dp = [[0] * (n + 2) for _ in range(n + 2)]

        # length：开区间内部气球数目，从1到n
        for length in range(1, n + 1):
            # left遍历，保证 right = left+length+1 <= n+1
            for left in range(0, n - length + 1):
                right = left + length + 1
                # k枚举区间内部最后戳破的气球
                for k in range(left + 1, right):
                    total = dp[left][k] + dp[k][right] + new_nums[left] * new_nums[k] * new_nums[right]
                    dp[left][right] = max(dp[left][right], total)
        return dp[0][n + 1]
```

## 🧩 关键知识点 & 技巧
1. 区间DP逆向思维：当删除元素会造成左右区间合并时，可以假设**该元素是最后被删除**，从而拆成左右互不干扰的子区间；
2. 虚拟边界技巧：数组前后补`1`，统一处理边界越界逻辑，不用额外if判断；
3. `dp[left][right]`是**开区间定义**：left、right作为边界保留不戳，只处理中间部分；不要混淆闭区间与开区间；
4. 区间DP遍历顺序：**先算小区间，再算大区间**；大区间的计算依赖已经求解完成的子小区间；
5. k的含义：k是区间内部最后戳破的气球，不是最先戳破。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 正向思路，枚举最先戳破的气球；戳破之后左右合并，边界动态变化，子问题无法复用，思路卡死；
2. 忘记前后补充虚拟边界`[1]`，边界条件处理混乱，容易越界；
3. 混淆开区间/闭区间，dp定义搞错，状态转移公式全部错乱；
4. 循环写错：`for length in (1, n+1)`，使用元组而非range，只会循环两次，小区间子问题没有计算；
5. left的range上界计算错误，导致right下标越界或者漏掉合法区间；
6. 遍历顺序颠倒，先算大区间再算小区间，子问题还未求解，dp得到错误值。

**避坑要点**
- `right = left + length + 1`，`length`是**区间内部气球数量**，不是下标差值；
- 虚拟边界0和n+1只充当边界，永远不能被选为k；
- 同类题型：多边形三角剖分的最低得分，同样使用“最后操作某个元素”的区间DP套路。

## ✅ 总结
戳气球是区间DP的经典困难题。解题最大门槛是逆向思维，放弃枚举最先戳破的气球，转而枚举区间内部**最后戳破的气球k**。
配合前后虚拟边界，定义开区间dp，从小到大枚举区间长度完成求解。
时间复杂度$O(n^3)$，题目数据范围适配该复杂度。
> 通用迁移经验：遇到移除元素后左右两段合并的问题，可以优先思考：假设该元素是最后被移除，拆分为左右独立子区间。
