# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：102
- **题目名称**：二叉树的层序遍历
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/binary-tree-level-order-traversal/](https://leetcode.cn/problems/binary-tree-level-order-traversal/)
- **刷题日期**：2026\09\08
- **标签**：二叉树 / BFS / 队列

## 📝 题目大意
给你二叉树的根节点 `root` ，返回其节点值的**层序遍历**。（即逐层地，从左到右访问所有节点）。
按层输出，每一层节点值放在一个子列表中，整体返回二维整数列表。
> 注意：返回的是节点的**val数值**，不是TreeNode对象。

## 💡 解题思路
**核心思想：BFS广度优先搜索，借助队列实现。队列先进先出，天然适合一层一层处理节点。**

**步骤拆解：**
1. 边界判断：如果根节点为空，直接返回空列表。
2. 初始化结果列表`res = []`；队列`deque`把根节点入队。
3. 队列不为空，说明还有节点需要处理：
    a. 记录当前这一层的节点数量 `level_size = len(q)`，固定本层节点总数。
    b. 新建当前层列表`cur_layer`，保存本层节点的值。
    c. 循环 `level_size` 次：
        - 队首弹出节点；
        - 将节点的值加入`cur_layer`；
        - 如果左孩子不为空，左孩子入队列；
        - 如果右孩子不为空，右孩子入队列。
    d. 本层处理完毕，把`cur_layer`加入总结果`res`。
4. 循环结束，返回res。

> 关键点：`level_size`必须在for循环之前获取，队列在循环中会不断加入下一层节点，不能循环内取len(q)。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，n为二叉树节点总数，每个节点入队、出队各一次。
- **空间复杂度**：$\(O(n)\)$，队列最多存储一层节点；最坏完全二叉树，最后一层约n/2个节点。

## 💻 AC 代码（Python）
```python
from collections import deque
from typing import List, Optional

# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        res = []
        if not root:
            return res
        
        q = deque([root])
        while q:
            level_size = len(q)
            cur_layer = []
            for _ in range(level_size):
                node = q.popleft()
                cur_layer.append(node.val)
                if node.left:
                    q.append(node.left)
                if node.right:
                    q.append(node.right)
            res.append(cur_layer)
        return res
```

## 🧩 关键知识点 \& 技巧
- 技巧1：BFS用`deque`，`popleft()`时间O(1)；不要用list的pop(0)，pop(0)是\(O(n)\)。
- 技巧2：队列里面存放**TreeNode节点对象**；结果列表只存放`node.val`整数值。
- 技巧3：`level_size = len(q)`，提前保存本层节点个数，隔离当前层和下一层。
- 技巧4：空树直接返回`[]`，不要返回`[[]]`，避免多出空数组。
- 易错点：把TreeNode对象直接append进结果，会报`Type is not JSON serializable: TreeNode`。

## ❌ 错题反思 / 踩坑记录
**错误1：结果列表append(node)而不是append(node.val)**
错误原因：把节点对象存入返回数组，LeetCode需要整数，序列化报错。
修正思路：`cur_layer.append(node.val)`。

**错误2：不提前存level_size，for循环写`for _ in range(len(q))`**
错误原因：循环过程中持续往队列加入子节点，`len(q)`不断变大，会把下一层节点混到本层处理。
修正思路：进入while循环第一行，`level_size = len(q)`。

**错误3：初始化res = [[]]**
错误原因：结果最开头多一个空的子数组，答案错误。
修正思路：`res = []`。

**错误4：使用普通list，pop(0)**
错误原因：list的pop(0)会移动全部元素，大数据量时间超时。
修正思路：使用`collections.deque`，`popleft()`。

## ✅ 总结
1. 层序遍历标准BFS模板，背下来；很多变体：层序求深度、层序从底向上、锯齿层序都基于这套模板修改。
2. 队列存节点，结果存节点值；提前记录本层节点数隔离层与层。
3. deque的popleft是O(1)，BFS必用。
4. 变体小提示：
    - 107二叉树的层序遍历Ⅱ：最后把res反转即可；
    - 103二叉树的锯齿形层序遍历：奇数层、偶数层交替反转本层数组。
