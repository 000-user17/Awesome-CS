# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：55
- **题目名称**：跳跃游戏 Jump Game
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/jump-game/](https://leetcode.cn/problems/jump-game/)
- **刷题日期**：2026\09\14
- **标签**：贪心 / 动态规划 DP

## 📝 题目大意
非负整数数组`nums`，起点在下标0。`nums[i]`代表在i位置最多可以向前跳`nums[i]`步。判断**能否到达最后一个下标**。

示例1：
`nums = [2,3,1,1,4]` → true
解释：0→1→4，可以跳到终点

示例2：
`nums = [3,2,1,0,4]` → false
解释：走到下标3（值为0），无法继续前进，永远到不了4

> 两种解法：贪心（最优，面试首选）；DP（辅助理解，不推荐大数据用）

## 💡 解题思路
### 思路1：贪心（最优）
遍历数组每个位置`i`：
1. 如果当前下标`i > max_reach`：说明**连i这个位置都到不了**，直接return False
2. 更新最远可达距离：`max_reach = max(max_reach, i + nums[i])`
3. 只要`max_reach >= len(nums)-1`：已经可以抵达终点，提前返回True

### 思路2：动态规划 DP
#### 状态定义
`dp[i]`：布尔值，代表**能否到达下标i**。

#### Base Case
`dp[0] = True`，起点一定可达。

#### 状态转移
对于位置`i`，只要存在任意一个 `j < i`，满足：
`dp[j] == True` **并且** `j + nums[j] >= i`
说明从j可以跳到i，那么`dp[i]=True`。
否则`dp[i]=False`。

> 理解：想要到达i，前面必须有一个可达的j，j的最远跳跃位置覆盖i。

## ⚙️ 复杂度分析
贪心版本：
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(1)\)$

DP版本：
- **时间复杂度**：$O(n^2)$，两层循环，大数据会超时
- **空间复杂度**：$\(O(n)\)$，dp数组

## 💻 AC 代码
### 贪心（推荐）
```python
class Solution:
    def canJump(self, nums: list[int]) -> bool:
        max_reach = 0
        n = len(nums)
        for i in range(n):
            if i > max_reach:
                return False
            max_reach = max(max_reach, i + nums[i])
            if max_reach >= n - 1:
                return True
        return True
```

### DP版本（仅学习理解，大数据超时）
```python
class Solution:
    def canJump(self, nums: list[int]) -> bool:
        n = len(nums)
        dp = [False] * n
        dp[0] = True # base case：起点可达
        
        for i in range(1, n):
            # 检查前面所有j，看是否有j能跳到i
            for j in range(i):
                if dp[j] and j + nums[j] >= i:
                    dp[i] = True
                    break # 找到一个即可，不用继续看其他j
        return dp[-1]
```

## 🧩 关键知识点 & 技巧
1. 贪心是本题最优解；DP适合理解问题本质，但时间复杂度高，`n`很大时超时，面试优先写贪心；
2. DP思路是自底向上，逐个标记每个位置是否可达；
3. 边界：`nums=[0]`，n=1，dp[0]=True，直接返回True；
4. DP可以做简单优化：记录前面最远可达位置，减少内层循环，但优化后基本就变成贪心思路。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. DP内层循环写反，i,j顺序混乱；
2. DP忘记break，重复无效判断；
3. 贪心里混淆`i+nums[i]`是下标，不是步数；
4. 边界：数组长度为1，nums=[0]，直接True，起点就是终点。

**避坑要点**
> DP只是用来理解题意，工程/面试写贪心；
> `i > max_reach` 是贪心核心判断。

## ✅ 总结
55两种解法：贪心维护最远可达下标，\(O(n)\)；DP数组标记每个位置是否可达O(n²)。
关联题目：45 跳跃游戏II（求最少跳跃次数）
