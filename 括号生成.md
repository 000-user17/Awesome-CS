# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：22
- **题目名称**：括号生成
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/generate-parentheses/
- **刷题日期**：2026\08\29
- **标签**：回溯 / 深度优先搜索 DFS
## 📝 题目大意
数字 n 代表生成括号的对数，请你设计一个函数，用于能够生成所有可能的并且有效的括号组合。
有效括号需要满足两点：
1. 任意前缀中，左括号数量 ≥ 右括号数量；
2. 最终左括号总数、右括号总数都等于n。
> 重点难点：不能暴力枚举所有括号再筛选（效率低）；利用剪枝，只去生成合法的组合。
## 💡 解题思路
### 核心思想：回溯（DFS）+ 剪枝
维护两个变量 `open`（已使用左括号数量）、`close`（已使用右括号数量）：
1. 当 `open < n`：可以继续添加左括号；
2. 当 `close < open`：才可以添加右括号（保证前缀合法）；
3. 当 `open == n && close == n`：当前字符串是合法组合，加入结果。

**步骤拆解**
1. 初始化结果列表；
2. 定义回溯函数，参数：当前字符串、已用左括号数open、已用右括号数close；
3. 终止条件：open和close都等于n，收集答案；
4. 分支1：open < n，追加`(`，递归，递归结束回溯（撤销选择）；
5. 分支2：close < open，追加`)`，递归，递归结束回溯（撤销选择）；
6. 初始调用回溯函数，传入空字符串，open=0，close=0。

示例推演：n=2
初始：s="" open=0 close=0
open<2 → s="(", open=1 close=0
    open<2 → s="((", open=2 close=0
        不能再加左括号，close<2 → s="(()", open=2 close=1
            close<2 → s="(())", open=2 close=2 ✅ 存入res
    close<1 → s="()", open=1 close=1
        open<2 → s="()(", open=2 close=1
            close<2 → s="()()", open=2 close=2 ✅ 存入res
最终答案：["(())","()()"]

## ⚙️ 复杂度分析
- **时间复杂度**：$O(\frac{1}{n+1}C_{2n}^{n})$，卡特兰数，是合法括号组合的数量
- **空间复杂度**：$\(O(n)\)$，递归栈深度最多为2n，不算返回结果存储空间
## 💻 AC 代码
```python
from typing import List
class Solution:
    def generateParenthesis(self, n: int) -> List[str]:
        res = []
        def backtrack(s: str, open_cnt: int, close_cnt: int):
            if open_cnt == n and close_cnt == n:
                res.append(s)
                return
            if open_cnt < n:
                backtrack(s + "(", open_cnt + 1, close_cnt)
            if close_cnt < open_cnt:
                backtrack(s + ")", open_cnt, close_cnt + 1)
        backtrack("", 0, 0)
        return res
```
## 🧩 关键知识点 & 技巧
1. 核心剪枝条件：**右括号数量不能超过左括号**，直接砍掉大量非法分支；
2. 属于经典回溯题型，每次二选一：加左括号 / 加右括号；
3. 字符串直接拼接写法隐式回溯，不需要手动pop；如果用列表存字符，则需要pop完成回溯；
4. 结果总数是卡特兰数。
## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 不做剪枝，直接暴力生成全部2n长度的括号字符串再校验，n稍大就超时；
2. 右括号判断条件写成 `close < n`，会出现像 `)))(((` 这类非法前缀；
3. 终止条件写错，open和close不等时就收集结果。
**避坑要点**
- 右括号添加前提一定是 close < open；
- 左右括号上限都是n，左括号最多放n个；
- 递归终止必须左右括号同时用完。
## ✅ 总结
经典回溯剪枝题目，不需要生成全部情况再过滤，依靠括号合法特性提前剪枝，是面试高频DFS基础题。
