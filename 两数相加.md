# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：2
- **题目名称**：两数相加 Add Two Numbers
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/add-two-numbers/
- **刷题日期**：2026\09\04
- **标签**：链表 / 模拟

## 📝 题目大意
两个非空链表，代表两个非负整数。**数字逆序存储**，每个节点存1位数字。
例如：`l1 = [2,4,3]` 代表数字 `342`；`l2 = [5,6,4]` 代表 `465`；相加 `342+465 = 807`，输出链表 `[7,0,8]`。

> 关键点：
1. 链表是逆序：链表头是**个位**，往后依次十位、百位；
2. 要处理**进位 carry**；
3. 两个链表长度可能不一样；
4. 循环结束后，**如果最后还有进位，必须新建节点保存进位**。

> 分析：
不能把链表直接转整数再相加，链表可以很长，会溢出。
直接**按位模拟竖式加法**，同时遍历两个链表，逐位相加，维护进位。
用**虚拟头结点哨兵**简化链表构建，不用处理头节点特殊逻辑。

## 💡 解题思路
### 核心架构
1. 虚拟头节点 `dummy`，用来挂载结果链表；`cur`指针指向当前结果链表尾部。
2. `carry` 保存进位，初始=0。
3. while循环：只要 `l1不为空 或者 l2不为空 或者 carry>0`，就继续算。
4. 取出当前位数值：
    - 如果l1不为空，取l1.val，l1后移；否则取0
    - 如果l2不为空，取l2.val，l2后移；否则取0
5. 本位总和：`sum_val = val1 + val2 + carry`
6. 当前位数字：`sum_val % 10`；新进位：`sum_val // 10`
7. 创建新节点，接到cur后面，cur向后移动。

> 循环条件非常重要：**即使两个链表都遍历完，如果carry还有1，也要再跑一轮，生成最后的进位节点**。
例子：`999 + 1 = 1000`，两个链表走完，carry=1，必须新增节点存最高位1。

### 执行流程示例
l1=[2,4,3], l2=[5,6,4]
carry初始=0
1. val1=2,val2=5 sum=7 → 当前位7，carry=0 →节点7
2. val1=4,val2=6 sum=10 → 当前位0，carry=1 →节点0
3. val1=3,val2=4 sum=3+4+1=8 → 当前位8，carry=0 →节点8
l1 l2都空，carry=0退出。结果链表：7 →0 →8。

## ⚙️复杂度分析
- **时间复杂度**：$\(O(max(m,n))\)$，m、n两个链表长度，遍历一遍链表。
- **空间复杂度**：$\(O(max(m,n)+1)\)$，新链表存储结果，最多多一个进位节点。

## 💻 AC代码
```python
# Definition for singly‑linked list.
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

class Solution:
    def addTwoNumbers(self, l1: ListNode, l2: ListNode) -> ListNode:
        dummy = ListNode()
        cur = dummy
        carry = 0

        while l1 or l2 or carry:
            v1 = l1.val if l1 else 0
            v2 = l2.val if l2 else 0

            sum_val = v1 + v2 + carry
            digit = sum_val % 10
            carry = sum_val // 10

            cur.next = ListNode(digit)
            cur = cur.next

            if l1:
                l1 = l1.next
            if l2:
                l2 = l2.next

        return dummy.next
```

## 🧩关键知识点 &技巧
1. 链表逆序：链表头部对应数字个位，直接从左往右按位相加，不用反转链表。
2. **虚拟头结点dummy**，不用单独处理第一个节点，非常经典链表题套路。
3. while条件：`l1 or l2 or carry`，**carry不能漏掉**，处理最后多出的进位。
4. 某一个链表遍历完之后，对应位置取0继续相加。
5. 不要转成整数再相加：链表长度很大会整型溢出，面试不能这么写。

## ❌错题反思 /踩坑记录
1. while条件只写 `while l1 and l2`：短链表结束循环直接退出，长链表剩下的数字没处理。
2. while条件写 `while l1 or l2`，漏掉判断carry：`999 + 1` 最后进位1丢失，结果少最高位。
3. 忘记更新carry，或者digit/carry计算写反。
4. cur指针没有向后移动，死循环，链表变成环。
5. 返回返回cur，而不是`dummy.next`，cur已经跑到链表末尾。

**避坑要点**
- 循环条件一定要带上 `carry`；
- 返回结果是 `dummy.next`，不是dummy；
- 一个链表走完，对应取值给0，不要直接break。

## ✅总结
链表高频模拟题。模拟竖式加法，逆序链表天然适合逐位相加；虚拟头简化新建链表逻辑；重点处理链表不等长、末尾进位。
如果你要，我可以手推一遍 999 + 1 的完整执行流程。
