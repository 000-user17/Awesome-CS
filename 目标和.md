# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：494
- **题目名称**：目标和 Target Sum
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/target-sum/
- **刷题日期**：2026\08\14
- **标签**：动态规划 / 01背包

## 📝 题目大意
给定非负整数数组 nums 和整数 target，在每个数字前添加 `+` 或 `-`，拼接成表达式，求运算结果等于 target 的表达式方案总数。

> 可行思路对比
> 方案1：暴力DFS枚举所有正负组合，时间复杂度 $O(2^n)$，数据量大时超时；
> 方案2：数学转化 + 01背包DP（最优解法）。

## 💡 解题思路
### 数学转化
设选取正数集合总和为 $sumA$，负数集合总和为 $sumB$，数组全部元素总和 $\(sums=sumA+sumB\)$。
根据题意：
$$sumA - sumB = target$$
联立推导：
$$sumA = \frac{sums + target}{2}$$
前置约束：
1. $\(sums+target\)$ 必须为偶数，否则不存在合法方案；
2. $sumA$ 不能小于0，直接返回0。

原问题等价转换：**从数组中选出若干数字，总和恰好等于 sumA，求选取方案总数**，属于标准01背包求方案数模型。

### DP逻辑
1. 状态定义：$\(dp[j]\)$ 代表选出数字总和等于 $j$ 的方案数量；
2. 初始化：$\(dp[0]=1\)$，总和为0，不选取任何数字，存在1种方案；
3. 状态转移：01背包，内层循环容量**逆序遍历**，避免同一个元素重复选取；
$$\(dp[j]\) = \(dp[j]\) + dp[j-num]$$
4. 最终答案为 $\(dp[sumA]\)$。

## ⚙️ 复杂度分析
设数组长度为 $n$，背包容量为 $sumA$
- **时间复杂度**：$O(n \times sumA)$
- **空间复杂度**：$\(O(sumA)\)$，一维DP数组优化空间

## 💻 AC 代码
```python
from typing import List
class Solution:
    def findTargetSumWays(self, nums: List[int], target: int) -> int:
        n = len(nums)
        sums = sum(nums)
        if (target + sums) % 2 == 1:
            return 0
        sumA = (target + sums) // 2
        if sumA < 0:
            return 0
        dp = [0] * (sumA + 1)
        dp[0] = 1
        for num in nums:
            for i in range(sumA, num - 1, -1):
                dp[i] += dp[i - num]
        return \(dp[sumA]\)
```

## 🧩 关键知识点 & 技巧
1. 数学等式转换是本题核心突破口，将正负表达式问题转化为子集和计数背包问题；
2. 每个元素只能选或不选，属于01背包，容量必须逆序遍历；
3. $\(dp[0]=1\)$ 是基础初始条件，极易遗漏；
4. 提前奇偶判断、sumA负数判断进行剪枝，减少无效计算。

## ❌ 错题反思 / 踩坑记录
1. 未做数学转化，直接使用DFS暴力搜索，大数据用例超时；
2. 误用完全背包正向遍历，造成元素重复选取，结果错误；
3. 忽略 `sumA<0` 边界，产生非法背包容量；
4. 曾经错误使用bool数组dp，只能判断能否凑出，无法统计方案数量；
5. range起始下标写错，数组下标越界。

## ✅ 总结
经典01背包应用题。
核心要点：**数学转换简化问题，01背包逆序遍历，dp数组累加统计方案数目。**
