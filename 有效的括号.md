# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：20
- **题目名称**：有效的括号
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/valid-parentheses/
- **刷题日期**：2026\09\03
- **标签**：栈、字符串

## 📝 题目大意
给定字符串 `s`，只包含 `()` `{}` `[]` 三种括号。判断字符串是否有效。
有效条件：
1. 左括号必须有相同类型右括号闭合；
2. 左括号必须按正确顺序闭合；
3. 每个右括号都有对应左括号。

> 隐含小条件：字符串长度奇数直接返回 false，不可能合法。

## 💡 解题思路
**核心思想：栈（后进先出 LIFO）**
遇到**左括号**：压入栈；
遇到**右括号**：栈顶必须是与之匹配的左括号，弹出栈顶；
如果栈顶不匹配 / 栈已经为空，直接无效。

建立映射字典：**右括号 → 对应的左括号**
`match = {')':'(', '}':'{', ']':'['}`

步骤拆解：
1. 建立括号映射字典 key：右括号，value：对应左括号。
2. 初始化空栈。
3. 遍历字符串每一个字符 c：
   - 如果 c 是**右括号**（在match的key中）：
     1. 栈不为空，取出栈顶；栈为空直接 return False；
     2. 栈顶元素 不等于 `match[c]` → 括号不匹配，返回False；
     3. 匹配成功，弹出栈顶。
   - 如果 c 是**左括号**：压入栈。
4. 遍历结束后：**栈必须为空**。
> 栈不为空：代表还有残留左括号，没有被右括号闭合，无效。

举例：`"([)]"`
- `(`入栈；`[`入栈；
- `)`右括号，匹配左括号`(`，栈顶现在是`[`，`[ != (` →不匹配，直接False。完美拦截交叉错误括号。

> 为什么栈适合：后出现的左括号，要优先闭合。后进先出。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，n字符串长度，每个字符入栈出栈最多一次
- **空间复杂度**：$\(O(n)\)$，最坏全是左括号，栈存全部字符

## 💻 AC 代码（Python）
```python
class Solution:
    def isValid(self, s: str) -> bool:
        match = {')':'(', '}':'{', ']':'['}
        stack = []
        for c in s:
            if c in match:
                # 右括号
                if not stack:
                    return False
                top = stack.pop()
                if top != match[c]:
                    return False
            else:
                # 左括号入栈
                stack.append(c)
        # 栈必须全部清空
        return len(stack) == 0
```

## 🧩 关键知识点 & 技巧
- 技巧1：字典映射：**右括号做key**，不要左括号做key；遇到右括号直接查需要什么左括号。
- 技巧2：奇数长度可以提前剪枝：`if len(s) % 2 ==1: return False`，加速。
- 技巧3：遍历结束一定要判断栈为空。比如 `"((("，遍历全程不会触发不匹配，但是栈残留左括号，是无效。

- 易错点：
  1. 遇到右括号栈已经空，没有判断直接pop，报索引错误；
  2. 遍历完直接return True，忘记判断栈是否为空；
  3. 映射字典写反：左括号当key，逻辑写复杂；
  4. 不能用计数变量：`"([)]"`计数会认为合法，但是实际交叉括号是非法，**计数无法处理顺序，必须栈**。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 使用单纯计数器，只统计括号数量，忽略顺序，`([)]`会误判true；
2. 右括号到来栈为空，仍然执行pop，程序报错；
3. 循环结束直接return True，残留左括号没有校验；
4. 映射字典key value写颠倒。

**修正思路：**
1. 括号匹配顺序问题必须栈，不能只用计数；
2. pop之前先判断栈是否非空；
3. 返回结果必须校验栈为空。

## ✅ 总结
20 有效的括号，栈经典入门题。
> 左括号压栈；右括号看栈顶是否匹配；栈空代表全部匹配完成。
> 记忆：右括号查字典拿期望左括号，和栈顶对比；最后栈必须清空。

> 拓展：最长有效括号 32，是本题的衍生困难题。
