# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：438
- **题目名称**：找到字符串中所有字母异位词 Find All Anagrams in a String
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/find-all-anagrams-in-a-string/
- **刷题日期**：2026\08\14
- **标签**：滑动窗口、双指针、字符频次统计

## 📝 题目大意
给定两个字符串 s 和 p，找到 s 中所有 p 的异位词子串，返回所有子串起始索引。
字母异位词：字符组成、每个字符出现次数完全相同，仅顺序不一致。

> 思路对比
> 暴力解法：截取子串排序对比，时间复杂度过高，大数据超时；
> 当前实现：**固定长度滑动窗口 + 共用频次数组**。

## 💡 解题思路
设 `lenP = len(p)`，异位词子串长度一定等于 lenP，采用固定窗口滑动：
1. 初始化频次数组 `alphaCnt`，统计 p 中每个字符出现次数；
2. 双指针 `start、end` 构成窗口，窗口最大长度限制为 lenP；
3. 右指针不断右移，每纳入一个字符，对应频次 `-1`；
4. 当窗口长度达到 lenP 时，调用比较函数：若数组全部为0，代表当前窗口是p的异位词，记录 start；
5. 窗口向右滑动一格：移出左指针字符，对应频次 `+1`，`start` 右移，继续迭代。

> 原理：频次数组全部为0，表示窗口内各字符数量与 p 完全抵消匹配。

## ⚙️ 复杂度分析
令 `lenS = n`，字符集为26个小写字母
- **时间复杂度**：$O(26\cdot n)$；窗口线性遍历，每次比较最多循环26次常数开销
- **空间复杂度**：$\(O(1)\)$，固定长度26数组，常数空间

## 💻 AC 代码
```python
from typing import List
class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
        def ctod(c):
            return ord(c) - ord('a')
        alphaCnt = [0] * 26
        for c in p:
            alphaCnt[ctod(c)] += 1
        
        lenP = len(p)
        lenS = len(s)
        ret = []
        if lenP > lenS:
            return ret
        start = 0
        end = 0
        def compare():
            for j in range(26):
                if alphaCnt[j] != 0:
                    return False
            return True
        while end < lenS:
            while end - start + 1 <= lenP:
                alphaCnt[ctod(s[end])] -= 1
                end += 1
            if compare():
                ret.append(start)
            alphaCnt[ctod(s[start])] += 1
            start += 1
        
        return ret
```

## 🧩 关键知识点 & 技巧
1. 异位词长度必须相等，因此使用**固定大小滑动窗口**；
2. 使用单个数组充当差额计数器，减少额外空间开销；
3. 字符映射函数 `ord(c)-ord('a')` 快速映射到0~25下标；
4. 窗口填满后再进行匹配判断，匹配成功记录起始位置 `start`。

## ❌ 错题反思 / 踩坑记录
1. 忘记提前判断 `lenP > lenS`，造成无效循环；
2. 滑动窗口移出字符时，忘记把频次加回来，数组状态持续错乱；
3. 混淆窗口边界，`end-start+1` 窗口长度计算出错；
4. 每次完整遍历26个元素进行对比，存在优化空间（可用match变量记录匹配字符种类，省去循环）。

## ✅ 总结
固定窗口滑动窗口经典题型。
核心要点：**异位词长度一致，频次数组记录字符差额，窗口填满后校验匹配。**
