# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：121
- **题目名称**：买卖股票的最佳时机
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/
- **刷题日期**：2026\08\19
- **标签**：动态规划 / 数组 / 贪心

## 📝 题目大意
给定数组 `prices`，`prices[i]` 表示第 i 天股票价格。只能选择**某一天买入，在未来不同的一天卖出**，最多完成一次交易，求可以获取的最大利润。
如果无法获取利润，返回 0。
> 重点难点：卖出必须在买入之后；仅允许一次完整买卖；可以选择不交易。
> 示例：`prices = [7,1,5,3,6,4]`，第2天价格1买入，第5天价格6卖出，最大利润为5。

## 💡 解题思路
### 思路1：贪心算法（本题最优，直观简单）
**核心思想**：把每一天假设为卖出日，维护遍历过程中**历史最低股价**作为最佳买入点。
对于每一天：
1. 以当天价格卖出，利润 = 当前股价 − 历史最低买入价；
2. 更新全局最大利润；
3. 更新历史最低股价，如果今天价格更低，则后续可以选择今天买入。

> 注意顺序：先计算今天卖出的利润，再更新最低价格；保证买入一定发生在卖出之前。

### 思路2：动态规划‑滚动变量（股票系列通用模板）
定义两个状态：
- `hold`：第i天结束后，**持有股票**的最大利润
- `not_hold`：第i天结束后，**不持有股票**的最大利润

状态转移公式：
$$
new\_not\_hold = \max(not\_hold,\; hold + prices[i])
$$
> 今天不持有：要么昨天就不持有；要么昨天持有，今天卖出。

$$
new\_hold = \max(hold,\; -prices[i])
$$
> 今天持有：要么之前就持有；要么**今天第一次买入（只能交易一次，直接写 `-prices[i]`）**。

⚠️ 关键：不能直接原地覆盖 `hold`、`not_hold`，必须先算出新状态再赋值；
> 区分121与122：本题只能一次交易，买入写 `-prices[i]`；无限次交易写成 `not_hold‑prices[i]`。

初始条件：
- `hold = -prices[0]`：第一天买入股票
- `not_hold = 0`：第一天不操作，不持有股票

**步骤拆解**
1. 边界：数组为空直接返回0；
2. 初始化 `hold`、`not_hold`；
3. 从第2天遍历价格数组；
4. 计算新的 `new_not_hold`、`new_hold`；
5. 滚动更新 `hold`、`not_hold`；
6. 遍历结束返回 `not_hold`。

> 最终答案必须返回不持有股票状态，股票卖出才算完成交易，不交易时 `not_hold=0`。

示例推演：`prices = [7,1,5,3,6,4]`
初始：hold=-7，not_hold=0
i=1，p=1
new_not_hold = max(0, -7+1) = 0
new_hold = max(-7, -1) = -1
hold=-1，not_hold=0

i=2，p=5
new_not_hold = max(0, -1+5)=4
new_hold = max(-1, -5)=-1
hold=-1，not_hold=4

i=3，p=3
new_not_hold = max(4, -1+3)=4
new_hold = max(-1, -3)=-1
hold=-1，not_hold=4

i=4，p=6
new_not_hold = max(4, -1+6)=5
new_hold = max(-1, -6)=-1
hold=-1，not_hold=5

i=5，p=4
new_not_hold = max(5, -1+4)=5
new_hold = max(-1, -4)=-1
hold=-1，not_hold=5
最终答案：5

## ⚙️ 复杂度分析
### 贪心版本
- **时间复杂度**：$\(O(n)\)$，一次线性遍历数组
- **空间复杂度**：$\(O(1)\)$，常数变量开销

### DP滚动变量版本
- **时间复杂度**：$\(O(n)\)$，一次线性遍历数组
- **空间复杂度**：$\(O(1)\)$，只用两个状态变量，无DP数组

## 💻 AC 代码
### ① 贪心解法
```python
from typing import List
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        if not prices:
            return 0
        min_price = prices[0]
        max_profit = 0
        for p in prices[1:]:
            max_profit = max(max_profit, p - min_price)
            min_price = min(min_price, p)
        return max_profit
```

### ② DP滚动变量（股票系列通用模板）
```python
from typing import List
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        if not prices:
            return 0
        hold = -prices[0]
        not_hold = 0
        for p in prices[1:]:
            new_not_hold = max(not_hold, hold + p)
            new_hold = max(hold, -p)
            hold, not_hold = new_hold, new_not_hold
        return not_hold
```

## 🧩 关键知识点 & 技巧
1. 贪心适合本题，但通用性弱；`hold / not_hold`二元状态是股票题通用范式，可以扩展到122、123、188、309、714；
2. 121与122核心区别：
   - 121（最多一次交易）：`new_hold = max(hold, -p)`
   - 122（无限次交易）：`new_hold = max(hold, not_hold - p)`
3. DP不能直接覆盖旧状态，要先计算新状态再赋值；
4. 结果返回`not_hold`；持有股票`hold`只是账面收益，没有卖出不算实际利润；
5. `max_profit`初始化为0，对应不做任何交易的情况。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 贪心先更新`min_price`，再计算利润，等价当天买入当天卖出，逻辑顺序出错；
2. DP买入写成 `not_hold‑p`，套用无限交易模板，本题只能一次交易造成结果错误；
3. 返回`hold`，手上还持有股票，不是真实交易利润；
4. `min_price`初始化为0，而不是`prices[0]`。

**避坑要点**
- 买入时间必须严格在卖出时间之前；
- 股票DP两个状态代表**当天结束后的状态**，不等于今天一定发生买入/卖出动作；
- 允许不交易，答案最小为0，不会出现负数。

## ✅ 总结
贪心可以简洁解决本题，但只能针对一次交易场景。
`hold / not_hold`滚动DP是股票系列的基础模板，虽然本题写起来略长，但可以无缝迁移到更复杂的股票变种题目。
