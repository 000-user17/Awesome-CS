# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：338
- **题目名称**：比特位计数
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/counting-bits/
- **刷题日期**：2026\08\19
- **标签**：动态规划 / 位运算
## 📝 题目大意
给定整数 `n`，对 `0 <= i <= n` 中每一个整数 `i`，计算其二进制表示中 **1 的个数**，返回长度为 `n+1` 的数组作为答案。
> 进阶要求：
> 1. 线性时间 $\(O(n)\)$ 解法；
> 2. 不使用内置库函数统计二进制1的数量。

## 💡 解题思路
### 思路1：暴力统计（$O(n\log n)$，基础写法）
遍历每个数字，通过循环逐位判断或者 Brian Kernighan 算法统计1的个数。
- 普通逐位：不断右移，判断最低位是否为1；
- Brian Kernighan：`x &= x - 1` 每次消除最右侧一个1，统计消除次数，常数更小。

### 思路2：动态规划（最优 $\(O(n)\)$，面试首选）
观察二进制规律推导递推公式：
1. 偶数：$i$ 二进制末尾为0，$i = i>>1 \times 2$
   $$ans[i] = ans[i>>1]$$
2. 奇数：$i$ 二进制末尾为1
   $$ans[i] = ans[i>>1] + 1$$
合并统一公式：
$$ans[i] = ans[i >> 1] + (i \& 1)$$

### 思路3：DP-最高有效位
找到小于等于 $i$ 的最大2的幂 `highBit`
$$ans[i] = ans[i - highBit] + 1$$
原理：数字 = 最高位1 + 剩余部分，剩余部分1的数量预先算出。

## ⚙️ 复杂度分析
### 暴力 / Brian Kernighan
- **时间复杂度**：$O(n\log n)$，每个数字最多遍历 $\log i$ 位
- **空间复杂度**：$\(O(n)\)$，存放结果数组

### DP递推（$ans[i] = ans[i >> 1] + (i \& 1)$）
- **时间复杂度**：$\(O(n)\)$，仅一次线性遍历
- **空间复杂度**：$\(O(n)\)$，结果数组，无额外空间开销

## 💻 多种AC代码
### ① 最优DP写法（推荐背诵）
```python
from typing import List
class Solution:
    def countBits(self, n: int) -> List[int]:
        ans = [0] * (n + 1)
        for i in range(1, n + 1):
            ans[i] = ans[i >> 1] + (i & 1)
        return ans
```

### ② Brian Kernighan 暴力版
```python
from typing import List
class Solution:
    def countBits(self, n: int) -> List[int]:
        def count_one(x):
            cnt = 0
            while x:
                x &= x - 1
                cnt += 1
            return cnt
        return [count_one(i) for i in range(n + 1)]
```

### ③ 最高有效位DP写法
```python
from typing import List
class Solution:
    def countBits(self, n: int) -> List[int]:
        ans = [0] * (n + 1)
        highBit = 0
        for i in range(1, n + 1):
            if i & (i - 1) == 0:
                highBit = i
            ans[i] = ans[i - highBit] + 1
        return ans
```

## 🧩 关键知识点 & 技巧
1. `i >> 1` 等价于整数除以2向下取整；`i & 1` 判断奇偶；
2. `i & (i - 1) == 0` 可以快速判断一个数是不是2的整数次幂；
3. 函数调用使用 `func()`，不要写成 `func[]`，会触发报错：`'function' object is not subscriptable`；
4. DP利用已计算的结果，避免重复统计，实现线性复杂度。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 混淆函数调用符号：`count_one[i]` 写成方括号，报类型错误；
2. 数组初始化长度错误，写成 `[0] * n`，忽略数字范围包含0~n，需要 `n+1` 个位置；
3. DP循环从0开始，没有利用初始值 `ans[0]=0`，产生多余判断；
4. 想用暴力解法应付进阶要求，无法通过时间严格的测试用例。

**避坑要点**
- 题目进阶优先写移位DP公式，是面试官最认可的标准解法；
- 记住：0的二进制1的个数为0，作为DP初始条件；

## ✅ 总结
比特位计数是位运算+动态规划入门经典题。
暴力写法容易想到，但想要达到线性时间复杂度必须挖掘二进制递推规律。
`ans[i] = ans[i >> 1] + (i & 1)` 代码最短、最好记忆，优先掌握。
