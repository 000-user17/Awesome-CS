# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：101
- **题目名称**：对称二叉树
- **题目难度**：简单
- **题目链接**：[https://leetcode.cn/problems/symmetric-tree/](https://leetcode.cn/problems/symmetric-tree/)
- **刷题日期**：2026\09\08
- **标签**：二叉树 / DFS递归 / BFS迭代

## 📝 题目大意
给你一个二叉树的根节点 `root`，检查它是否轴对称。
一棵二叉树轴对称：树的左右两半互为镜像。
也就是：**左子树和右子树镜像相等**。
镜像规则：
1. 两个根节点值相等
2. 第一个树的左子树 = 第二个树的右子树
3. 第一个树的右子树 = 第二个树的左子树

## 💡 解题思路
### 方法一：递归（DFS）
**核心思想：写辅助函数，判断两棵子树是否互为镜像。比较 root.left 和 root.right。**

**步骤拆解：**
1. 主函数：如果root为空，返回True；调用辅助函数比较`root.left`与`root.right`。
2. 辅助函数`isMirror(a,b)`：判断a、b两棵树是不是镜像。
    1. 边界条件：
        - a为None，b为None：都为空，镜像，返回True。
        - a、b其中一个为空，另一个不为空：不是镜像，返回False。
    2. 当前层判断：a.val 必须等于 b.val。
    3. 递归：
        - a的左子树 和 b的右子树镜像
        - a的右子树 和 b的左子树镜像
    4. 两个递归条件同时成立，才返回True。

### 方法二：迭代（BFS队列）
**核心思想：队列成对放入需要对比的节点。每次取出一对，做镜像校验。**
1. 队列初始把 `root.left`、`root.right`成对入队。
2. 循环取出一对节点 n1,n2：
    - 两者都为空，继续下一对。
    - 一个空一个非空，直接return False。
    - 值不相等，return False。
    - **成对入队**：n1.left 和 n2.right；n1.right 和 n2.left。
3. 队列全部处理完成，返回True。

## ⚙️ 复杂度分析
- **递归DFS**
    - **时间复杂度**：$\(O(n)\)$，每个节点访问一次。
    - **空间复杂度**：$\(O(h)\)$，h为树高度；最坏链表$\(O(n)\)$。
- **BFS迭代**
    - **时间复杂度**：$\(O(n)\)$
    - **空间复杂度**：$\(O(n)\)$，队列存储节点。

## 💻 AC 代码（Python）
### 版本1：递归版本（面试优先）
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        def is_mirror(a: Optional[TreeNode], b: Optional[TreeNode]) -> bool:
            # 都为空
            if not a and not b:
                return True
            # 其中一个为空
            if not a or not b:
                return False
            # 值相等，并且a左镜像b右；a右镜像b左
            return a.val == b.val and is_mirror(a.left, b.right) and is_mirror(a.right, b.left)
        
        if not root:
            return True
        return is_mirror(root.left, root.right)
```

### 版本2：BFS队列迭代
```python
from collections import deque
class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        if not root:
            return True
        q = deque()
        q.append(root.left)
        q.append(root.right)
        while q:
            n1 = q.popleft()
            n2 = q.popleft()
            if not n1 and not n2:
                continue
            if not n1 or not n2:
                return False
            if n1.val != n2.val:
                return False
            # 成对放入镜像位置
            q.append(n1.left)
            q.append(n2.right)

            q.append(n1.right)
            q.append(n2.left)
        return True
```

## 🧩 关键知识点 \& 技巧
- 技巧1：不要想着分别遍历左、右子树再对比遍历数组；遇到重复值样例会出错。直接写镜像比较函数。
- 技巧2：镜像对比：`a.left ↔ b.right`；`a.right ↔ b.left`，左右交叉匹配。
- 技巧3：递归三个条件：都空True；一单空False；值相等+交叉递归同时成立。
- 技巧4：迭代队列必须**成对取出、成对入队**。

## ❌ 错题反思 / 踩坑记录
**错误1：比较 a.left vs b.left，a.right vs b.right**
错误原因：不是相等，是镜像，要交叉对比。
修正思路：`is_mirror(a.left,b.right) && is_mirror(a.right,b.left)`。

**错误2：边界判断顺序写错，先访问.val再判None**
错误原因：节点为None访问.val抛异常。
修正思路：先判空，再判断val。

**错误3：BFS入队顺序写反**
错误原因：队列成对，顺序必须一一对应，取出n1,n2。

**错误4：把树拆成前序遍历数组对比，遇到[1,2,2,null,3,null,3]会错误。**
错误原因：单纯序列相同不能代表镜像。

## ✅ 总结
1. 核心：判断**两棵子树是否镜像**，不是判断两棵子树是否相等。相等是左对左、右对右；镜像交叉：左对右、右对左。
2. 递归辅助函数传入两个节点，交叉递归；迭代队列成对存放镜像节点。
3. 注意边界：两个同时为空返回True；一个空一个不为空直接False。
4. 本题和「100相同的树」对比记忆：
    - 100相同树：`isSame(a.left,b.left) and isSame(a.right,b.right)`
    - 101对称树：`isMirror(a.left,b.right) and isMirror(a.right,b.left)`
