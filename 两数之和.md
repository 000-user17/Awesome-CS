# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：1
- **题目名称**：两数之和 Two Sum
- **题目难度**：简单
- **题目链接**：[https://leetcode.cn/problems/two-sum/](https://leetcode.cn/problems/two-sum/)
- **刷题日期**：2026\09\09
- **标签**：数组 / 哈希表

## 📝 题目大意
给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 和为目标值 `target` 的那两个整数，并返回它们的数组下标。
约束：
1. 每种输入只会对应一个答案
2. 不能使用两次相同的元素（同一个下标不能用两次）
3. 返回下标顺序任意

> 分析：
> 暴力解法：两层循环，枚举所有两个数的组合求和，时间复杂度 $O(n^2)$，数组大的时候很慢。
> ✅ 核心思路：**哈希表（字典）空间换时间**
> 遍历数组，对于当前数字 `nums[i]`，我们需要找的是 `complement = target - nums[i]`
> 也就是：有没有一个之前遍历过的数，刚好等于 `complement`。
> - 如果补数存在于哈希表：直接返回 `[哈希表[complement], i]`
> - 如果不存在：把当前数字 `nums[i]` 和下标 i 存入哈希表，继续往后遍历
> 哈希表查找是 $\(O(1)\)$，整体一次遍历完成。

## 💡 解题思路
### 核心架构
1. 创建字典 `hashmap`，key = 数组的值，value = 对应下标；用来保存已经遍历过的数字
2. 遍历数组，`i` 是下标，`num` 是当前数字：
    - 计算需要寻找的补数 `complement = target - num`
    - 判断补数是否在 hashmap 里面：
      - ✅ 存在：找到答案，返回 `[hashmap[complement], i]`
      - ❌ 不存在：把 `num:i` 放入hashmap，继续循环
3. 题目保证一定存在一组解，不需要处理找不到的情况

> 示例：nums = [2,7,11,15], target = 9
> i=0, num=2，complement=7。7不在字典，存入 {2:0}
> i=1, num=7，complement=2。2在字典，hashmap[2]=0 → 返回 [0,1]

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，数组只遍历一遍，哈希查找\(O(1)\)
- **空间复杂度**：$\(O(n)\)$，哈希表最多存储全部n个元素

## 💻 AC 代码
```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        hashmap = {}
        for i, num in enumerate(nums):
            complement = target - num
            if complement in hashmap:
                # 找到之前存的补数，返回两个下标
                return [hashmap[complement], i]
            # 不存在，把当前数字和下标存入字典
            hashmap[num] = i
        # 题目保证一定有解，不会走到这里
        return []
```

## 🧩 关键知识点 & 技巧
1. 空间换时间：用哈希牺牲内存，把 $O(n^2)$ 暴力降到 $\(O(n)\)$；
2. 先查补数，**后存入哈希表**！防止把当前元素重复使用；
3. 字典key存数组的值，value存下标，方便反向查找下标；
4. 题目说明只有唯一解，找到就可以直接return，不用继续遍历。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 先put进字典，再判断补数：会读取当前元素本身，比如nums=[3,3], target=6，错误返回同一个下标；
2. 把key和value写反，字典存下标为key，数值为value，查找失败；
3. 暴力双重循环，大数据样例超时；
4. 返回数字而不是下标，题目要求返回索引。

**避坑要点**
> 顺序很重要：**先判断补数是否存在，再把当前元素加入哈希表**，避免重复选取同一个元素。

## ✅ 总结
1题是算法入门经典哈希表题，核心思想：遍历的时候，记录已经见过的数字，直接查询所需要的另一半补数。
面试可以先说暴力解法，指出时间缺陷，再引出哈希表优化方案。

关联题目：15三数之和、18四数之和（需要排序+双指针，不能直接哈希）

要不要继续按这个模板整理 15 三数之和？
