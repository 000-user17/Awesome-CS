# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：283
- **题目名称**：移动零 Move Zeroes
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/move-zeroes/
- **刷题日期**：2026\08\21
- **标签**：数组 / 双指针

## 📝 题目大意
给定整数数组 `nums`，将数组中所有的 0 移动到数组末尾，同时保持非零元素的**相对顺序不变**。
> 硬性约束：必须**原地修改数组**，不允许拷贝新数组；函数无返回值。
> 示例：输入 `nums = [0,1,0,3,12]`，原地修改后数组为 `[1,3,12,0,0]`。

## 💡 解题思路
### 思路1：快慢双指针（覆盖+后置补0 ⭐推荐）
**核心思想**
- `slow`：下一个非零元素应当存放的位置
- `fast`：遍历指针，扫描整个数组

**步骤拆解**
1. fast遍历数组，遇到非零元素，把值赋值到`nums[slow]`，slow向后移动；
2. 遍历结束后，`[0, slow‑1]`区间全部是非零元素；
3. 将`slow`到数组末尾全部赋值为0，完成0的后置。

### 思路2：快慢双指针交换写法
fast扫描数组，遇到非零元素，直接交换`nums[slow]`与`nums[fast]`，slow右移。
> 不需要二次循环补零，原地交换完成；同样保持非零元素相对顺序。

> ❌不可以排序：排序会打乱非零元素原有相对顺序。

## ⚙️ 复杂度分析
### 覆盖补零版本
- **时间复杂度**：$\(O(n)\)$，最多两次遍历数组
- **空间复杂度**：$\(O(1)\)$，纯原地操作，不使用额外数组

### 交换版本
- **时间复杂度**：$\(O(n)\)$，一次遍历数组
- **空间复杂度**：$\(O(1)\)$

## 💻 AC 代码（Python）
### ① 覆盖 + 后置补0
```python
from typing import List
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        slow = 0
        for fast in range(len(nums)):
            if nums[fast] != 0:
                nums[slow] = nums[fast]
                slow += 1
        # slow之后全部置0
        for i in range(slow, len(nums)):
            nums[i] = 0
```

### ② 交换写法
```python
from typing import List
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        slow = 0
        for fast in range(len(nums)):
            if nums[fast] != 0:
                nums[slow], nums[fast] = nums[fast], nums[slow]
                slow += 1
```

## 🧩 关键知识点 & 技巧
- 技巧1：双指针处理数组原地移动类题目；slow记录目标存放位置，fast负责遍历筛选；
- 技巧2：题目重点要求**维持非零元素相对顺序**，不能排序；
- 技巧3：函数不需要返回数组，直接原地修改输入数组。
- 易错点：不能新建list保存非零元素再赋值，属于额外拷贝，违背原地要求。

## ❌ 错题反思 / 踩坑记录
**错误1**：创建新列表收集非零数字，最后赋值，产生数组拷贝，不满足in‑place原地条件。
**错误2**：使用排序实现，非零元素顺序被打乱，答案错误。
**错误3**：函数return返回数组，本题要求无返回值，判题出错。

**修正思路：**
1. 原地数组题优先思考双指针，不要开新容器；
2. 审题关注“相对顺序”约束，不要随意排序。

## ✅ 总结
1. 本题是双指针基础模板题，两种写法都满足原地$\(O(1)\)$空间；
2. 覆盖补零写法逻辑更直观；交换写法少一轮循环；
3. 核心：slow标记非零元素的存放位置，fast做筛选遍历；
4. 同类题型：移除元素，都是快慢指针原地处理数组。
