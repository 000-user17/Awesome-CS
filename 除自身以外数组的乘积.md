# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：238
- **题目名称**：除自身以外数组的乘积
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/product-of-array-except-self/
- **刷题日期**：2026\08\12
- **标签**：数组 / 前缀积 / 后缀积

## 📝 题目大意
给定整数数组 `nums`，返回数组 `answer`，其中 `answer[i]` 等于 `nums` 中除 `nums[i]` 之外所有元素的乘积。
要求：
1. 时间复杂度 $\(O(n)\)$
2. **禁止使用除法**
3. 任意前缀、后缀乘积保证在32位整数范围内

> 重点限制：不能除法，因此不能求出全部元素总乘积再除以当前值（数组存在0时直接失效）。

## 💡 解题思路
### 核心思想：前缀积 + 后缀积
`answer[i] = i左侧所有元素乘积 × i右侧所有元素乘积`
优化方案：**复用输出数组存储前缀积**，额外只用一个变量维护后缀积，实现 $\(O(1)\)$ 额外空间（不计结果数组）。

**步骤拆解**
1. 初始化 `products` 数组全部为1，用来存放前缀积；
2. 从左向右遍历：`products[i]` 存储下标 `i` 左边所有元素乘积；
3. 定义变量 `right` 维护当前位置右侧元素乘积，从右向左遍历；
4. `products[i] = products[i] * right`，之后更新 `right *= nums[i]`；
5. 遍历完成，`products` 即为答案。

示例：nums = [1,2,3,4]
第一轮左遍历前缀积：`products = [1, 1, 2, 6]`
第二轮右遍历：
- i=2：products[2] = 2 * 4 = 8，right=12
- i=1：products[1] = 1 * 12 = 12，right=24
- i=0：products[0] = 1 * 24 = 24，right=24
最终结果：`[24,12,8,6]`

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，两次线性遍历数组
- **空间复杂度**：$\(O(1)\)$，仅使用常数临时变量（题目规定输出数组不计入额外空间）

## 💻 AC 代码
```python
from typing import List

class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        n = len(nums)
        products = [1] * n

        # 从左往右计算前缀积
        for i in range(1, n):
            products[i] = nums[i - 1] * products[i - 1]

        # 从右往左，用right保存后缀积，直接相乘更新结果
        right = nums[n - 1]
        for i in range(n - 2, -1, -1):
            products[i] = products[i] * right
            right *= nums[i]
            
        return products
```

## 🧩 关键知识点 & 技巧
1. 不额外开辟前缀、后缀数组，复用结果数组节省空间，是面试最优写法；
2. 禁止除法！数组包含0时，总乘积除法方案会出错；
3. 前缀遍历起点 `i=1`，`products[0]` 天然等于1（左侧无元素，乘积为1）；
4. 后缀遍历从 `n-2` 开始，最右侧元素右侧无元素，后缀初始值 `right = nums[-1]`；
5. 边界初始值：空乘积的乘法单位元是 **1**，不要写成0。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 前缀循环从 `i=0` 开始，错误覆盖初始值；
2. 后缀遍历循环下标写错，边界越界；
3. 颠倒顺序：先更新right再计算products[i]，导致使用错误的右侧乘积；
4. 想用除法解题，遇到数组含0直接报错。

**避坑要点**
- 先利用前缀积填入数组，再从右往左乘后缀积；
- 顺序不能调换：`products[i] = products[i] * right` → 再更新 `right`；
- 遇到负数、多个0的测试用例，前缀后缀积方案均可正常处理。

## ✅ 总结
常规思路需要两个数组分别存前缀、后缀积，空间 $\(O(n)\)$；
本题最优解法复用输出数组，将额外空间压缩至 $\(O(1)\)$。
核心模型：**前缀积 × 后缀积**
