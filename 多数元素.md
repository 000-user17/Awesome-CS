# LeetCode 刷题记录

## 📌 题目信息
- **题目编号**：169
- **题目名称**：多数元素
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/majority-element/
- **刷题日期**：2026\08\12
- **标签**：摩尔投票法 / 快速选择 / 数组

## 📝 题目大意
给定大小为 `n` 的数组 `nums`，找出其中的多数元素。
多数元素定义：出现次数严格大于 $\lfloor n/2 \rfloor$ 的元素。
题目保证数组中一定存在多数元素。

核心性质：
多数元素出现次数超过一半，数组排序后，下标 `n//2` 的元素一定是答案；
利用该性质可以使用快速选择（QuickSelect）求解；最优解法为摩尔投票法。

## 💡 解题思路
### 思路一：快速选择（QuickSelect，双路快排分区）
**核心思想**
无需完整排序，不断对数组分区，寻找下标 `target = n//2` 的元素。
分区时随机选取基准pivot，降低最坏情况概率。
分区完成后，pivot落到位置pos：
1. `pos == target`：直接返回当前元素
2. `pos > target`：目标在左区间
3. `pos < target`：目标在右区间

**步骤拆解**
1. 设置目标下标 `target = n // 2`
2. 实现双路快排partition函数，随机选取基准
3. 循环分区，不断收缩查找区间，直到找到目标位置

> 复杂度说明：平均O(n)，最坏O(n²)；会修改原数组。

### 思路二：摩尔投票法（最优解法）
**核心思想**
众数数量超过半数，众数与其他元素两两抵消，最后剩余的候选一定是众数。
**步骤拆解**
1. 定义候选 candidate、计数器 count = 0
2. 遍历数组：
   - count == 0，更新 candidate 为当前数字
   - 当前数字 == candidate：count += 1
   - 当前数字 != candidate：count -= 1
3. 遍历结束 candidate 即为答案

> ⚠️ 前提：题目保证多数元素一定存在；若无保证，得到候选后需要再遍历验证次数。

## ⚙️ 复杂度分析
### 快速选择
- **时间复杂度**：平均 O(n)，最坏 O(n²)
- **空间复杂度**：O(1)，仅常数变量（迭代实现，无递归栈）

### 摩尔投票法
- **时间复杂度**：O(n)，仅一次遍历数组
- **空间复杂度**：O(1)，最优时空方案

## 💻 AC 代码 快速选择（你修正后的双路partition版本）
```python
from typing import List
from random import randint

class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        n = len(nums)
        target = n // 2

        def partition(nums, l, r):
            random_idx = randint(l, r)
            pivot = nums[random_idx]
            nums[l], nums[random_idx] = nums[random_idx], nums[l]

            i = l + 1
            j = r
            while True:
                while i <= j and nums[i] < pivot:
                    i += 1
                while i <= j and nums[j] > pivot:
                    j -= 1
                if i >= j:
                    break
                nums[i], nums[j] = nums[j], nums[i]
                i += 1
                j -= 1
            
            nums[l], nums[j] = nums[j], nums[l]
            return j
        
        left = 0
        right = n - 1
        while True:
            pos = partition(nums, left, right)
            if pos == target:
                return nums[pos]
            elif pos > target:
                right = pos - 1
            else:
                left = pos + 1
```

## 💻 AC 代码 摩尔投票法（推荐最优解法）
```python
from typing import List

class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        candidate = 0
        count = 0
        for num in nums:
            if count == 0:
                candidate = num
            if num == candidate:
                count += 1
            else:
                count -= 1
        return candidate
```

## 🧩 关键知识点 & 技巧
- 技巧1：摩尔投票是唯一稳定 O(n)+O(1) 的标准解法；快速选择仅作拓展思路，存在最坏时间风险
- 技巧2：数学符号 $\lfloor n/2 \rfloor$ 对应Python `n // 2`（向下取整），不要混用 `/` 浮点数除法
- 技巧3：快速选择核心难点：partition分区必须以pivot为基准，不能拿左右指针元素互相比较
- 易错点：
  1. 快速选择partition循环条件、交换逻辑极易写错，break条件、指针边界容易颠倒
  2. 摩尔投票不能直接套用在「不存在多数元素」的场景，缺少验证步骤会出错
  3. 混淆向下取整 `//` 和浮点数除法 `/`

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 初次手写partition时，未使用pivot作为比较基准，错误拿nums[i]与nums[j]对比，分区完全失效；
2. while循环内部break条件写反，元素交换代码永远无法执行；
3. 分区结束后忘记把pivot交换到分界位置，返回下标错误。

**修正思路：**
1. 双路快排范式固定：左指针找大于等于pivot，右指针找小于等于pivot；
2. 理清指针退出条件 `i > j`，最后把基准元素交换至j位置；
3. 区分两种解法适用场景：面试优先写摩尔投票。

## ✅ 总结
本题存在多种解法：哈希表（O(n)空间）、排序（O(nlogn)）、快速选择、摩尔投票。
**摩尔投票法是最优标准答案**，利用元素相互抵消思想，时空复杂度最优。
快速选择属于拓展思路，依靠「多数元素一定落在n//2下标」的特性实现，适合用来练习快排分区逻辑。
拓展：摩尔投票算法可以升级，求解出现次数大于 n/3 的候选元素。
