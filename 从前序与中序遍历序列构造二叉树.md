# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：105
- **题目名称**：从前序与中序遍历序列构造二叉树
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/
- **刷题日期**：2026\09\07
- **标签**：二叉树 / DFS / 分治 / 哈希优化

## 📝 题目大意
给定两个整数数组 `preorder` 和 `inorder`，其中 `preorder` 是二叉树的先序遍历，`inorder` 是同一棵树的中序遍历，请构造二叉树并返回其根节点。
> 假设树中没有重复元素。

## 💡 解题思路
**核心思想：分治递归**
- 前序遍历：`[根 | 左子树全部节点 | 右子树全部节点]`，**第一个元素一定是当前子树的根节点**。
- 中序遍历：`[左子树全部节点 | 根 | 右子树全部节点]`；拿到根，在中序数组里根左边全部是左子树，右边全部是右子树。

**步骤拆解：**
1. 取 preorder 的第一个元素，作为当前子树的根节点。
2. 在 inorder 中找到根节点下标 `root_idx`：
   - inorder 中，`root_idx` 左边区间：全部属于**左子树**
   - inorder 中，`root_idx` 右边区间：全部属于**右子树**
3. 统计左子树节点数量：`left_size = root_idx - in_start`
4. 根据左子树节点数量，切分前序数组：
   - preorder 左子树区间：`[pre_start+1 , pre_start + left_size]`
   - preorder 右子树区间：`[pre_start+left_size+1 , pre_end]`
5. 递归构建左子树、递归构建右子树，挂到当前根节点。
6. 递归边界：区间无效（start > end），返回 None。

> 优化点：每次在inorder找根下标如果用list.index()，时间\(O(n)\)；建立哈希表`val→index`，查找下标O(1)。

### 区间定义
- preorder：`[pre_start, pre_end]` 当前子树在前序的闭区间
- inorder：`[in_start, in_end]` 当前子树在中序的闭区间

## ⚙️ 复杂度分析
- **朴素版本（list.index）**
  - 时间复杂度：$O(n^2)$，每层找根下标 \(O(n)\)，最坏链表树递归n层。
  - 空间复杂度：$\(O(n)\)$，递归栈。
- **哈希表优化版本（面试推荐）**
  - 时间复杂度：$\(O(n)\)$，建立哈希\(O(n)\)，每个节点处理一次。
  - 空间复杂度：$\(O(n)\)$，哈希表 + 递归栈。

## 💻 AC 代码（Python）
### 版本1：哈希优化版（面试写这个）
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> TreeNode:
        # 值到中序下标的哈希映射
        val2idx = {val:i for i,val in enumerate(inorder)}

        def dfs(pre_start, pre_end, in_start, in_end):
            # 区间无效，没有节点
            if pre_start > pre_end:
                return None
            # 前序第一个是根
            root_val = preorder[pre_start]
            root = TreeNode(root_val)
            root_idx = val2idx[root_val]
            # 左子树节点个数
            left_size = root_idx - in_start

            # 递归构建左子树
            root.left = dfs(pre_start+1, pre_start + left_size,
                            in_start, root_idx -1)
            # 递归构建右子树
            root.right = dfs(pre_start + left_size + 1, pre_end,
                             root_idx +1, in_end)
            return root
        
        return dfs(0, len(preorder)-1, 0, len(inorder)-1)
```

### 版本2：不使用哈希，直接index（简洁，但性能差）
```python
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> TreeNode:
        if not preorder:
            return None
        root_val = preorder[0]
        root = TreeNode(root_val)
        idx = inorder.index(root_val)
        root.left = self.buildTree(preorder[1:idx+1], inorder[:idx])
        root.right = self.buildTree(preorder[idx+1:], inorder[idx+1:])
        return root
```
> 缺点：切片会生成新数组，额外空间；`index()` \(O(n)\)，大数据会超时。适合理解逻辑，面试不推荐。

## 🧩 关键知识点 \& 技巧
- 技巧1：前序定根，中序划左右。**前序第一个一定是当前区间根**。
- 技巧2：算出左子树节点数量 `left_size`，用来切前序数组，不要凭感觉写下标。
- 技巧3：哈希表预存inorder的值和下标，把查找根下标从\(O(n)\)降到O(1)。
- 技巧4：递归传**下标区间**而不是切片数组，避免拷贝数组，节省时间空间。
- 易错点：各个区间边界，闭区间，`pre_start > pre_end`才返回None，不要写>=。

## ❌ 错题反思 / 踩坑记录
**错误1：区间下标写错，右子树前序起始写成 pre_start+1**
错误原因：没有用`left_size`计算，凭肉眼分割数组。
修正思路：`pre_start + left_size + 1`才是右子树前序起点。

**错误2：递归边界写成 `pre_start == pre_end`**
错误原因：叶子节点可以进入，但是空区间需要`> `判断。
修正思路：`if pre_start > pre_end: return None`。

**错误3：每次递归调用inorder.index(root_val)**
错误原因：每次遍历数组找下标，大数据样例超时。
修正思路：提前构建全局哈希表，O(1)取下标。

**错误4：切片写法 preorder[1:idx+1] 记混切片右开特性**
> python切片 a[l:r]，包含l，不包含r。

## ✅ 总结
1. 核心规则：**前序找根，中序划分左右子树；用左子树节点数量切分前序数组**。
2. 递归分治，传下标区间，不要切片拷贝数组；哈希表优化查找根节点下标。
3. 闭区间模板：`[start, end]`，终止条件 `start > end` 返回None。
4. 同类题：106 从中序与后序遍历构造二叉树，逻辑几乎一样，只是根取后序的末尾。
