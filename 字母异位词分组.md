# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：49
- **题目名称**：字母异位词分组 Group Anagrams
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/group-anagrams/
- **刷题日期**：2026\08\31
- **标签**：哈希表 / 字符串 / 排序
## 📝 题目大意
给你一个字符串数组，请你将**字母异位词**组合在一起。可以按任意顺序返回结果列表。
> 字母异位词：字母种类、每个字母出现次数完全相同，仅字母顺序不同，例如 `eat`、`tea`、`ate`。
示例：
输入：`strs = ["eat","tea","tan","ate","nat","bat"]`
输出：`[["eat","tea","ate"],["tan","nat"],["bat"]]`

> 分析：
> - 异位词核心：需要生成一个统一的特征key，相同key归为同一组；
> - 方案1排序法：异位词排序之后字符串完全一致，把排序后的字符串作为哈希key；
> - 方案2计数法：统计26个小写字母出现次数，用计数元组做哈希key，不需要排序；
> - 哈希表value保存原始字符串列表，最后取出所有分组。

## 💡 解题思路
### 方法一：排序 + defaultdict（最常用，代码简洁）
1. 建立哈希字典，key为排序后的字符串，value为异位词列表；
2. 遍历每一个原始字符串：
   - `key = ''.join(sorted(s))`，生成标准化特征字符串；
   - 将原始字符串append到哈希表对应key的列表；
3. 将字典全部value转为二维列表返回。

### 方法二：字符计数法（不排序，时间更优）
1. 每个字符串开辟长度26计数数组，统计a‑z每个字母出现次数；
2. list不能做字典key，将计数数组转为**tuple元组**作为哈希key；
3. 相同计数元组代表是字母异位词，归入同一分组。

> 对比：
> - 排序法：代码简短，适合字符串较短场景，时间 $O(nk\log k)$；
> - 计数法：避免排序，时间 $\(O(nk)\)$，适合字符串很长的场景。

## ⚙️ 复杂度分析
> n：字符串数组长度；k：单字符串最大长度
- **排序解法**
  - 时间复杂度：$O(nk\log k)$，每个字符串内部排序耗时 $k\log k$
  - 空间复杂度：$\(O(nk)\)$，存储全部字符串分组
- **计数解法**
  - 时间复杂度：$\(O(nk)\)$，遍历每个字符串的每个字符做计数
  - 空间复杂度：$\(O(nk)\)$

## 💻 AC 代码
### 解法1：排序+defaultdict（面试首选）
```python
from collections import defaultdict
from typing import List

class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        hash_map = defaultdict(list)
        for s in strs:
            # 将字符串排序得到特征key
            key = ''.join(sorted(s))
            hash_map[key].append(s)
        # 取出所有分组转为列表返回
        return list(hash_map.values())
```

### 解法2：字符计数法（进阶优化）
```python
from collections import defaultdict
from typing import List

class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        hash_map = defaultdict(list)
        for s in strs:
            count = [0] * 26
            for ch in s:
                count[ord(ch) - ord('a')] += 1
            # list不能做key，转tuple
            key = tuple(count)
            hash_map[key].append(s)
        return list(hash_map.values())
```

### 不使用defaultdict，原生dict写法
```python
from typing import List

class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        hash_map = {}
        for s in strs:
            key = ''.join(sorted(s))
            if key not in hash_map:
                hash_map[key] = []
            hash_map[key].append(s)
        return list(hash_map.values())
```

## 🧩 关键知识点 & 技巧
1. **字母异位词两大判定手段**：字符串排序、26字母频次计数；
2. `defaultdict(list)`：key不存在自动创建空列表，省去`if key not in dict`判断；
3. python中**list不能作为字典key**，必须转字符串 / tuple元组，因为list可变不可哈希；
4. `sorted(s)`返回字符列表，`''.join()`把字符列表拼接回字符串；
5. 输出允许组与组之间、组内元素之间任意顺序，不用额外排序。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 直接把`sorted(s)`得到的list当做字典key，报`TypeError: unhashable type: 'list'`，忘记用`join`转字符串；
2. 使用普通dict忘记判断key是否存在，直接append触发`KeyError`；
3. 计数法使用`list(count)`做key，没有转为tuple，同样触发不可哈希报错；
4. 边界忽略空字符串输入，例如输入`[""]`，排序法、计数法均可正常处理；
5. 最后返回忘记调用`list()`，直接返回`hash_map.values()`，部分OJ类型不匹配。

**避坑要点**
- 字典key必须是不可变类型：str、int、tuple；list、dict不可以；
- defaultdict传入参数是类型对象，写`defaultdict(list)`，不能写`defaultdict([])`；
- 计数法：`ord(ch)-ord('a')`把小写字母映射到0‑25下标。

## ✅ 总结
本题是哈希表经典分组题，核心思想：**把一类事物提取统一特征，用特征做key做分组聚合**。
- 简单场景直接用排序解法，代码量最少；
- 面试官追问优化，再给出26字母计数的方案；
该思想大量应用于字符串、子串类题目。
