# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：70
- **题目名称**：爬楼梯 Climbing Stairs
- **题目难度**：简单
- **题目链接**：[https://leetcode.cn/problems/climbing-stairs/](https://leetcode.cn/problems/climbing-stairs/)
- **刷题日期**：2026\09\14
- **标签**：动态规划 / 递归

## 📝 题目大意
需要爬 n 阶楼梯，每次只能爬 1 阶或者 2 阶，求一共有多少种不同方式爬到楼顶。

示例：
n = 3
输出：3
解释：
1. 1 + 1 + 1
2. 1 + 2
3. 2 + 1

> 核心思考：要到达第 n 阶，最后一步只有两种可能：
> 1. 从第 n-1 阶，爬1阶上来
> 2. 从第 n-2 阶，爬2阶上来
> 所以总方法数 = 到达 n-1 的方法数 + 到达 n-2 的方法数。斐波那契数列。

## 💡 解题思路
### 状态定义
`dp[i]`：爬到第 i 阶楼梯的方案数量。

### Base Case
- `dp[1] = 1`：1阶，只有1种（爬1步）
- `dp[2] = 2`：2阶，两种（1+1 或者 2）

### 状态转移方程
$$dp[i] = dp[i-1] + dp[i-2]$$

> 不推荐纯递归（自顶向下）：会大量重复计算，时间爆炸，n稍大就超时。
> 推荐：迭代DP，只保留前两个变量，空间压缩到\(O(1)\)。

## ⚙️ 复杂度分析
迭代优化版本：
- **时间复杂度**：$\(O(n)\)$，循环n次
- **空间复杂度**：$\(O(1)\)$，只用两个变量保存状态

完整DP数组版本：
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(n)\)$

## 💻 AC 代码（空间优化版，面试首选）
```python
class Solution:
    def climbStairs(self, n: int) -> int:
        if n <= 2:
            return n
        prev_prev = 1 # dp[1]
        prev = 2      # dp[2]
        for i in range(3, n+1):
            cur = prev_prev + prev
            prev_prev = prev
            prev = cur
        return prev
```

DP数组版本（便于理解）
```python
class Solution:
    def climbStairs(self, n: int) -> int:
        if n <= 2:
            return n
        dp = [0]*(n+1)
        dp[1] = 1
        dp[2] = 2
        for i in range(3, n+1):
            dp[i] = dp[i-1] + dp[i-2]
        return dp[n]
```

## 🧩 关键知识点 & 技巧
1. 自顶向下递归会有大量重复子问题，时间复杂度 $O(2^n)$，n=40就很慢，面试不要写；
2. 动态规划本质：**子问题最优解可以推导大问题最优解，重叠子问题**；
3. 状态压缩：当前值只依赖前两项，不需要完整dp数组，节省内存。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. base case写错：`dp[0]=1` 容易混淆，要看题目定义；本题n>=1；
2. 递归直接暴力写，大数据超时；
3. 循环范围写错，终止条件搞错。

**避坑要点**
> 递推公式 `dp[i] = dp[i-1] + dp[i-2]`，和斐波那契数列只是起始值不一样。

## ✅ 总结
70是DP入门第一题。核心：拆解最后一步，得到子问题关系。学会状态压缩，是DP基础训练。
关联题目：746 使用最小花费爬楼梯。

要不要继续按这个模板整理 746 使用最小花费爬楼梯？
