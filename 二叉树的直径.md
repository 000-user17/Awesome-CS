# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：543
- **题目名称**：二叉树的直径
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/diameter-of-binary-tree/
- **刷题日期**：2026‑09‑01
- **标签**：二叉树 / DFS / 后序遍历

## 📝 题目大意
给你一棵二叉树的根节点，返回该树的**直径**。
二叉树的直径：树中任意两个节点之间最长路径的**长度（边的数量）**。这条路径可以经过根节点，也可以不经过根节点。
> 重点：路径长度统计的是**边数，不是节点个数**。
> 任意一条路径，一定可以看作：某个节点作为拐点，该节点的左子树向下最深路径 + 右子树向下最深路径。

示例：
```
    1
   / \
  2   3
 / \
4   5
```
最长路径：4‑2‑5，边数=2；或者4‑2‑1‑3，边数=3。直径为 **3**。

## 💡 解题思路
### 核心思想：后序深度优先搜索 DFS
定义递归函数：输入节点，返回**该节点向下的最大深度（边数）**。
- 空节点：深度返回 0
- 当前节点深度 = `max(左子树深度, 右子树深度) + 1`

**关键洞察**：
以当前节点为拐弯点的路径边数 = `左子树深度 + 右子树深度`。
我们遍历每一个节点，把每个节点当做拐点计算路径长度，维护全局最大值，这个全局最大值就是整棵树的直径。

> 为什么用后序？要先拿到左、右子树的深度，才能计算当前节点深度和经过本节点的路径长度。

**执行步骤**
1. 全局变量保存最大直径；
2. DFS递归函数：
   1. 节点为空返回0；
   2. 递归求左子树深度、右子树深度；
   3. 计算经过当前节点的路径长度：`left_depth + right_depth`，更新全局直径；
   4. 返回当前节点向下的最大深度 `max(left_depth, right_depth)+1`；
3. 调用DFS，返回全局最大直径。

推演示例：`[1,2,3,4,5]`
- dfs(4)：左右0，路径0+0=0；返回1
- dfs(5)：左右0，路径0+0=0；返回1
- dfs(2)：left=1，right=1；路径=2，全局max=2；返回max(1,1)+1=2
- dfs(3)：左右0；路径0；返回1
- dfs(1)：left=2，right=1；路径=3，全局max=3；返回max(2,1)+1=3
最终直径 = 3。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，$n$ 为节点数量，每个节点访问恰好一次。
- **空间复杂度**：$\(O(h)\)$，$h$ 是树的高度；递归调用栈。最坏链表树 $\(O(n)\)$；平衡二叉树 $\(O(logn)\)$。

## 💻 AC 标准代码
```python
# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

from typing import Optional
class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        max_diameter = 0

        def dfs(node):
            nonlocal max_diameter
            if not node:
                return 0
            left_depth = dfs(node.left)
            right_depth = dfs(node.right)
            # 以当前node为拐点，路径边数
            max_diameter = max(max_diameter, left_depth + right_depth)
            # 返回本节点向下的最大深度(边数)
            return max(left_depth, right_depth) + 1

        dfs(root)
        return max_diameter
```

## 🧩 关键知识点 & 技巧
1. 直径**不一定经过根节点**，必须遍历全部节点，把每一个节点都当作路径拐点做计算。
2. DFS返回的是**向下深度（边数）**；经过该节点的路径 = 左深度 + 右深度，不需要做加减修正。
3. 使用 `nonlocal` 修改外层整数变量；int是不可变类型，内层直接赋值会生成局部变量。
4. 递归是后序遍历顺序：先算左右子树，再处理当前节点。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 混淆「节点计数」和「边计数」，多加/少减1，答案差1。
2. 忘记 `nonlocal`，内层修改不会作用外层，输出永远0。
3. 误以为直径一定经过根节点，只计算根节点的左右深度之和，子树内部的最长路径被漏掉。
4. 递归函数返回值搞错，把路径长度当做深度返回，递归逻辑完全崩坏。

> 你之前写的bug版本回顾：
> 你统计的是节点个数的高度，写了 `left+right‑2`；同时缺少 `nonlocal`，结果恒为0。

**避坑要点**
- 递归返回：**向下的深度（边）**；路径 = 左深度 + 右深度。
- 全局最优要在递归内部每一个节点都更新，而不是只看根节点。

## ✅ 总结
二叉树直径是经典后序DFS题目。
核心模型：**任意一条最长路径一定存在某个拐点节点，路径由该节点的左最深、右最深拼接而成**。
一次DFS同时完成两件事：①返回子树深度；②更新全局直径。
