# LeetCode 刷题记录 148 排序链表（双版本整合）
## 📌 题目信息
- **题目编号**：148
- **题目名称**：排序链表 Sort List
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/sort-list/
- **刷题日期**：2026\08\13
- **标签**：链表 / 归并排序 / 递归 / 自顶向下 / 自底向上迭代

## 📝 题目大意
给你链表的头结点 `head`，请将其按**升序**排列并返回排序后的链表。
**进阶要求**：实现 $O(n\log n)$ 时间复杂度，并且**常数级空间复杂度 $\(O(1)\)$**。

> 分析：
> 数组常用快速排序，但链表无法随机访问，快排实现麻烦、最坏 $O(n^2)$；
> 堆排序链表实现极其繁琐；
> 归并排序天然适配链表。
> 方案1：递归自顶向下归并：代码简洁，空间 $O(\log n)$（递归调用栈），**不满足进阶**；
> 方案2：自底向上迭代归并：无递归，仅使用若干临时指针，满足 $\(O(1)\)$ 额外空间。

# 方案一：自顶向下 递归归并排序（笔试首选）
## 💡 解题思路
1. **递归终止条件**：链表为空 / 只有单个节点，天然有序，直接返回；
2. **快慢指针寻找中点**，将链表切分为左右两段，切断链表；
3. 递归排序左半段、递归排序右半段；
4. 合并两个有序链表，返回新链表头。

快慢指针要点：`slow, fast = head, head.next`
保证 slow 停在中点前一个节点，方便断开链表，避免偶数链表无限递归。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n\log n)$
- **空间复杂度**：$O(\log n)$，递归栈深度，无法满足进阶 $\(O(1)\)$ 要求

## 💻 AC 代码
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
from typing import Optional
class Solution:
    def sortList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if not head or not head.next:
            return head

        def findMid(s):
            slow, fast = s, s.next
            while fast and fast.next:
                slow = slow.next
                fast = fast.next.next
            mid = slow.next
            slow.next = None
            return mid

        def merge(p, q):
            dummy = ListNode(0)
            temp = dummy
            while p and q:
                if p.val < q.val:
                    temp.next = p
                    p = p.next
                else:
                    temp.next = q
                    q = q.next
                temp = temp.next
            temp.next = p if p else q
            return dummy.next

        def mergeSort(s):
            if not s or not s.next:
                return s
            mid = findMid(s)
            left = mergeSort(s)
            right = mergeSort(mid)
            return merge(left, right)
        
        return mergeSort(head)

    def print_list(self, head):
        res = []
        cur = head
        while cur:
            res.append(str(cur.val))
            cur = cur.next
        print(" ".join(res))
```

## 🧩 关键知识点 & 技巧
1. 快慢指针初始化 `fast = head.next`，是防止偶数链表死递归的关键；
2. `slow.next = None` 物理切断链表，左右两段完全独立；
3. merge 函数为基础模板，对应 LeetCode 21 合并两个有序链表；
4. 代码简短，适合快速写出，面试先用这个阐述思路。

## ❌ 错题反思 / 踩坑记录
1. 快慢指针写成 `slow, fast = head, head`，偶数链表中点错位，栈溢出；
2. 忘记 `slow.next = None`，链表未断开，递归无限循环；
3. 不要误以为递归版本满足进阶常数空间要求。

## ✅ 小结
✅ 优点：逻辑清晰、代码短、容易默写
❌ 缺点：存在递归栈开销，无法满足进阶要求

---

# 方案二：自底向上 迭代归并排序（进阶标准答案）
## 💡 解题思路
### 核心思想
单个节点天然有序，初始有序段长度 `seq_len = 1`；
一轮遍历链表，每次截取相邻两段长度为 `seq_len` 的有序链表，合并；
每一轮遍历结束后有序段长度翻倍 `seq_len *= 2`；
不断循环，直到有序段长度 ≥ 链表总长度，整体链表有序。

执行示例：链表 `[4,2,1,3]`
- seq_len=1：两两合并 [4]&[2]、[1]&[3] → [2,4,1,3]
- seq_len=2：两两合并 [2,4]&[1,3] → [1,2,3,4]
- seq_len=4 ≥ 总长度，结束

**关键操作要点**
1. 哨兵 `dummy` 统一处理头节点变更；
2. `last` 指针：保存已合并链表尾部，用来拼接新合并的链表；
3. 截取两段链表后**必须手动截断 next**，防止链表粘连；
4. 边界处理：遍历到末尾只剩第一段、没有第二段时，直接把第一段接上主链。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n\log n)$
  外层循环 $\log n$ 轮；每一轮完整遍历链表一次 $\(O(n)\)$。
- **空间复杂度**：$\(O(1)\)$
  仅使用有限指针变量，无递归栈、无额外数组，满足进阶要求。

## 💻 AC 代码
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
from typing import Optional
class Solution:
    def sortList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        n = 0
        p = head
        while p:
            p = p.next
            n += 1
        
        seq_len = 1
        dummy = ListNode(0)
        dummy.next = head
        while seq_len < n:
            last = dummy
            cur = dummy.next

            while cur:
                h1 = cur
                for i in range(seq_len - 1):
                    if not cur.next:
                        break
                    cur = cur.next
                
                h2 = cur.next
                cur.next = None
                cur = h2
                # 只剩第一段，没有第二段
                if not cur:
                    last.next = h1
                    break
                # 截取第二段
                for i in range(seq_len - 1):
                    if not cur.next:
                        break
                    cur = cur.next
                temp = cur.next
                cur.next = None

                last.next = self.merge(h1, h2)
                # last移动到合并链表末尾
                while last.next:
                    last = last.next
                cur = temp

            seq_len *= 2
        return dummy.next

    def merge(self, p, q) -> Optional[ListNode]:
        """合并两个有序链表"""
        temp = ListNode(0)
        dummy = temp
        while p and q:
            if q.val < p.val:
                temp.next = q
                q = q.next
            else:
                temp.next = p
                p = p.next
            temp = temp.next
        
        temp.next = p if p else q
        return dummy.next
    
    def print_list(self, head):
        """调试打印链表"""
        res = []
        cur = head
        while cur:
            res.append(str(cur.val))
            cur = cur.next
        print(" ".join(res))
```

## 🧩 关键知识点 & 技巧
1. 链表排序优先考虑归并排序；快排、堆排序不适合面试手写；
2. 面试官追问「能否不用递归、常数空间」，切换为此版本；
3. 分段截取后**强制断开 next = None**，高频遗忘点；
4. `last` 指针至关重要：负责串联每一次合并后的有序片段；
5. merge 函数和递归版本完全通用。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 忘记截断链表，链表粘连，出现环或者无限循环；
2. 缺少「只剩第一段、无第二段」边界判断，末尾节点丢失；
3. 指针变量混淆：h1、h2、cur、temp 截取长度出错；
4. 合并完成后没有移动 `last` 到链表尾部，链表片段覆盖丢失。

**避坑要点**
- 每次截取完一段链表立刻 `cur.next = None`；
- 若 `cur = h2` 后为空，必须挂载 h1，不能直接 break；
- 指针含义牢记：
  `last`：已合并尾部｜`cur`：当前起点｜`temp`：下一组起点

## ✅ 总结
本题属于链表经典高频面试题，两套方案分工清晰：
1. 常规作答：**自顶向下递归归并**，快速展示思路；
2. 进阶优化：**自底向上迭代归并**，满足题目 $\(O(1)\)$ 空间进阶要求。

核心本质：
自顶向下：**先拆分，后合并**
自底向上：**从最小有序段开始，不断向上两两合并**

---

# 📝 面试答题话术（直接背诵）
> 链表排序选用归并排序，可以达到 $O(n\log n)$。
> 第一种递归写法，利用快慢指针二分链表，递归排序左右区间再合并，代码简洁，但递归栈占用 $O(\log n)$ 空间。
> 如果题目要求常数空间，使用自底向上迭代归并：把单个节点当作有序区间，不断两两合并有序段，区间长度持续翻倍，全程只使用指针，不需要递归，满足 $\(O(1)\)$ 空间。
