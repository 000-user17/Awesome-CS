# LeetCode 刷题记录

## 📌 题目信息
- **题目编号**：148
- **题目名称**：排序链表 Sort List
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/sort-list/
- **刷题日期**：2026\08\13
- **标签**：链表 / 归并排序 / 自底向上迭代

## 📝 题目大意
给你链表的头结点 `head`，请将其按**升序**排列并返回排序后的链表。
**进阶要求**：实现 $O(n\log n)$ 时间复杂度，并且**常数级空间复杂度 $O(1)$**。

> 分析：
> 数组常用快速排序，但链表无法随机访问，快排实现麻烦、最坏 $O(n^2)$；
> 堆排序链表实现极其繁琐；
> 归并排序天然适配链表。
> 递归自顶向下归并：空间 $O(\log n)$（递归栈），不满足进阶；
> **自底向上迭代归并**：无递归，仅使用若干指针，满足 $O(1)$ 额外空间。

## 💡 解题思路
### 核心思想：自底向上归并排序
1. 单个节点天然有序，初始有序段长度 `seq_len = 1`；
2. 一轮遍历链表，每次截取相邻两段长度为 `seq_len` 的有序链表，合并；
3. 每一轮遍历完成后，有序段长度翻倍 `seq_len *= 2`；
4. 不断循环，直到有序段长度 ≥ 链表总长度，整体链表有序。

执行示例：链表 `[4,2,1,3]`
- seq_len=1：两两合并 [4]&[2]、[1]&[3] → [2,4,1,3]
- seq_len=2：两两合并 [2,4]&[1,3] → [1,2,3,4]
- seq_len=4 ≥ 总长度，结束

**关键操作要点**
1. 哨兵 `dummy` 统一处理头节点变化；
2. `last` 指针：保存已合并链表尾部，用来拼接新合并的链表；
3. 截取两段链表后**必须手动截断 next**，防止链表粘连；
4. 边界处理：遍历到末尾只剩第一段、没有第二段时，直接把第一段接上主链。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n\log n)$
  外层循环 $\log n$ 轮；每一轮完整遍历链表一次 $O(n)$。
- **空间复杂度**：$O(1)$
  仅使用有限指针变量，无递归栈、无额外数组，满足进阶要求。

## 💻 AC 代码
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def sortList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        n = 0
        p = head
        while p:
            p = p.next
            n += 1
        
        seq_len = 1
        dummy = ListNode(0)
        dummy.next = head
        while seq_len < n:
            last = dummy
            cur = dummy.next

            while cur:
                h1 = cur
                for i in range(seq_len - 1):
                    if not cur.next:
                        break
                    cur = cur.next
                
                h2 = cur.next
                cur.next = None
                cur = h2
                # 只剩第一段，没有第二段
                if not cur:
                    last.next = h1
                    break
                # 截取第二段
                for i in range(seq_len - 1):
                    if not cur.next:
                        break
                    cur = cur.next
                temp = cur.next
                cur.next = None

                last.next = self.merge(h1, h2)
                # last移动到合并链表末尾
                while last.next:
                    last = last.next
                cur = temp

            seq_len *= 2
        return dummy.next

    def merge(self, p, q) -> Optional[ListNode]:
        """合并两个有序链表"""
        temp = ListNode(0)
        dummy = temp
        while p and q:
            if q.val < p.val:
                temp.next = q
                q = q.next
            else:
                temp.next = p
                p = p.next
            temp = temp.next
        
        temp.next = p if p else q
        return dummy.next
    
    def print_list(self, head):
        """调试打印链表"""
        res = []
        cur = head
        while cur:
            res.append(str(cur.val))
            cur = cur.next
        print(" ".join(res))
```

## 🧩 关键知识点 & 技巧
1. 链表排序优先考虑归并排序；快排、堆排序不适合面试手写；
2. 自顶向下（递归）代码简单，但空间 $O(\log n)$；面试官追问进阶就写自底向上迭代；
3. 分段截取后**强制断开 next = None**，是最容易遗忘的细节；
4. `last` 指针至关重要：负责串联每一次合并后的有序片段；
5. `merge` 函数是基础工具，也是 21.合并两个有序链表原题。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 忘记截断链表，两段链表粘连，出现环或者无限循环；
2. 缺少「只剩第一段、无第二段」的边界判断，末尾节点丢失；
3. 混淆指针变量：h1、h2、cur、temp 相互覆盖，截取长度出错；
4. 外层循环条件写成 `seq_len < 2*n`，虽然能跑，但不规范；
5. 合并完成后没有移动 `last` 到链表尾部，新片段覆盖前面链表。

**避坑要点**
- 每次截取完一段链表立刻 `cur.next = None`；
- 只要 `cur = h2` 后发现 cur 为空，直接把 h1 挂载到 last 后面，不能直接 break 丢弃；
- 理清指针含义：
  `last`：已合并尾部｜`cur`：当前起点｜`temp`：下一组起点

## ✅ 总结
本题属于链表经典难题，是面试高频考题。
- 普通写法：自顶向下归并（递归，好写，不满足进阶）
- 最优进阶写法：自底向上迭代归并（本题代码）
核心思想：不拆分链表，从最小有序段向上两两合并，用空间极小的迭代方式实现 $O(n\log n)$ 排序。

---
