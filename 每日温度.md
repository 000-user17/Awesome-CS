# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：739

- **题目名称**：每日温度

- **题目难度**：中等

- **题目链接**：https://leetcode.cn/problems/daily-temperatures/?envType=problem-list-v2&envId=2cktkvj&

- **刷题日期**：2026\08\10

- **标签**：栈，数组，单调栈

## 📝 题目大意

给定一个整数数组 temperatures ，表示每天的温度，返回一个数组 answer ，其中 answer[i] 是指对于第 i 天，下一个更高温度出现在几天后。如果气温在这之后都不会升高，请在该位置用 0 来代替。

示例 1:

输入: temperatures = [73,74,75,71,69,72,76,73]
输出: [1,1,4,2,1,1,0,0]
示例 2:

输入: temperatures = [30,40,50,60]
输出: [1,1,1,0]
示例 3:

输入: temperatures = [30,60,90]
输出: [1,1,0]

## 💡 解题思路

**核心思想：** 利用单调栈记录当前温度信息，栈顶部的元素为当前遍历位置时，最大温度的元素下标。遍历过程中比较栈顶部元素，如果温度更大，则当前遍历的温度为栈顶部位置遇到的第一个更高温度

**步骤拆解：**

1. 遍历温度，判断是否栈为空，如果非空且当前遍历到的温度大于栈顶部位置下标的温度，则栈顶部下标的下一个最高温度出现在 **当前遍历下标 - 栈顶部下标** 天数后。

2. 循环上述流程直到栈为空或者当前遍历的温度小于等于栈顶部下标代表温度

3. 栈填入当前温度下标

## ⚙️ 复杂度分析

- **时间复杂度**：O\(n)

- **空间复杂度**：O\(n)


## 💻 AC 代码（Python）

```python

class Solution(object):
    def dailyTemperatures(self, temperatures):
        """
        :type temperatures: List[int]
        :rtype: List[int]
        """
        ret = [0 for i in range(len(temperatures))]
        stack = []
        for idx in range(len(temperatures)):
            while stack and temperatures[stack[-1]] < temperatures[idx]:
                curIdx = stack.pop()
                ret[curIdx] = idx - curIdx
                
            stack.append(idx)

        return ret

    
```

## 🧩 关键知识点 \& 技巧

- 技巧1：单调栈

- 技巧2：

- 易错点：

## ❌ 错题反思 / 踩坑记录



**错误原因：**

**修正思路：**

## ✅ 总结
灵活使用 **单调栈**， 且单调栈记录的是温度的下标信息， 节省时间复杂度

