# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：279
- **题目名称**：完全平方数
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/perfect-squares/
- **刷题日期**：2026\08\29
- **标签**：动态规划 / BFS / 数学

## 📝 题目大意
给你一个整数 `n` ，返回 和为 `n` 的完全平方数的最少数量。
完全平方数 是一个整数，其值等于另一个整数的平方；换句话说，其值等于一个整数自乘的积。例如，1、4、9 和 16 都是完全平方数，而 3 和 11 不是。
> 重点难点：求最少数量，可以建模成最短路径问题，也可以用动态规划或者数学定理快速求解。

## 💡 版本1：BFS解法
### 核心思想：BFS求无权图最短路径
把数字看成节点，**当前数字减去一个完全平方数**看作一条边，目标是从 `n` 走到 0，需要的最少步数就是最少平方数个数。BFS天然适合求最短路径，第一次遇到0时直接返回层数。
- 预处理所有不大于n的完全平方数
- 队列保存每一层待处理的数值
- visited集合去重，避免重复计算
**步骤拆解**
1. 预处理出所有 ≤ n 的完全平方数列表；
2. 初始化队列，存入初始值n，visited集合标记n；
3. 初始化step=0，循环处理队列每一层；
4. 每层开始step+1，依次取出当前层所有数字；
5. 当前数字依次减去每个平方数得到next_val；
6. next_val == 0 直接返回step；next_val>0且未访问则入队、标记；
7. 循环结束返回step。
示例推演：n=12
预处理平方数：[1,4,9]
初始化：queue=[12]，visited={12}，step=0
step=1，处理12 → 11,8,3入队
step=2，处理11、8、3 → 10,7,2,4入队
step=3，处理10,7,2,4
cur=4时，4-4=0，直接返回step=3
最终答案：3

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n\sqrt{n})$
- **空间复杂度**：$\(O(n)\)$

## 💻 AC 代码
```python
from collections import deque
class Solution:
    def numSquares(self, n: int) -> int:
        max_s = int(n ** 0.5)
        squares = [i * i for i in range(1, max_s + 1)]
        q = deque([n])
        visited = set([n])
        step = 0
        while q:
            layer_size = len(q)
            step += 1
            for _ in range(layer_size):
                cur = q.popleft()
                for s in squares:
                    next_val = cur - s
                    if next_val == 0:
                        return step
                    if next_val > 0 and next_val not in visited:
                        visited.add(next_val)
                        q.append(next_val)
        return step
```

## 🧩 关键知识点 & 技巧
1. BFS适合“最少数量、最短步数”类问题；
2. visited集合是核心优化，大量减少重复节点入队，防止超时；
3. 必须分层遍历，一层代表一步，不能每pop一个元素step+1；

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 不分层遍历，step计数错误；
2. 没有visited去重，n较大时超时；
3. next_val不做大于0判断，负数入队无效计算。
**避坑要点**
- BFS求最短路径核心就是按层扩散；
- 相同数值不需要多次入队，第一次到达该数时的步数一定是最优。

## ✅ 总结
BFS思路直观，适合面试口述，模型转化（最短路径）是本题核心考点。

---

## 💡 版本2：动态规划DP解法
### 核心思想
`dp[i]`：和为i的完全平方数最少数量
状态转移：$dp[i]=\min(dp[i-j^2]+1),\ j^2\le i$
意思：凑i可以由$i-j^2$的最优解再加一个平方数$j^2$。
**步骤拆解**
1. dp数组初始化无穷大，dp[0]=0；
2. 遍历1~n；
3. 遍历所有满足$j^2\le i$的j，更新dp[i]；
4. 返回dp[n]。
示例推演：n=12
dp[0]=0
dp[1] = dp[0]+1 = 1
dp[4] = dp[0]+1 =1
dp[12]=min(dp[11]+1, dp[8]+1, dp[3]+1)=3

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n\sqrt{n})$
- **空间复杂度**：$\(O(n)\)$

## 💻 AC 代码
```python
class Solution:
    def numSquares(self, n: int) -> int:
        dp = [float('inf')] * (n + 1)
        dp[0] = 0
        for i in range(1, n + 1):
            j = 1
            while j * j <= i:
                dp[i] = min(dp[i], dp[i - j * j] + 1)
                j += 1
        return dp[n]
```

## 🧩 关键知识点 & 技巧
1. dp[0]=0是基础边界；
2. 对每个i，枚举所有可以减去的平方数；

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. dp初始值设置过小；
2. 漏掉$dp[i-j^2]+1$里+1；
**避坑要点**
- 状态含义要清晰：dp[i]是凑出i的最少平方数数量。

## ✅ 总结
DP写法稳妥，属于基础动态规划模板，容易写，但大数据量下性能弱于BFS和数学法。

---

## 💡 版本3：数学四平方和定理（最优）
### 核心思想（拉格朗日四平方和定理）
任何自然数最多可以表示为4个整数的平方和，直接判定：
1. n本身是完全平方数 → 返回1
2. n可以写成两个平方数之和 → 返回2
3. $n=4^k\times(8m+7)$ → 返回4
4. 其余情况 → 返回3
**步骤拆解**
1. 判断是否完全平方数，是直接return1；
2. 不断除以4，消去因子$4^k$；
3. 如果模8等于7，return4；
4. 枚举i，判断n-i*i是否完全平方，是return2；
5. 否则return3。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(\sqrt{n})$
- **空间复杂度**：$\(O(1)\)$

## 💻 AC 代码
```python
class Solution:
    def numSquares(self, n: int) -> int:
        def is_square(x):
            s = int(x ** 0.5)
            return s * s == x
        if is_square(n):
            return 1
        while n % 4 == 0:
            n //= 4
        if n % 8 == 7:
            return 4
        i = 0
        while i * i <= n:
            if is_square(n - i * i):
                return 2
            i += 1
        return 3
```

## 🧩 关键知识点 & 技巧
1. 四平方和定理是数论结论，直接跳过大量遍历；
2. 笔试追求极限速度首选；

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. $4^k$的处理忘记循环除4；
2. 平方根判断精度问题；
**避坑要点**
- 不能直接用`int(math.sqrt(x))**2 ==x`时要注意浮点误差。

## ✅ 总结
数学解法速度最快，属于本题的高阶优化，面试可以作为加分拓展思路。

如果你需要，我可以直接把**BFS版本单独提取成一份独立精简的单模板文档**。
