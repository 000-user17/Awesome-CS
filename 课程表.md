# LeetCode 刷题记录

## 📌 题目信息
- **题目编号**：207
- **题目名称**：课程表
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/course-schedule/description/?envType=problem-list-v2&envId=2cktkvj&
- **刷题日期**：2026\08\12
- **标签**：图 / 有向图判环 / DFS / 三色标记

## 📝 题目大意
这个学期必须选修 `numCourses` 门课程，编号 `0 ~ numCourses-1`。
先修关系数组 `prerequisites`，`prerequisites[i] = [ai, bi]`：想要学习课程 `ai`，必须先完成课程 `bi`。
判断是否可以完成所有课程。等价于：**判断该有向图中是否存在环。有环则无法完成，返回False；无环返回True。**

> 边关系：`bi → ai`

## 💡 解题思路
**核心思想：三色标记DFS检测有向图环路**
三色状态定义：
0 = 未访问
1 = 访问中（当前递归路径上，再次碰到代表存在环）
2 = 访问完成（分支全部遍历完毕，无环）

**步骤拆解：**
1. 构建邻接表，根据 `[ai,bi]` 添加有向边 `bi → ai`
2. 初始化 `visited` 数组，全部初始为0（未访问）
3. 定义dfs函数：传入当前课程编号
   - 如果状态=1：找到环，返回False
   - 如果状态=2：该分支无环，直接返回True
   - 将当前节点标记为1（访问中）
   - 递归遍历当前节点所有后继课程
   - 所有后继遍历完成，标记为2（访问完成），返回True
4. 遍历全部课程节点，未访问的节点启动dfs；一旦发现环直接返回False
5. 全部节点遍历完毕，无环，返回True

## ⚙️ 复杂度分析
- **时间复杂度**：O(N+M)，N为课程数量，M为先修关系数量，每个节点、每条边只遍历一次
- **空间复杂度**：O(N+M)，邻接表存储所有边；递归栈最坏O(N)

## 💻 AC 代码（Python）
```python
from typing import List

class Solution:
    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:
        visited = [0] * numCourses
        graph = [[] for i in range(numCourses)]
        # 构建有向边 bi -> ai
        for pair in prerequisites:
            graph[pair[1]].append(pair[0])

        def dfs(course):
            if visited[course] == 1:
                return False
            if visited[course] == 2:
                return True
            visited[course] = 1
            # 遍历后继节点
            for c in graph[course]:
                if not dfs(c):
                    return False
            visited[course] = 2
            return True

        for c in range(numCourses):
            if visited[c] == 0:
                if not dfs(c):
                    return False
        return True
```

## 🧩 关键知识点 & 技巧
- 技巧1：有向图判环不能只用二值visited（True/False），必须三色标记区分「正在遍历路径」和「已遍历完成分支」
- 技巧2：先理清边的方向 `[ai,bi] → bi → ai`，方向写反整张图逻辑失效
- 技巧3：递归结束后把节点标记为2，实现“回溯”，不影响其他分支遍历
- 易错点：
  1. 邻接表构建时边方向写反，是最高频错误
  2. 使用二值数组代替三色标记，导致跨分支误判环路
  3. DFS传入邻接列表而非课程编号，递归逻辑彻底混乱

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 最初使用只有True/False的visited数组，无法区分当前递归路径和已经遍历完成的节点，出现误判；
2. 邻接表构建方向颠倒，`graph[pair[0]].append(pair[1])`，图结构完全错误；
3. DFS函数设计错误，错误传入邻居列表，而非课程编号，循环下标错乱。

**修正思路：**
1. 采用三色标记法区分节点状态；
2. 牢记题意 `[ai,bi]`：学ai先学bi，边 `bi → ai`；
3. DFS入参为课程编号，遍历该节点的所有后继节点。

## ✅ 总结
本题本质是**有向图环路检测**。
无向图简单二值标记可以判环，但**有向图必须三色标记DFS或拓扑排序**。
三色DFS核心：节点进入递归标记为“访问中”，离开递归标记为“访问完成”；再次遇到“访问中”节点代表存在环路。
拓扑排序（BFS入度法）是另一套主流解法，两种思路可以互相印证。
