# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：33
- **题目名称**：搜索旋转排序数组
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/search-in-rotated-sorted-array/
- **刷题日期**：2026\09\02
- **标签**：数组 / 二分查找

## 📝 题目大意
整数数组 `nums` 原本按升序排列，数组元素互不相同。在某个下标 k 进行左旋，数组被拆分成两段升序子数组。给定目标值 `target`，存在则返回下标，不存在返回 `-1`。
要求算法时间复杂度 $O(\log n)$。
> 重点难点：整体数组无序，但中点分割后，**左右两部分至少有一半是有序的**；本题代码采用**左闭右开 `[left, right)`** 二分区间模板。

## 💡 解题思路
### 核心思想：二分查找，利用局部有序判断搜索区间
旋转数组整体无序，但 `mid` 将区间切分后，左半段 `[left, mid)`、右半段 `[mid+1, right)` 二者必有一段有序。
1. 如果 `nums[left] < nums[mid]`：代表左半段 `[left, mid)` 有序；
   - 若 `target` 落在左半有序区间内：收缩右边界，`right = mid`，去左半边查找；
   - 否则目标一定在右半区间，`left = mid + 1`。
2. 否则代表右半段 `[mid+1, right)` 有序；
   - 右半区间的末尾元素是开边界前一位 `nums[right‑1]`；
   - 若 `target` 落在右半有序区间内：`left = mid + 1`，向右搜索；
   - 否则目标落在左边，`right = mid`。
3. 循环中若 `nums[mid]==target`，直接返回下标；循环结束未找到返回 `-1`。

**步骤拆解**
1. 初始化二分区间 `left=0，right=n`，左闭右开；
2. `while left < right` 循环，计算 `mid = (left + right) // 2`；
3. 命中 target，直接返回 mid；
4. 判断左半段是否有序，根据 target 的位置收缩对应区间；
5. 左半无序，则右半一定有序，结合区间末尾元素 `nums[right‑1]` 判断 target 范围，移动指针；
6. 循环结束没有找到元素，返回 `-1`。

示例推演：`nums = [4,5,6,7,0,1,2], target = 0`
初始：`left=0, right=7`
- mid=3，num=7，`7>0`；`nums[0]<7`左半有序；target不在`[4,7]`，`left=4`
- mid=(4+7)//2=5，num=1，`1>0`；`nums[4]=0`不小于num=1，右半有序；执行`right=5`
- mid=(4+5)//2=4，num=0，命中target，返回4。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(\log n)$，二分查找，每次区间折半
- **空间复杂度**：$\(O(1)\)$，仅常数变量，无额外空间

## 💻 AC 代码
```python
from typing import List
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        n = len(nums)
        left, right = 0, n
        while left < right:
            mid = (left + right) // 2
            num = nums[mid]
            if num == target:
                return mid
            elif num > target:
                if nums[left] < num:
                    if nums[left] <= target:
                        right = mid
                    else:
                        left = mid + 1
                else:
                    right = mid
            else:
                if nums[left] < num:
                    left = mid + 1
                else:
                    if nums[right - 1] < target:
                        right = mid
                    else:
                        left = mid + 1
        return -1
```

## 🧩 关键知识点 & 技巧
1. 旋转数组二分核心：**不找旋转点，每次判断哪一半区间局部有序**，利用有序区间判定 target 所在范围；
2. 本实现使用**左闭右开 `[left, right)`**，开边界不能直接访问，区间真实末尾元素取 `nums[right‑1]`；
3. 本题数组元素互不重复，使用 `nums[left] < nums[mid]` 判断左半段有序；
4. 二分每一条分支都**必须修改 left / right**，保证搜索区间不断缩小，防止死循环。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 直接套用普通升序数组二分，没有处理旋转后局部有序逻辑；
2. 左闭右开模板误用，直接访问 `nums[right]`，造成索引越界；
3. 某分支只定义临时变量，忘记修改 `left`、`right`，区间不收缩，发生死循环；
4. 边界条件写反，`<=`、`<` 混用，漏掉目标元素。

**避坑要点**
- 区分两套二分模板：闭区间 `while left <= right`；左闭右开 `while left < right`，不要混用；
- 左闭右开：`right` 是开边界，不在搜索区间内；
- 旋转数组，判断有序看的是区间两端元素，**不是 mid 和 target 的大小**。

## ✅ 总结
33 搜索旋转排序数组，属于二分的经典变形。
数组整体无序，但一定存在局部有序子段；利用局部有序判断 target 在哪一侧，完成二分收缩。
> 拓展：LeetCode81 搜索旋转排序数组Ⅱ，数组允许重复元素，`nums[left] < nums[mid]` 判断失效，需要单独处理相等情况。

> 补充对比：
> - 闭区间模板逻辑更直观，新手优先；
> - 左闭右开适合求插入位置、边界查找，但旋转数组场景边界极易出错。
