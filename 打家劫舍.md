# LeetCode 刷题记录

## 📌 题目信息
- **题目编号**：198
- **题目名称**：打家劫舍
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/house-robber/description/?envType=problem-list-v2&envId=2cktkvj&
- **刷题日期**：2026\08\12
- **标签**：动态规划 / DP

## 📝 题目大意
你是一个小偷，计划沿街偷窃房屋。每间房屋存放一定金额现金。
约束条件：**不能同时偷窃相邻两间房屋**。
给定数组 `nums`，每个元素代表房屋现金，求一夜能偷窃到的最大总金额。

核心理解：到达第i间房屋时有两种选择
1. 不偷第i间：最大金额 = dp[i-1]
2. 偷第i间：最大金额 = dp[i-2] + nums[i]
最终取两者较大值。

## 💡 解题思路
**核心思想：线性动态规划**
标准DP状态定义：
`dp[i]`：前 `i` 间房屋能偷窃的最大金额
状态转移方程：
$$dp[i] = \max(dp[i-1], dp[i-2] + nums[i])$$

**步骤拆解：**
1. 处理边界：只有1间房直接返回；2间房取两者最大值；
2. 初始化dp数组，dp[i]初始为nums[i]；
3. 从第3个房屋（下标i=2）开始循环；
4. 思路：`dp[i] = nums[i] + max(dp[i-2], dp[i-3])`
5. 遍历过程持续更新全局最大值，最后返回答案。

> 优化方向：不需要完整dp数组，只保存前两项数值，空间从O(n)优化到O(1)。

## ⚙️ 复杂度分析
### 原始数组DP版本
- **时间复杂度**：O(n)，n为房屋数量，仅一次遍历
- **空间复杂度**：O(n)，开辟dp数组

### 空间优化版（双变量）
- **时间复杂度**：O(n)
- **空间复杂度**：O(1)，仅使用常数变量

## 💻 AC 代码
```python
from typing import List

class Solution:
    def rob(self, nums: List[int]) -> int:
        n = len(nums)
        if n == 1:
            return nums[0]
        if n == 2:
            return max(nums[0], nums[1])
        
        maxCoin = max(nums[0], nums[1])
        dp = [nums[i] for i in range(n)]
        for i in range(2, n):
            dp[i] = dp[i] + max(dp[i - 2], dp[i - 3] if i >= 3 else 0)
            maxCoin = max(dp[i], maxCoin)
        return maxCoin
```

## 💻 AC 代码 优化
```python
from typing import List

class Solution:
    def rob(self, nums: List[int]) -> int:
        n = len(nums)
        if n == 1:
            return nums[0]
        dp = [0] * n
        dp[0] = nums[0]
        dp[1] = max(nums[0], nums[1])
        for i in range(2, n):
            dp[i] = max(dp[i-1], dp[i-2] + nums[i])
        return dp[-1]
```

## 💻 空间最优优化版本（O(1)空间）
```python
from typing import List

class Solution:
    def rob(self, nums: List[int]) -> int:
        prev2 = 0
        prev1 = 0
        for val in nums:
            cur = max(prev1, prev2 + val)
            prev2 = prev1
            prev1 = cur
        return prev1
```

## 🧩 关键知识点 & 技巧
- 技巧1：线性DP重点找准**状态定义**和**转移方程**；
- 技巧2：本题标准转移 `max(dp[i-1], dp[i-2]+nums[i])` 可以直接套用在环形房屋（打家劫舍II）拓展题；
- 技巧3：线性DP很多场景不需要完整数组，只用两个变量保存前两项，实现空间压缩；
- 易错点：
  1. 边界条件漏掉长度为1的数组；
  2. 转移方程想当然写成 `dp[i]=dp[i-2]+nums[i]`，忘记对比不偷当前房屋的情况；
  3. 你的变形写法 `max(dp[i-2], dp[i-3])` 仅适用于本题，不推荐直接套用到变种题目。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 一开始没有提炼通用DP转移公式，自行推导变形式子，虽然能AC，但难以迁移到后续环形房屋变种题目；
2. 使用dp数组存储全部结果，没有做空间优化；
3. 需要额外维护maxCoin变量，标准写法直接使用dp末尾元素，代码更简洁。

**修正思路：**
1. 优先掌握标准DP转移方程，保证可拓展性；
2. 熟练掌握双变量空间压缩技巧，降低空间开销；
3. 理解两种选择：偷当前房屋 / 不偷当前房屋。

## ✅ 总结
打家劫舍是入门级线性动态规划经典题。
核心抉择：**选当前位置，则不能选前一位；不选当前位置，则继承前一位最优解**。
标准转移方程：`dp[i] = max(dp[i-1], dp[i-2]+nums[i])`。
在此基础上可以进阶练习环形房屋、树形房屋两道同类拓展题。
