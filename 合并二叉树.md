# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：617
- **题目名称**：合并二叉树
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/merge-two-binary-trees/
- **刷题日期**：2026\09\07
- **标签**：二叉树/DFS/递归

## 📝 题目大意
想象一下，当你将其中一棵覆盖到另一棵之上时，两棵树上的一些节点将会重叠（而另一些不会）。你需要将这两棵树合并成一棵新二叉树。合并的规则是：如果两个节点重叠，那么将这两个节点的值相加作为合并后节点的新值；否则，不为 null 的节点将直接作为新二叉树的节点。
返回合并后的二叉树。
注意: 合并过程必须从两个树的根节点开始。

## 💡 解题思路
**核心思想：**递归，函数输入两棵树的节点，返回合并完成后的子树根节点。遇到空节点直接返回另一棵树的节点；两个节点都存在时值相加，再递归合并左右子节点。

**步骤拆解：**
1. 确定递归函数：入参为 `root1`、`root2`，返回合并后的子树根节点。
2. 递归边界条件：
    a. 如果 `root1` 为空，直接返回 `root2`。root1不存在，直接取用root2子树，无需继续递归子节点。
    b. 如果 `root2` 为空，直接返回 `root1`。root2不存在，直接取用root1子树。
3. 当前层处理：两个节点都不为空
    a. 创建新节点，节点值等于两个节点值相加。
    b. 递归合并两棵树的左子节点，赋值给新节点的left。
    c. 递归合并两棵树的右子节点，赋值给新节点的right。
4. 返回新构建的节点。

> 补充：也可以原地修改其中一棵树，直接复用原有节点，节省内存；但会修改输入原始二叉树。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(\min(m,n))$，m、n分别为两棵二叉树节点数量。只遍历两棵树重叠的节点。
- **空间复杂度**：$O(\min(m,n))$，递归调用栈深度；最坏情况树退化为链表。

## 💻 AC 代码（Python）
### 版本1：新建节点，不修改原输入（推荐）
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def mergeTrees(self, root1: TreeNode, root2: TreeNode) -> TreeNode:
        # 边界：其中一棵树节点为空，直接返回另一棵的节点
        if not root1:
            return root2
        if not root2:
            return root1
        
        # 两个节点都存在，新建节点，值相加
        new_node = TreeNode(root1.val + root2.val)
        new_node.left = self.mergeTrees(root1.left, root2.left)
        new_node.right = self.mergeTrees(root1.right, root2.right)
        return new_node
```

### 版本2：原地修改，复用root1（节省空间）
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def mergeTrees(self, root1: TreeNode, root2: TreeNode) -> TreeNode:
        if not root1:
            return root2
        if not root2:
            return root1
        
        root1.val += root2.val
        root1.left = self.mergeTrees(root1.left, root2.left)
        root1.right = self.mergeTrees(root1.right, root2.right)
        return root1
```

## 🧩 关键知识点 \& 技巧
- 技巧1：二叉树递归模板，先处理边界，再处理当前节点，递归左、递归右，返回节点。
- 技巧2：只要其中一个节点为None，直接返回另一个节点，递归会自动接管后续子树，不需要额外判断左右孩子是否为空。
- 技巧3：两种实现：新建节点不破坏原树；原地修改节省内存，工程环境慎用。
- 易错点：**先判空，再访问 `.val`**，空节点访问val会报属性错误。

## ❌ 错题反思 / 踩坑记录
**错误1：没有提前判空，直接访问root1.val / root2.val**
错误原因：当节点为None时，访问.val会抛出异常。
修正思路：递归最开头优先处理节点为空的边界条件。

**错误2：手动if判断左右子树是否为None再递归**
错误原因：写大量冗余if‑else，代码臃肿，容易漏分支。
修正思路：直接把left、right传入递归函数，空的情况交给递归边界处理。

## ✅ 总结
本题是二叉树递归基础模板题。递归函数的语义要明确：输入两个子树根，输出合并后的子树根。
边界处理是关键：只要有一棵树当前节点为空，直接返回另一棵子树，不需要继续往下遍历。
递归会把空、非空节点的子树全部拼接完成。
原地修改可以节省内存，但会改变输入数据；算法题两种写法均可，业务代码优先新建节点。
