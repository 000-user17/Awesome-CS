# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：56
- **题目名称**：合并区间 Merge Intervals
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/merge-intervals/](https://leetcode.cn/problems/merge-intervals/)
- **刷题日期**：2026\09\14
- **标签**：数组 / 排序

## 📝 题目大意
给区间数组 `intervals`，每个元素 `[start, end]`，合并所有重叠或者相接的区间，返回合并之后无重叠的区间列表。

示例：
`intervals = [[1,3],[2,6],[8,10],[15,18]]`
输出：`[[1,6],[8,10],[15,18]]`
解释：`[1,3]` 和 `[2,6]` 重叠，合并成 `[1,6]`

> 核心思想：**先按照区间起点排序，再依次遍历合并**。
> 如果不排序，区间乱序无法直接判断是否重叠。

## 💡 解题思路
### 步骤
1. **排序**：按每个区间的**起始值 start**从小到大排序。排序之后，能重叠的区间一定是相邻的。
2. 创建结果数组 `res`，把排序后的第一个区间先放进res。
3. 依次遍历剩下每一个区间 `cur`：
    - 取出结果数组最后一个区间 `last = res[-1]`
    - 判断：`cur[0] > last[1]`
      ✅ 当前区间起点 > 上一个区间终点 → **不重叠**，直接把cur加入res
      ❌ 否则重叠/相接，需要合并：更新res最后区间的终点 `last[1] = max(last[1], cur[1])`
> 合并时，新终点取两者较大的end，不能直接用cur[1]，防止cur被包含在旧区间内。

> 重叠判断逻辑：
> `[1,5]` 和 `[2,3]`：2<=5，重叠，合并后还是[1,5]
> `[1,4]` 和 `[2,6]`：合并成[1,6]
> `[1,2]` 和 `[3,4]`：不重叠，分开

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n\log n)$，主要消耗在排序，遍历是O(n)
- **空间复杂度**：$O(\log n)$，排序需要栈空间；不计返回结果数组

## 💻 AC 代码
```python
class Solution:
    def merge(self, intervals: list[list[int]]) -> list[list[int]]:
        if not intervals:
            return []
        # 按区间起点排序
        intervals.sort(key=lambda x: x[0])
        res = [intervals[0]]
        for cur in intervals[1:]:
            last = res[-1]
            if cur[0] > last[1]:
                # 无重叠，直接添加
                res.append(cur)
            else:
                # 重叠，更新末尾区间的end，取最大值
                last[1] = max(last[1], cur[1])
        return res
```

## 🧩 关键知识点 & 技巧
1. **必须先排序**，不排序不能保证重叠区间相邻；
2. 合并更新终点用`max(last[1], cur[1])`，不能直接赋值cur[1]；
3. 区间相接如`[1,4] [4,5]`，题目要求合并，`cur[0]==last[1]`属于要合并场景；
4. 边界：输入为空数组，直接返回空。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 忘记排序，直接遍历，乱序区间合并出错；
2. 合并时直接写`last[1]=cur[1]`，忽略cur区间被包含在老区间里面的情况；
3. 边界判断写成 `cur[0] >= last[1]` 逻辑搞反；
4. 输入为空没有特判，取`intervals[0]`索引报错。

**避坑要点**
> 排序是前提；合并只修改结果数组最后一个区间的右端点。

## ✅ 总结
56区间模板题。先按start升序排序，维护结果列表，逐个比较和末尾区间是否重叠，重叠则合并右端点。
关联题目：57插入区间、452用最少数量的箭引爆气球
