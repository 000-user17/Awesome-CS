# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：78
- **题目名称**：子集 Subsets
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/subsets/](https://leetcode.cn/problems/subsets/)
- **刷题日期**：2026\09\09
- **标签**：回溯 / 位运算

## 📝 题目大意
给你一个整数数组 `nums`，数组元素互不相同，返回数组所有可能的子集（幂集）。
要求：
1. 解集不能包含重复子集；
2. 可以任意顺序返回；
> 子集包括：空集 + 所有单个元素 + 所有组合。
> 数组长度n，总子集数量 = $2^n$，每个元素有「选 / 不选」两种状态。

> 示例 nums = [1,2,3]
> 输出：`[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]`

> 分析：
> 暴力思路：枚举所有组合，但是手动写循环无法适配任意n。
> ✅ 主流两种解法：**回溯法（DFS）**（面试最推荐）；**位运算**。
> 回溯思路：遍历每个元素，对每个元素做两种决策：选 或者 不选，递归向下，到达数组末尾时收集当前路径作为一个子集。

## 💡 解题思路
### 回溯（DFS）核心架构
1. 维护一个临时路径 `path`，保存当前正在构建的子集；`res` 存最终全部结果
2. 递归函数参数：当前处理到数组的下标 `start`
3. 递归逻辑：
    - 每次进入递归，先把当前`path`复制一份加入结果集（每一步path都是合法子集）
    - 从`start`开始遍历：
        - 选择当前nums[i]，加入path
        - 递归处理下一个位置 i+1
        - 回溯：撤销选择，把nums[i]从path弹出
4. 初始调用：`backtrack(start=0)`

> 模拟 nums=[1,2,3]
> start=0, path=[] → 存入[]
> - 选1，path=[1]，递归start=1
>   path=[1]存入结果
>   - 选2，path=[1,2]，递归start=2
>       path=[1,2]存入
>       -选3 → [1,2,3]存入，回溯弹出3
>   弹出2，选3 → [1,3]存入，回溯弹出3
> 弹出1，选2，path=[2]存入
>   -选3 → [2,3]存入
> 弹出2，选3 → [3]存入

### 可选思路：位运算
n个元素，用0 ~ $2^n-1$ 的二进制数字。
二进制每一位代表：0=不选，1=选对应位置元素。
例 n=3：数字5 → `101`，取第0、2位 → [1,3]

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n \times 2^n)$，总共有$2^n$个子集；每个子集复制保存最多花费\(O(n)\)
- **空间复杂度**：$\(O(n)\)$，递归栈深度最多n，path数组最多存n个元素（不计返回结果占用空间）

## 💻 AC 代码（回溯，面试首选）
```python
class Solution:
    def subsets(self, nums: list[int]) -> list[list[int]]:
        res = []
        path = []
        def backtrack(start):
            # 收集当前path作为一个子集，注意拷贝
            res.append(path.copy())
            for i in range(start, len(nums)):
                # 选当前元素
                path.append(nums[i])
                # 下一层只能从i+1开始，避免重复组合
                backtrack(i + 1)
                # 回溯，撤销选择
                path.pop()
        backtrack(0)
        return res
```

### 位运算版本（拓展）
```python
class Solution:
    def subsets(self, nums: list[int]) -> list[list[int]]:
        n = len(nums)
        res = []
        for mask in range(0, 1 << n):
            tmp = []
            for i in range(n):
                if mask & (1 << i):
                    tmp.append(nums[i])
            res.append(tmp)
        return res
```

## 🧩 关键知识点 & 技巧
1. 回溯核心：**选/不选**；`start`变量保证只向后选，不会生成`[1,2]`和`[2,1]`这种重复组合；
2. `res.append(path.copy())`，**必须拷贝**！直接append(path)存的是引用，后续path修改会改动已经存入结果里的数据；
3. 子集、组合类回溯：递归循环从start开始；排列题才从0开始；
4. 数组元素互不相同，所以不会产生重复子集；如果有重复元素（90题子集II），需要排序+去重。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 直接 `res.append(path)` 不拷贝，结果数组全部是空列表；
2. 循环不从start开始，每次i从0开始，产生排列，出现`[1,2] [2,1]`重复；
3. 忘记回溯pop，path不断累加，数组越来越长；
4. 混淆子集、组合、排列：子集不考虑顺序，排列考虑顺序。

**避坑要点**
- 保存子集一定要拷贝path；
- 组合/子集：`i 从 start`；排列：i从0，并且用used数组标记是否使用；
- 78无重复元素；90子集II存在重复元素，需要增加去重逻辑。

## ✅ 总结
78是回溯入门模板题。核心：每个元素二选一（选/不选），start控制只向后取元素，避免重复组合，每次递归把当前path加入结果。
面试优先写回溯版本，位运算可以作为优化补充。
> 关联题目：90 子集II（数组含重复元素，需要去重）、39组合总和、46全排列
