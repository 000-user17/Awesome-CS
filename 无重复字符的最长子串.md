# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：3
- **题目名称**：无重复字符的最长子串 Longest Substring Without Repeating Characters
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/longest-substring-without-repeating-characters/
- **刷题日期**：2026\09\04
- **标签**：滑动窗口 / 双指针 / 哈希表

## 📝 题目大意
给定字符串 `s`，找出**不含重复字符**的最长子串的长度。子串是**连续**的一段字符。

示例：
`s = "abcabcbb"` →输出 `3`（`abc`）
`s = "bbbbb"` →输出 `1`
`s = "pwwkew"` →输出 `3`（`wke`，注意`pwke`是子序列，不是子串）

> 分析：
1. 暴力：枚举所有子串，用集合判重，时间 $O(n^2)$，会超时。
2. 最优：**滑动窗口（双指针+哈希表）**，$\(O(n)\)$，每个元素最多进出窗口一次。
> 窗口：`[left, right]`，代表当前正在考察的无重复字符的子串区间。
> right不断向右扩张；一旦出现重复字符，移动left收缩窗口，保证窗口内部没有重复。

## 💡 解题思路
### 核心思想：滑动窗口
- `right`：右指针，逐个向后遍历字符，作为窗口右端。
- `left`：左指针，窗口的左端，**只向右走，绝不回退**。
- 哈希表`last_occur`：记录**字符上一次出现的下标**。

流程：
1. right遍历每一个字符`s[right]`
2. 如果这个字符曾经出现过，并且**上一次出现的下标 ≥ left**：说明该字符现在就在窗口内部，发生重复。
   → 需要把left更新为 `last_occur[char] + 1`，跳过重复的旧字符。
3. 更新当前字符最新的下标：`last_occur[char] = right`
4. 计算当前窗口长度 `right‑left+1`，更新全局最大长度。

>关键点：left不能往左回退，只向右移动。
>判断条件：只有重复字符**在当前窗口内**，才移动left；历史上出现过、但是在窗口左边外面，不用管。

举例 `pwwkew`
- right=0(p):不在map，存p:0，max=1
- right=1(w):不在map，存w:1，max=2
- right=2(w):w上一次下标是1，>=left(0) → left更新到2；更新w:2；窗口[2,2]，max依旧2
- right=3(k):存入k:3，窗口[2,3] len=2
- right=4(e):存入e:4，窗口[2,4] len=3 →max=3
- right=5(w):w上一次下标2 >= left(2) → left更新到3；更新w:5；窗口[3,5] len=3。最终max=3。

## ⚙️复杂度分析
- **时间复杂度**：$\(O(n)\)$，n字符串长度，right遍历一遍，left只向右移动，总共最多n次操作。
- **空间复杂度**：$\(O(Σ)\)$，字符集大小；如果是ascii字符最多128。

## 💻 AC代码
```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        last_occur = dict()  #字符:上一次出现的下标
        max_len = 0
        left = 0
        for right, char in enumerate(s):
            # 当前字符曾经出现，并且重复位置落在窗口内，移动left
            if char in last_occur and last_occur[char] >= left:
                left = last_occur[char] + 1
            # 更新字符最新位置
            last_occur[char] = right
            # 更新最大长度
            current_len = right - left + 1
            if current_len > max_len:
                max_len = current_len
        return max_len
```

## 🧩关键知识点 &技巧
1. **滑动窗口模板：left只右移，不回退**，这是本题时间\(O(n)\)的核心。
2. 条件 `last_occur[char] >= left` 非常关键：
    字符以前出现过，但出现在left左边（不在当前窗口），不算窗口内重复，不需要移动left。
3. 每次无论是否重复，都要更新该字符的最新下标。
4. 子串是连续；不要和子序列混淆。
5. 边界：空字符串`""`返回0，单字符返回1，全部相同字符返回1。

## ❌错题反思 /踩坑记录
1. 漏掉判断 `>= left`，只要字符在字典里就直接移动left。
例子 `abba`：这个坑非常经典。
`s="abba"`：
right=0:a，map[a]=0
right=1:b，map[b]=1
right=2:b，b旧下标1≥left0 → left=2，map[b]=2
right=3:a，a旧下标0；如果不判断`>=left`，直接left=0+1=1，窗口变成[1,3]，里面有b,b重复，结果出错。
> 必须加上 `last_occur[char] >= left`。

2. 忘记更新字符最新下标，map里面保存的是旧位置。
3. max_len初始化错，初始设成1，空字符串输入`""`得到错误答案。
4. 窗口长度算错：`right‑left`，忘记+1。

**避坑要点**
- 一定要加 `last_occur[char] >= left` 的判断，处理历史重复、但不在窗口内的字符；
- 每次循环都更新字符的下标；
- 窗口长度 `right‑left+1`。

## ✅总结
中等高频滑动窗口题。
用双指针维护`[left,right]`无重复字符窗口，哈希保存字符最近下标；right不断扩张，出现窗口内部重复就右移left收缩，记录窗口最大长度。
重点坑：`abba`测试用例，必须判断重复字符是否落在当前窗口中。

>拓展：也可以用集合版本滑动窗口，逻辑更好理解，但会有while循环删除左边元素；上面字典版本效率更高。

集合版本简单示例（方便理解）：
```python
def lengthOfLongestSubstring(s):
    window = set()
    left = 0
    max_len = 0
    for right in range(len(s)):
        while s[right] in window:
            window.remove(s[left])
            left +=1
        window.add(s[right])
        max_len = max(max_len, right-left+1)
    return max_len
```
集合版本也是\(O(n)\)，但会多一层while；字典版本一步定位left，更优。
