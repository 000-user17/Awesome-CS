# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：406
- **题目名称**：根据身高重建队列
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/queue-reconstruction-by-height/
- **刷题日期**：2026\08\18
- **标签**：贪心 / 排序

## 📝 题目大意
数组 `people`，`people[i] = [hi, ki]`，代表身高 `hi`，队列中**前面恰好有 ki 个身高大于或等于 hi 的人**。
输入乱序，要求重构出合法队列。

> 重点难点：如何安排插入顺序，保证插入操作不会破坏已排好人员的约束条件。

## 💡 解题思路
### 核心思想：贪心策略，先排序，再依次插入
排序规则：
1. 身高 `h` **降序**排列；
2. 身高相等时，`k` **升序**排列。

原理：
优先处理高个子。高个子先进入队列，后续插入的都是身高更矮或等高的人。
矮个子插入时，只会排在队列内部，**不会改变已经存在的高个子前面大于等于自身的人数**。

对于当前 `[h, k]`：
队列中现存所有人身高 ≥ 当前人，想要前面恰好有 k 个更高/等高的人，**直接插入到下标 k 的位置**。

**步骤拆解**
1. 按照规则排序 people；
2. 初始化空结果列表；
3. 遍历排序后的每个人，将其插入结果数组索引 `k` 处；
4. 遍历完成得到最终队列。

示例推演：`people = [[6,1],[5,0],[4,3],[7,0],[5,2],[7,1]]`
排序后：`[[7,0],[7,1],[6,1],[5,0],[5,2],[4,3]]`
- `[7,0]` 插入下标0 → `[[7,0]]`
- `[7,1]` 插入下标1 → `[[7,0],[7,1]]`
- `[6,1]` 插入下标1 → `[[7,0],[6,1],[7,1]]`
- `[5,0]` 插入下标0 → `[[5,0],[7,0],[6,1],[7,1]]`
- `[5,2]` 插入下标2 → `[[5,0],[7,0],[5,2],[6,1],[7,1]]`
- `[4,3]` 插入下标3 → `[[5,0],[7,0],[5,2],[4,3],[6,1],[7,1]]`

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n^2)$
  排序 $O(n\log n)$；列表 `insert` 属于数组移位操作，单次 $\(O(n)\)$，n次插入总计 $O(n^2)$。
- **空间复杂度**：$\(O(n)\)$，存储结果数组；排序若不计栈开销，额外空间为常数。

> 拓展：数据量极大时，可用树状数组+二分查找优化至 $O(n\log n)$。

## 💻 AC 代码
```python
from typing import List
class Solution:
    def reconstructQueue(self, people: List[List[int]]) -> List[List[int]]:
        # 身高降序，同身高k升序
        people.sort(key=lambda x: (-x[0], x[1]))
        res = []
        for p in people:
            # 插入到下标k位置
            res.insert(p[1], p)
        return res
```

## 🧩 关键知识点 & 技巧
1. 贪心顺序选择是本题突破口：**必须从高到低处理**；
   如果从小到大插入，后续高个子插入会打乱前面所有人的 k 值；
2. 身高相同按k升序：等高的人，k更小的要排在前面；
3. 利用插入位置等价约束：现有所有人都≥当前身高，索引k天然满足前面有k个符合条件的人；
4. Python list.insert(pos, val) 会把元素放在pos，原有元素向后顺移。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 排序条件写反：按身高升序，直接导致整个逻辑失效；
2. 身高相等时 k 降序排序，产生错误队列；
3. 误解题意：把条件理解为「前面严格大于h」，忽略等于的情况。

**避坑要点**
- 约束条件：**大于或等于 hi**，等高人员互相计数；
- 贪心核心：先固定高个子，矮个子插入不影响前人；
- 不要尝试多重循环暴力枚举排列，时间复杂度爆炸。

## ✅ 总结
经典贪心应用题。难点不在代码，在于想到「高个子优先、按k下标插入」的贪心策略。
代码简洁，是面试高频模板写法；需要理解贪心正确性，不能死记排序规则。

需要我把这两道题整理成一份统一格式的Markdown文档方便你归档吗？
