# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：96
- **题目名称**：不同的二叉搜索树 Unique Binary Search Trees
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/unique-binary-search-trees/](https://leetcode.cn/problems/unique-binary-search-trees/)
- **刷题日期**：2026\09\09
- **标签**：动态规划 / 二叉搜索树 / 卡特兰数

## 📝 题目大意
给你一个整数 `n`，求恰由 `n` 个节点组成且节点值从 `1` 到 `n` 互不相同的二叉搜索树有多少种，返回满足题意的二叉搜索树的种数。

> 分析：
> 二叉搜索树性质：选定一个数字 `i` 作为根节点，**左边所有数字 < i，右边所有数字 > i**。
> 根确定后，左子树只能由 `[1,i-1]` 构建，右子树只能由 `[i+1,n]` 构建。
> 左子树方案数 × 右子树方案数 = 当前根对应的总方案数。
> 把所有可以作为根的 i 的方案累加，就是 n 个节点的总BST数量。
> 这个数列就是**卡特兰数(Catalan Number)**。

## 💡 解题思路
### DP 思路
定义 `dp[i]`：**i 个互不相同节点，可以组成的二叉搜索树的数量**
1. 初始条件：
    - `dp[0] = 1`：0个节点，空树，算作1种方案（乘法单位元）
    - `dp[1] = 1`：1个节点，只有1种树
2. 状态转移：
对于 `dp[i]`，我们枚举根节点位置 `j`（j从1~i）：
- 根是 j：左子树有 `j-1` 个节点；右子树有 `i-j` 个节点
- 当前j贡献方案：`dp[j-1] * dp[i-j]`
$$
dp[i]=\sum_{j=1}^{i} dp[j-1] \times dp[i-j]
$$
3. 最终答案：`dp[n]`

> 卡特兰数公式（可选，数学公式直接算）
> $$C_n=\frac{1}{n+1}\binom{2n}{n}$$
> 面试优先写DP，卡特兰公式作为拓展。

### 举例 n=3
- j=1作为根：左0个节点，右2个节点 → dp[0]*dp[2] =1*2=2
- j=2作为根：左1个节点，右1个节点 → dp[1]*dp[1] =1*1=1
- j=3作为根：左2个节点，右0个节点 → dp[2]*dp[0] =2*1=2
总和 dp[3]=2+1+2 =5，和样例一致。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n^2)$，两层循环
- **空间复杂度**：$\(O(n)\)$，dp数组存储

## 💻 AC 代码（DP版本，面试首选）
```python
class Solution:
    def numTrees(self, n: int) -> int:
        # dp[i]：i个节点的BST数量
        dp = [0]*(n+1)
        dp[0] = 1
        dp[1] = 1
        for i in range(2, n+1):
            # 枚举根节点 j
            for j in range(1, i+1):
                dp[i] += dp[j-1] * dp[i-j]
        return dp[n]
```

## 🧩 关键知识点 & 技巧
1. 关键点：**数字连续不影响树的形态数量**。`[1,2,3]` 和 `[2,3,4]` 构建BST种类一样，只和节点个数有关；
2. `dp[0]=1` 是理解难点：空树是一种合法子树，用于乘法计算；
3. 拆分思想：根固定，左右子树独立，方案数相乘（组合计数乘法原理）；
4. 这是卡特兰数经典应用场景：BST计数、括号匹配、栈进出序列、多边形三角剖分。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. dp[0] 设置为0，所有结果全部变成0；
2. 误以为节点值大小会影响数量，纠结数字排列；
3. 转移方程写成加法而不是乘法，左右子树是组合关系，不是相加；
4. 循环边界写错，j范围不对。

**避坑要点**
- 计数问题：独立子问题方案相乘，不同根情况相加；
- dp[0]=1，记住是空树的定义；
- 区分：本题求**数量**；95题是输出所有BST（递归生成），不要混淆两道题。

## ✅ 总结
96题是卡特兰数+DP计数题，核心思路：选定根节点，拆分左右子树，利用乘法原理累加所有根的方案。
面试可以先说DP思路，最后补充一句：这个数列是卡特兰数。
