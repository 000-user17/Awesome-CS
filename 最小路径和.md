# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：64
- **题目名称**：最小路径和 Minimum Path Sum
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/minimum-path-sum/](https://leetcode.cn/problems/minimum-path-sum/)
- **刷题日期**：2026\09\14
- **标签**：动态规划 DP

## 📝 题目大意
给定 m × n 的非负整数网格 grid，从**左上角(0,0)**走到**右下角(m-1,n-1)**。
只能**向右 或者 向下**走一步，求路径数字总和的最小值。

示例：
```
grid = [
  [1,3,1],
  [1,5,1],
  [4,2,1]
]
```
输出：7
路径：1 → 3 → 1 → 1 → 1，总和7

> 分析：每一步只能来自上方或者左边。当前位置最小路径和 = min(上方, 左边) + 当前格子的值。典型二维DP。

## 💡 解题思路
### 状态定义
`dp[i][j]`：走到坐标 `(i,j)` 的最小路径和。

### Base Case 初始化
1. **第一行（i=0，只能从左边过来，不能向上）**
`dp[0][j] = dp[0][j-1] + grid[0][j]`
2. **第一列（j=0，只能从上面过来，不能向左）**
`dp[i][0] = dp[i-1][0] + grid[i][0]`

### 状态转移方程
i>0 并且 j>0：
$$dp[i][j] = \min(dp[i-1][j], dp[i][j-1]) + grid[i][j]$$
- `dp[i-1][j]`：从**上方**走到(i,j)
- `dp[i][j-1]`：从**左方**走到(i,j)

> 优化：可以**原地修改grid**，不需要额外开辟dp数组，空间从\(O(mn)\)降到\(O(1)\)；也可以压缩为一维数组\(O(n)\)。

## ⚙️ 复杂度分析
二维DP版本
- **时间复杂度**：$\(O(mn)\)$，遍历整个网格
- **空间复杂度**：$\(O(mn)\)$，dp数组；原地修改为$\(O(1)\)$

一维空间压缩版本
- **时间复杂度**：$\(O(mn)\)$
- **空间复杂度**：$\(O(n)\)$

## 💻 AC 代码
### 版本1：原地修改grid（面试推荐，空间最优）
```python
class Solution:
    def minPathSum(self, grid: list[list[int]]) -> int:
        m = len(grid)
        n = len(grid[0])
        
        # 初始化第一行
        for j in range(1, n):
            grid[0][j] += grid[0][j-1]
        # 初始化第一列
        for i in range(1, m):
            grid[i][0] += grid[i-1][0]
        
        # 填充其余格子
        for i in range(1, m):
            for j in range(1, n):
                grid[i][j] += min(grid[i-1][j], grid[i][j-1])
        return grid[m-1][n-1]
```

### 版本2：一维DP空间压缩（不修改原数组）
```python
class Solution:
    def minPathSum(self, grid: list[list[int]]) -> int:
        m = len(grid)
        n = len(grid[0])
        dp = [0]*n
        dp[0] = grid[0][0]
        # 初始化第一行
        for j in range(1,n):
            dp[j] = dp[j-1] + grid[0][j]
        
        for i in range(1,m):
            dp[0] = dp[0] + grid[i][0] # 更新当前行第一个（只能从上边来）
            for j in range(1,n):
                dp[j] = min(dp[j], dp[j-1]) + grid[i][j]
        return dp[-1]
```

## 🧩 关键知识点 & 技巧
1. 二维网格DP经典模板：只能右/下移动；
2. 一维压缩原理：dp[j]保存上一行j位置的值，dp[j-1]是当前行左边的值；
3. 只能向右向下，没有回头路，不需要BFS；BFS适合求步数，DP适合求路径代价。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 忘记单独初始化第一行/第一列，直接进入双重循环；
2. 转移方程写反：`max`代替`min`；
3. 边界：grid只有一行 或者 只有一列的情况；
4. 想用DFS暴力搜索，网格一大直接超时。

**避坑要点**
> 第一行只能从左来，第一列只能从上来，必须先预处理。

## ✅ 总结
64是二维DP基础题。核心：每个点只能来自上方或左方，取最小值加上当前格子数值。可以原地修改或者一维数组压缩空间。
关联题目：62不同路径、63不同路径II（带障碍物）
