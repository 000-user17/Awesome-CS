# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：394
- **题目名称**：字符串解码
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/decode-string/
- **刷题日期**：2026\08\18
- **标签**：栈 / 字符串 / DFS递归

## 📝 题目大意
给定编码字符串，编码格式 `k[encoded_string]`，代表括号内字符串重复 k 次。
支持多层嵌套 `3[a2[bc]]`，输入保证括号合法、数字仅代表重复次数。
要求输出完整展开后的字符串。

> 难点：多层括号嵌套，内层字符串必须优先解码，再参与外层重复。
> 两种主流思路：**双栈迭代**、**区间DFS递归**。

## 💡 解题思路
### 思路一：双栈迭代（面试首选，推荐背诵）
**核心思想**
利用栈保存「遇到左括号前的上下文」。
- `num_stack`：存放重复次数k
- `str_stack`：存放左括号之前已经拼接完成的字符串

遍历字符分为四类：
1. **数字**：连续数字累加，支持多位数（10、100）
2. **字母**：直接追加到当前临时字符串
3. **左括号 `[`**
   - 将当前累计数字、当前字符串压入对应栈
   - 清空临时变量，准备收集括号内部内容
4. **右括号 `]`**
   - 弹出重复倍数k、弹出外层前缀字符串
   - 新字符串 = 前缀字符串 + 当前括号内字符串 * k
   - 赋值给临时字符串，继续向后解析

> 示例推演 `3[a2[bc]]`
读到 `[` 存档；读到内层 `]` 先展开 `bc*2`；读到外层 `]` 再整体乘3。

### 思路二：区间DFS递归（你手写版本）
**核心思想**
定义 `dfs(i,j)`：解析区间 `s[i...j]` 并返回解码字符串。
1. 先收集区间开头连续字母；
2. 收集连续数字k；
3. 找到 `[` 对应的匹配 `]`（括号计数器）；
4. 递归解析括号内部 `dfs(i+1,m-1)`，重复k次；
5. 递归解析括号右侧剩余字符串 `dfs(m+1,j)`；
6. 三段拼接：前缀字母 + k×内层字符串 + 右侧字符串。

> 优缺点对比
- 双栈：逻辑直观，一遍遍历，常数效率更好，面试官更偏好
- 区间DFS：代码较长，需要手动匹配括号，适合锻炼字符串区间处理能力

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(N)\)$，N为输出字符串长度，每个字符只会生成一次
- **空间复杂度**：$\(O(N)\)$，栈/递归调用栈保存中间字符串

## 💻 AC 代码
### 解法1：双栈迭代
```python
from typing import List
class Solution:
    def decodeString(self, s: str) -> str:
        num_stack = []
        str_stack = []
        cur_num = 0
        cur_str = ""
        for c in s:
            if c.isdigit():
                cur_num = cur_num * 10 + int(c)
            elif c == "[":
                num_stack.append(cur_num)
                str_stack.append(cur_str)
                cur_num = 0
                cur_str = ""
            elif c == ']':
                num = num_stack.pop()
                prev = str_stack.pop()
                cur_str = prev + num * cur_str
            else:
                cur_str += c
        return cur_str
```

### 解法2：区间DFS递归
```python
class Solution:
    def decodeString(self, s: str) -> str:
        lens = len(s)
        def dfs(i, j):
            if i >= lens or i > j:
                return ""
            if i == j:
                return s[i]
            cur_num = 0
            cur_str = ""
            # 收集开头字母
            while i <= j and s[i].isalpha():
                cur_str += s[i]
                i += 1
            # 收集连续数字
            while i <= j and s[i].isdigit():
                cur_num = cur_num * 10 + int(s[i])
                i += 1

            if i > j:
                return cur_str
            m = i
            left = 0
            if s[m] == '[':
                left = 1
                while left > 0:
                    m += 1
                    if s[m] == '[':
                        left += 1
                    if s[m] == ']':
                        left -= 1
            # 拼接：前缀字母 + 内层重复 + 右侧剩余字符串
            cur_str = cur_str + cur_num * dfs(i + 1, m - 1) + dfs(m + 1, j)
            return cur_str
        return dfs(0, lens - 1)
```

## 🧩 关键知识点 & 技巧
1. **嵌套结构通用处理手段**：栈（迭代）或递归；
2. **多位数处理公式**：`num = num * 10 + int(ch)`，不要只处理单个数字；
3. 双栈核心规则：`[` 存档，`]` 出栈合并；
4. 区间递归关键点：利用括号计数器寻找匹配的右括号，避免错误截断内层嵌套；
5. Python字符串乘法 `str * k` 可以直接完成重复拼接。

## ❌ 错题反思 / 踩坑记录
### 高频错误
1. 只处理单个数字，无法处理 `12[abc]` 这类多位数；
2. 嵌套场景丢失外层前缀字符串，只返回括号内重复内容；
3. 递归写法寻找匹配右括号时，忽略内层嵌套 `[ ]`，定位 `]` 下标出错；
4. 遇到 `[` 忘记清空临时数字、临时字符串；
5. 边界判断遗漏，区间越界造成索引报错。

### 避坑要点
- 凡是存在括号嵌套，优先思考栈；
- 递归区间解法必须手动维护括号平衡计数器；
- 每次遇到 `]` 合并顺序：**外层前缀 + 当前字符串×倍数**，顺序不能颠倒。

## ✅ 总结
字符串解码是栈经典入门题。
双栈迭代代码简洁、执行高效，面试优先掌握；区间DFS适合训练字符串区间划分、括号匹配能力。
遇到嵌套表达式（括号、四则运算），优先想到栈/递归两种实现路线。
