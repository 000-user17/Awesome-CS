# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：142
- **题目名称**：环形链表 II Linked List Cycle II
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/linked-list-cycle-ii/
- **刷题日期**：2026\08\13
- **标签**：链表 / 快慢指针 / 数学推导

## 📝 题目大意
给定一个链表的头节点 `head`，返回链表开始入环的第一个节点。如果链表无环，则返回 `null`。
**不允许修改链表**。
进阶要求：尝试使用 $\(O(1)\)$ 空间复杂度实现。

> 分析：
> 方案1：哈希集合，遍历链表记录访问过的节点，重复节点即为环入口；时间 $\(O(n)\)$，空间 $\(O(n)\)$，不满足进阶。
> 方案2：**快慢指针（Floyd判圈算法）**，仅使用常数指针，空间 $\(O(1)\)$，最优解法。
> 分为两步：
> 1. 快慢指针寻找环内相遇点，证明链表存在环；
> 2. 利用数学推论，双指针同速移动找到环入口。

## 💡 解题思路
### 核心原理与数学推导
设：
- $a$：链表头节点 → 环入口节点距离
- $b$：环入口 → 快慢指针相遇点距离
- $c$：相遇点 → 环入口剩余距离
环总长 $L = b + c$

慢指针每次走1步，快指针每次走2步。相遇时：
慢指针路程：$S_{slow}=a+b$
快指针路程：$S_{fast}=a+b+kL$（$k$ 为快指针在环内多跑圈数）
速度关系：$S_{fast}=2\cdot S_{slow}$
$$
\(2(a+b)=a+b+k(b+c)\)
$$
化简得到：
$$
a = (k-1)(b+c)+c
$$
**结论**：
头节点到入口距离 = 相遇点走到入口距离 + 整数圈环长。
因此：**一个指针从 head 出发，一个指针从相遇点出发，每次同步走1步，相遇点就是环入口**。

### 执行流程
1. 快慢指针 `slow`、`fast` 均从 head 出发，slow走1步，fast走2步；
2. 如果 fast 或 fast.next 为空，说明链表无环，直接返回 `None`；
3. 如果快慢指针相遇，证明存在环；
4. 新建指针 `first` 从 head 出发，`slow` 停留在相遇点；
5. `first` 与 `slow` 每次同步前进1步，两者相遇节点即为环入口。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$
  快慢指针相遇最多遍历一次链表；寻找入口最多再遍历一次链表。
- **空间复杂度**：$\(O(1)\)$
  仅使用有限指针变量，无额外容器，满足进阶要求。

## 💻 AC 代码（你手写版本）
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if not head or not head.next:
            return None
        
        slow, fast = head, head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if slow == fast:
                break
        # 退出循环后判断是否是无环导致退出
        if not fast or not fast.next:
            return None
        first = head
        while first != slow:
            first = first.next
            slow = slow.next
        return first
```

## 🧩 关键知识点 & 技巧
1. Floyd判圈算法分为「判环」和「寻找环入口」两阶段，面试常追问数学推导；
2. 快慢指针**初始必须同时从 head 出发**，不要写成 fast=head.next，容易破坏入口推导；
3. 相遇后两个指针**步长必须都为1**，不能继续快慢步长；
4. 循环结束后必须二次校验：区分「无环退出」和「相遇退出」；
5. 本题前置题：141.环形链表（只需要判断有无环，不需要找入口）。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 快慢指针初始化位置写错，`fast=head.next`，导致推导失效；
2. 找到相遇点后，忘记判断无环场景，直接进入第二阶段循环，出现死循环；
3. 第二阶段依旧保持一快一慢的步长，无法在入口相遇；
4. 边界漏判：空链表、单节点链表直接返回 None；
5. 只背代码不理解公式，面试官追问原理无法解释。

**避坑要点**
- 两层循环分工清晰：第一层找相遇点；第二层找入口；
- 退出第一层 while 后一定要校验 `fast` 与 `fast.next`，区分两种退出条件；
- 牢记推论：相遇后双指针同速前进，相遇点=环入口。

## ✅ 总结
快慢指针是链表经典技巧，常用于：寻找链表中点、判环、寻找环入口。
本题难点不在代码，而在于数学推导。面试不仅要能写出代码，还要能口述距离公式，解释为什么两个指针同速走一定会在环入口相遇。
