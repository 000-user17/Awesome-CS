# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：297
- **题目名称**：二叉树的序列化与反序列化 Serialize and Deserialize Binary Tree
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/serialize-and-deserialize-binary-tree/
- **刷题日期**：2026\08\20
- **标签**：二叉树 / DFS / 递归 / 设计题

## 📝 题目大意
序列化：将二叉树转化为字符串；反序列化：将字符串还原为原始二叉树。
> 重点难点：普通前序遍历只保存节点数值，丢失空节点信息，无法复原二叉树；**必须记录空节点标记**；序列化、反序列化遍历顺序必须完全保持一致。
> 示例树：
```
    1
   / \
  2   3
     / \
    4   5
```
序列化输出字符串：`"1,2,#,#,3,4,#,#,5,#,#"`

## 💡 解题思路
### 思路：前序DFS（根‑左‑右）
1. **serialize 序列化**
递归前序遍历二叉树：
- 当前节点不为空：拼接节点数值+逗号；
- 当前节点为空：拼接空标记`#`+逗号；
最后切片去掉末尾多余逗号，返回字符串。

2. **deserialize 反序列化**
1）将字符串按逗号分割得到字符串数组；
2）维护成员变量`self.idx`作为全局指针，递归构建二叉树；
3）读取当前`arr[self.idx]`，指针向后移动；
   - 如果是`#`，返回`None`；
   - 如果是数字，新建TreeNode；递归构建左子树，递归构建右子树；返回当前节点。

> 关键约束：序列化与反序列化遍历顺序必须完全一致；反序列化的索引必须全局共享，递归过程持续向后移动。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，每个节点访问一次
- **空间复杂度**：$\(O(n)\)$，存储序列化字符串；递归栈最坏$\(O(n)\)$（斜二叉树）

## 💻 AC 代码
> 你的原始代码存在bug：`preorder`函数逻辑错误：`if root`执行完拼接之后，没有else分支直接继续递归，**root为None的时候直接执行递归，会触发None.left报错**。下面给出修正后的完整可提交代码。
```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None

class Codec:
    def preorder(self, root):
        if not root:
            self.data += '#,'
            return
        self.data += str(root.val) + ','
        self.preorder(root.left)
        self.preorder(root.right)

    def serialize(self, root):
        """Encodes a tree to a single string.
        :type root: TreeNode
        :rtype: str
        """
        self.data = ""
        self.preorder(root)
        return self.data[:-1]


    def deserialize(self, data):
        """Decodes your encoded data to tree.
        :type data: str
        :rtype: TreeNode
        """
        arr = data.split(',')
        self.idx = 0

        def build():
            s = arr[self.idx]
            self.idx += 1
            if s == "#":
                return None
            node = TreeNode(int(s))
            node.left = build()
            node.right = build()
            return node
        return build()

# Your Codec object will be instantiated and called as such:
# ser = Codec()
# deser = Codec()
# ans = deser.deserialize(ser.serialize(root))
```

## 🧩 关键知识点 & 技巧
1. 只保存节点值而不记录空节点`#`，信息丢失，无法复原二叉树；
2. 序列化与反序列化遍历顺序必须完全相同（全部前序）；
3. 反序列化递归，索引需要全局共享，不能每次递归从0开始；
4. 使用逗号`,`分割节点，避免多位数数字解析错误；
5. 字符串不可变，拼接字符串性能一般；工程上优先使用list append再join；
6. `s[:-1]`切片去除末尾多余逗号，空字符串切片不会抛异常。

## ❌ 错题反思 / 踩坑记录
**你的原始代码bug**
1. preorder逻辑错误：`if root`里面做拼接，else只处理空节点拼接，但是if分支结束后，没有阻止继续执行递归；当`root=None`时会执行`root.left`，直接触发`AttributeError: 'NoneType' object has no attribute 'left'`。
> 错误片段
```python
if root:
    ...
else:
    ...
return
# 下面两行无论root是否为None都会执行！
self.preorder(root.left, self.data)
self.preorder(root.right, self.data)
```

**其他高频错误**
1. deserialize写`return build`，缺少括号，返回函数对象，LeetCode报`Type <class 'function'>: Not implemented`；
2. 忘记逗号分隔，直接拼接数字，例如`12`无法区分是数字12，还是`1`和`2`；
3. 反序列化时，局部变量idx，递归调用不会修改上层；需要使用`self.idx`；
4. 没有切掉末尾逗号，split之后数组最后多出空字符串，解析出错。

**避坑要点**
- preorder函数，空节点分支必须return，阻止继续访问`.left .right`；
- 内部递归函数调用一定要加括号`build()`执行，不要直接返回函数名；
- 空节点标记`#`不能省略，是复原二叉树的核心信息。

## ✅ 总结
二叉树序列化反序列化有两种主流实现：前序DFS、BFS层序遍历。
面试优先写**前序DFS版本**，代码简短清晰。核心本质：**必须保存空节点标记，序列化、反序列化遍历顺序严格一致**。
> 注意：中序+前序遍历方案不能处理树中有重复val的场景，本题不推荐。
