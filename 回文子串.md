# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：647
- **题目名称**：回文子串 Palindromic Substrings
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/palindromic-substrings/
- **刷题日期**：2026\08\14
- **标签**：字符串 / 中心扩散法 / 双指针

## 📝 题目大意
给一个字符串，统计字符串中**所有回文子串的数量**。
子串要求连续；不同起始、终止位置就算不同子串，哪怕内容相同。

> 主流思路：
> 方案1：**中心扩散法（你当前代码实现）**
> 回文存在两种形式：奇数长度、偶数长度，分别以中心点向外扩散；
> 方案2：动态规划 DP；
> 方案3：Manacher（马拉车算法，进阶优化，面试一般不要求）。

## 💡 解题思路
回文特性：存在对称中心。
1. **奇数长度回文**：中心是单个字符 `[c]`
   扩散起点：`i = c, j = c`
2. **偶数长度回文**：中心是两个相邻字符 `[c-1, c]`
   扩散起点：`i = c-1, j = c`

### 中心扩散函数逻辑
`CentralDiffusion(i, j)`：
以 `i,j` 为初始左右边界，不断向左右扩展：
- 只要 `s[i] == s[j]`，说明构成回文，计数+1；
- 不等则停止扩散；
- `i -= 1，j += 1` 继续向外扩张。

遍历字符串每一个位置 `c`，分别执行奇数、偶数两种中心扩散，累加所有回文数量。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n^2)$
  一共 $2n$ 个扩散中心，每个中心最多向外扩散 $\(O(n)\)$ 次
- **空间复杂度**：$\(O(1)\)$
  仅使用局部变量，无额外数组（递归版中心扩散会产生栈开销，当前迭代扩散最优）

## 💻 AC 代码
```python
class Solution:
    def countSubstrings(self, s: str) -> int:
        n = len(s)
        cnt = 0
        
        def CentralDiffusion(i, j):
            num = 0
            while i >= 0 and j < n:
                if s[i] == s[j]:
                    num += 1
                else:
                    break
                i -= 1
                j += 1
            return num
        
        for c in range(n):
            # 奇数长度回文，中心为 c
            cnt += CentralDiffusion(c, c)
            # 偶数长度回文，中心为 c-1 和 c
            cnt += CentralDiffusion(c - 1, c)

        return cnt
```

## 🧩 关键知识点 & 技巧
1. 回文一定要区分**奇数、偶数两种长度**，漏掉任意一种会少统计；
2. 中心扩散不需要枚举所有子串，利用对称性大量剪枝；
3. 每次匹配成功立刻计数，不用记录子串，只统计数量；
4. 边界判断：`i >= 0`、`j < n`，防止下标越界。

## ❌ 错题反思 / 踩坑记录
1. 只写奇数长度扩散，忘记偶数长度回文，答案偏小；
2. 扩散循环内外 `i,j` 下标修改顺序写错，造成死循环或者越界；
3. 混淆「子序列」和「子串」，本题必须连续；
4. 扩散函数内部变量和外层全局计数变量重名，容易引发逻辑混乱。

## ✅ 总结
中心扩散是回文子串、最长回文子串（5题）最常用、最容易手写的算法。
核心要点：**一个中心点负责奇数回文，两个相邻中心点负责偶数回文**。

---
需要我顺带把 **DP版本解法** 也补充进这份笔记作为拓展吗？
