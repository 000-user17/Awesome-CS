# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：34
- **题目名称**：在排序数组中查找元素的第一个和最后一个位置
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/
- **刷题日期**：2026\09\02
- **标签**：数组 / 二分查找

## 📝 题目大意
给你一个按照**非递减顺序排列**的整数数组 `nums`，和一个目标值 `target`。请你找出给定目标值在数组中的开始位置和结束位置。
如果数组中不存在目标值 `target`，返回 `[-1, -1]`。
> 硬性要求：算法时间复杂度必须为 $O(\log n)$。

## 💡 解题思路
**核心思想：**使用**左闭右开区间**的二分查找，分别求目标元素的**左边界**与**右边界**。普通二分只能找到任意一个匹配位置，本题需要两次二分，一次找第一个出现位置，一次找最后一个出现位置。

**步骤拆解：**
1. 数组是有序非递减，$O(\log n)$ 复杂度只能使用二分，不能线性遍历。
2. `binarySearchLeft` 查找左边界：区间 `[0, n)`，条件 `nums[mid] >= target` 时收缩右边界，最终返回第一个大于等于 target 的下标。
3. `binarySearchRight` 查找右边界：区间 `[0, n)`，条件 `nums[mid] > target` 时收缩右边界，返回值需要减 1，得到最后一个等于 target 的下标。
4. 边界校验：得到 start、end 之后，不能直接使用。需要判断下标没有越界，并且该位置的值确实等于 target；不满足说明数组不存在 target，赋值为 `-1`。
5. 特殊前置判断：数组为空直接返回 `[-1,-1]`。

> 区间说明：代码采用**左闭右开 [l, r)**，`r = n`，循环条件 `while l < r`，和 `[l, r]` 闭区间二分写法有所区别。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(\log n)$，两次二分查找，每次二分时间为 $O(\log n)$。
- **空间复杂度**：$\(O(1)\)$，仅使用常数变量，没有额外开辟数组/递归栈。

## 💻 AC 代码（Python）
```python
from typing import List

class Solution:
    def searchRange(self, nums: List[int], target: int) -> List[int]:
        n = len(nums)
        left, right = 0, n
        if not nums:
            return [-1, -1]

        def binarySearchLeft(l, r):
            while l < r:
                mid = (l + r) // 2
                if nums[mid] >= target:
                    r = mid
                else:
                    l = mid + 1
            return l

        def binarySearchRight(l, r):
            while l < r:
                mid = (l + r) // 2
                if nums[mid] > target:
                    r = mid
                else:
                    l = mid + 1
            return l - 1

        res = []
        start = binarySearchLeft(0, n)
        end = binarySearchRight(0, n)

        if start < n and nums[start] == target:
            res.append(start)
        else:
            res.append(-1)

        if end < n and nums[end] == target:
            res.append(end)
        else:
            res.append(-1)

        return res
```

## 🧩 关键知识点 & 技巧
- 技巧1：左闭右开 `[l, r)` 二分模板。
  - 找左边界：`nums[mid] >= target` → `r = mid`；
  - 找右边界：`nums[mid] > target` → `r = mid`，结果需要 `l‑1`。
- 技巧2：二分返回的下标**不一定代表存在target**，必须二次校验下标合法性与数组值。比如 target 大于数组全部元素，左边界会返回 n，直接访问会越界。
- 易错点：
  1. 忘记校验返回下标是否越界，直接访问 `nums[start]`，造成索引越界报错。
  2. 混淆闭区间 `[l,r]` 和左闭右开 `[l,r)` 的模板，循环条件、mid 更新逻辑混用。
  3. 右边界函数忘记 `return l‑1`，直接返回 l，得到错误位置。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 直接把二分返回的 start、end 当做答案返回，没有校验该位置的值是否等于 target；当 target 不在数组中，会输出错误下标。
2. 区间模板混用，把左闭右开模板写成 `while l <= r`，死循环或者结果出错。

**修正思路：**
1. 二分只是找到“应该插入的位置”，不等于元素一定存在；必须判断下标范围 `start < n` 并且 `nums[start]==target`。
2. 固定一套二分模板，区分清楚闭区间、左闭右开两套写法，不要混写。

## ✅ 总结
本题是二分查找经典变形题，考察**二分边界**而不是简单找到元素。
1. 左边界：第一个 >= target 的位置；
2. 右边界：最后一个 <= target 的位置；
3. 二分得到的下标只是插入点，**一定要校验元素是否真实存在**。
有序数组 + $O(\log n)$ 时间，优先想到二分，不要直接遍历。

> 补充：两种二分模板
> - 闭区间 `[left, right]`：`while left <= right`
> - 左闭右开 `

[left, right)`：`while left < right`

---
