# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：399
- **题目名称**：除法求值
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/evaluate-division/
- **刷题日期**：2026\08\18
- **标签**：并查集 / 图 / 加权并查集

## 📝 题目大意
已知若干等式 $\dfrac{A_i}{B_i}=values[i]$，给出若干查询 $\dfrac{C_j}{D_j}=?$
1. 能推导出结果，返回数值；
2. 任意变量从未出现、或者两个变量不在同一个连通关系里，返回 `-1.0`。
> 本质：变量之间存在带传递性的除法关系，适合**加权并查集**。

> 前置科普：普通并查集能干什么？
普通并查集：只判断两个元素是否属于同一集合（连通），**没有附加信息**。
本题不仅要判断连通，还要维护「两个节点之间的比值」→ **加权（带权）并查集**。

## 💡 解题思路
### 核心定义（重中之重，不要记反！）
```
parent[x]：x 的父节点
weight[x] = x / parent[x]
```
含义：x 除以它的父节点等于 weight[x]。
自身指向自己时：`weight[x]=1.0`，也就是 $\(x/x=1\)$。

### 1. find() 函数两大作用
1. 找到当前节点的根节点；
2. **路径压缩**，让 x 直接连到根，同步更新 weight[x] = x/根。
推演：
链条 `x → p → root`
$weight[x]=\dfrac{x}{p},\quad weight[p]=\dfrac{p}{root}$
$\dfrac{x}{root}=\dfrac{x}{p} \times \dfrac{p}{root}=weight[x] \times weight[p]$
对应代码：
```python
p = parent[x]
parent[x] = find(parent[x])
weight[x] *= weight[p]
```

### 2. 合并 Union 公式推导
已知条件：$\dfrac{a}{b}=val$
执行 `find(a)`、`find(b)` 后：
$weight[a]=\dfrac{a}{rootA},\quad weight[b]=\dfrac{b}{rootB}$

我们令 `parent[rootA] = rootB`，需要求出 $weight[rootA]=\dfrac{rootA}{rootB}$
$$
\frac{a}{b}=val \implies \frac{a}{rootA} \cdot \frac{rootA}{rootB} \div \frac{b}{rootB}=val
$$
$$
weight[a] \times weight[rootA] \div weight[b] = val
$$
变形得到代码公式：
$$weight[rootA] = \frac{weight[b] \times val}{weight[a]}$$

### 3. 查询逻辑
查询 $\dfrac{c}{d}$
先执行 `find(c)`、`find(d)` 完成路径压缩
- 两个变量不在 parent 字典 → -1.0
- 根节点不一样（不连通）→ -1.0
- 根相同：
$weight[c]=\dfrac{c}{root},\quad weight[d]=\dfrac{d}{root}$
$$\frac{c}{d}=\dfrac{c/root}{d/root}=\frac{weight[c]}{weight[d]}$$

**步骤拆解**
1. 初始化两个字典 parent、weight，新变量默认父节点是自己，权重=1.0；
2. 遍历所有等式，对 a、b 执行并查集合并；
3. 遍历每个查询，按上面规则计算答案；

## ⚙️ 复杂度分析
- **时间复杂度**：$O((N+Q)\alpha(M))$
  N为等式数量，Q为查询数量，M是变量总数；$\alpha$ 是阿克曼函数反函数，几乎等于常数。
- **空间复杂度**：$\(O(M)\)$，存储所有变量的父节点与权重。

## 💻 AC 代码（你提供版本）
```python
from typing import List
class Solution:
    def calcEquation(self, equations: List[List[str]], values: List[float], queries: List[List[str]]) -> List[float]:
        parent = dict()
        weight = dict()

        def find(x):
            if x != parent[x]:
                p = parent[x]
                parent[x] = find(parent[x])
                weight[x] *= weight[p]
            return parent[x]

        for (a, b), val in zip(equations, values):
            if a not in parent:
                parent[a] = a
                weight[a] = 1.0
            if b not in parent:
                parent[b] = b
                weight[b] = 1.0
            
            rootA = find(a)
            rootB = find(b)
            if rootA != rootB:
                parent[rootA] = rootB
                weight[rootA] = weight[b] * val / weight[a]

        res = []
        for (a, b) in queries:
            if a not in parent or b not in parent:
                res.append(-1.0)
                continue
            rootA = find(a)
            rootB = find(b)
            if rootA != rootB:
                res.append(-1.0)
                continue
            res.append(weight[a] / weight[b])    

        return res
```

## 🧩 关键知识点 & 技巧
1. **区分普通并查集 vs 加权并查集**
普通并查集：只连通；
加权并查集：连通 + 维护节点之间的相对关系（距离、比值、偏移量）。
2. **权重定义是一切的源头**
一定要固定 `weight[x] = x / parent[x]`；定义一旦更换，所有公式全部改写。
3. find 递归顺序不能乱：
先保存原始父节点 `p=parent[x]`，递归完成后再更新权重，因为递归会修改父节点。
4. 所有查询前必须先调用 find()，触发路径压缩，保证 weight 是「节点到根」的比值。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. weight 定义写反（写成 parent[x]/x），所有计算结果全部颠倒；
2. find 函数不保存原始父节点，直接使用更新后的 parent[x]，权重计算错误；
3. 合并 rootA、rootB 的公式凭记忆乱写，不做数学推导；
4. 查询前忘记调用 find，节点没有路径压缩，weight 保存的不是到根的距离；
5. 没有判断变量是否存在于字典，直接调用find，触发KeyError。

**避坑要点**
- 所有公式全部从 `weight[x] = x / parent[x]` 代数推导，不要死记硬背；
- 只要用到weight，优先执行find完成路径压缩；

## ✅ 总结
除法求值是加权并查集经典模板题。
把变量当作节点，除法关系作为带权双向边，并查集维护连通性与节点相对比值。
同类题型：带偏移量的并查集（等式约束、分数关系、食物链）。

---

# 给小白通俗讲解：并查集（普通并查集 + 加权并查集）
## 一、普通并查集能干啥？
场景：一群元素，不断合并两组元素，频繁查询两个元素是不是一伙的。
例子：朋友圈、连通块、岛屿数量。
两个核心函数：
1. find(x)：找到x的老大（根节点），附带路径压缩
2. union(x,y)：把x所在集合和y所在集合合并

### 最简小白代码模板
```python
parent = {}
def find(x):
    if parent[x] != x:
        parent[x] = find(parent[x]) # 路径压缩，直接指向根
    return parent[x]

# 合并
def union(x,y):
    rx = find(x)
    ry = find(y)
    if rx != ry:
        parent[rx] = ry

# 查询是否同一组
def isConnect(x,y):
    return find(x) == find(y)
```
理解：
每个人有直属上级 parent[x]；
如果 parent[x] == x，说明这个人是老大。
路径压缩：向上寻找老大的路上，所有人直接挂靠老大，下次查找更快。

## 二、普通并查集的局限
只能知道「是不是一伙」，不知道**两者之间的关系**。
比如本题：
a和b一伙，b和c一伙，我们不仅要知道a和c连通，还要算出 a/c = ?
→ 引入：**加权并查集**

## 三、加权并查集思想
在 parent[x]（上级）之外，新增一个 weight[x]
代表：**x 和直属上级之间的相对关系**。
不同题目，weight含义不一样：
- 食物链：代表物种之间捕食关系
- 等式约束：代表差值
- 本题除法求值：代表 x/parent[x]

### 不变的规则
1. find 查找老大时，**顺带更新权重**，路径压缩后，上级直接变成根；
2. 合并两个集合时，利用已知条件算出两个根之间的权重关系；
3. 查询时，两个元素到根的权重互相运算，得到两者关系。

## 四、最容易卡住的思维误区
1. ❌ 不要想象 parent 是固定不变的
路径压缩会不断修改父节点，所以 weight 必须同步刷新；
2. ❌ 不要死背公式，一定要从你自己定义的weight关系式数学推导；
3. ❌ 不要跳过find直接使用weight
只有执行find之后，weight[x] 才代表 x 到根的关系。

## 五、学习路线建议
1. 先吃透普通并查集：连通性、路径压缩；
2. 做一道简单题：LeetCode 200岛屿数量 / 990等式方程的可满足性；
3. 再接触加权并查集；
4. 除法求值是加权并查集入门标杆，把样例手动模拟一遍find流程，立刻通透。
