# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：114
- **题目名称**：二叉树展开为链表 Flatten Binary Tree to Linked List
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/flatten-binary-tree-to-linked-list/
- **刷题日期**：2026\09\04
- **标签**：二叉树 / 前序遍历 / 递归 / 迭代

## 📝 题目大意
给二叉树根节点 `root`，把树**原地展开成单链表**：
1. 使用原来的`TreeNode`节点，**right当作链表next，left全部置为null**
2. 链表节点顺序 = 二叉树**前序遍历（根→左→右）**顺序
3. **原地修改，不能新建节点链表**

示例：
```
    1
   / \
  2   5
 / \   \
3   4   6
```
展开后：
`1 → 2 → 3 →4 →5 →6`，全部left=None，只用right串联。

>分析：
1. 直接前序遍历把节点存列表，再遍历列表改指针；简单，但需要额外\(O(n)\)数组存节点，不满足原地。
2. 递归解法：后序变形处理，先把左子树、右子树分别摊平；把摊平后的左链表接到根的右边，原来的右链表接在左链表尾部。
3. **原地最优思路（莫里斯变形）\(O(1)\)额外空间**：找左子树的最右节点，把原右子树挂到这个最右节点的right；再把左子树移到root.right，left置空，root往右走循环处理。

## 💡解题思路
### 思路1：递归（好理解，面试优先写）
递归函数作用：把一棵树flatten摊平为链表。
流程：
1. 如果root为空直接返回。
2. 递归摊平左子树 `flatten(root.left)`
3. 递归摊平右子树 `flatten(root.right)`
4. 保存原来的右子树：`temp_right = root.right`
5. 把摊平好的左子树放到root.right；`root.left = None`
6. 找到现在root右链表的末尾节点（一路while往右走）
7. 把原来保存的temp_right接到末尾节点的right。

>递归是后序逻辑：先把左右子树全部处理完，再处理当前根节点。

### 思路2：原地迭代（莫里斯思想，\(O(1)\)空间，无栈）
循环处理当前节点cur：
1. cur不为空：
   - 如果cur没有左孩子：cur = cur.right，直接往后走
   - 如果cur有左孩子：
     ① 找到左子树的**最右下角节点predecessor**
     ② `predecessor.right = cur.right`：把当前节点的右子树挂到左子树最右节点后面
     ③ `cur.right = cur.left`，cur.left = None，左子树移到右边，左置空
     ④ cur = cur.right，继续处理下一个节点

>核心图解上面例子：
cur=1，左存在，左子树最右是4；把5‑6挂到4.right；把2‑3‑4移到1.right；left置None；cur走到2。
cur=2，左存在，左子树最右是3；把4挂到3.right；3移到2.right；cur走到3。
cur=3，无左，cur=3.right=4；cur=4无左，cur=4.right=5；cur=5，左空，cur=6结束。

## ⚙️复杂度分析
- **递归版本**
时间：$\(O(n)\)$每个节点访问一次；
空间：$\(O(h)\)$，h树的高度，最坏退化成链表\(O(n)\)递归栈。

- **莫里斯原地迭代版本**
时间：$\(O(n)\)$；
空间：$\(O(1)\)$，不使用栈、不使用数组，原地修改。

## 💻 AC代码
### 版本1 递归写法，简洁好写
```python
# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def flatten(self, root: TreeNode) -> None:
        """
        Do not return anything, modify root in-place instead.
        """
        if not root:
            return
        # 先摊平左右子树
        self.flatten(root.left)
        self.flatten(root.right)

        temp_right = root.right
        # 左子树挪到右边，left置空
        root.right = root.left
        root.left = None

        # 找到当前右链表尾部
        p = root
        while p.right:
            p = p.right
        # 接上原来的右子树
        p.right = temp_right
```

### 版本2 原地迭代 \(O(1)\)空间（莫里斯）
```python
class Solution:
    def flatten(self, root: TreeNode) -> None:
        cur = root
        while cur:
            if cur.left:
                #找左子树最右节点
                pre = cur.left
                while pre.right:
                    pre = pre.right
                #原右子树接到左子树最右
                pre.right = cur.right
                #左子树移动到右侧，left清空
                cur.right = cur.left
                cur.left = None
            cur = cur.right
```

## 🧩关键知识点 &技巧
1. 要求**原地修改，void函数，不要返回新链表**，直接修改原树节点。
2. 顺序是**前序遍历顺序**：根、左、右。
3. 递归顺序：要先递归处理左、右子树，再处理当前节点；不能前序先处理根。
4. 递归处理完左子树之后，一定要遍历找到新右链表的尾部，再接旧右子树。
5. 莫里斯版本核心：**左子树最右节点充当临时桥接节点**，把旧右子树接过去，再把左移到右边。

## ❌错题反思 /踩坑记录
1. 递归顺序写反：先处理根，再递归左右，指针已经改动，递归的原始左右已经被破坏。
2. 移动左子树之后忘记把`root.left = None`，题目强制left必须全部为null。
3. 接旧右子树时，直接`root.right=temp_right`，而不是找到链表尾部再接，直接覆盖掉已经摊平的左链表。
4. 题目函数无返回值，不要return节点。
5. 莫里斯版本：找前驱节点`pre`的时候，循环条件`pre.right`，不要写成`pre`。

**避坑要点**
- 递归：先把子树全部摊平，再操作当前节点指针；
- 左子树移到右侧后，必须把left置空；
- 拼接旧右子树，要找到链表末尾，不能直接覆盖root.right。

## ✅总结
二叉树中等高频题。
两种主流解法：
1. 递归：先把左右子树摊平，左挪到右边，找到链表尾部接上原右子树；逻辑直观适合手写。
2. 莫里斯迭代：\(O(1)\)空间，利用左子树最右节点桥接，原地完成。
>注意：普通前序遍历存数组的做法可以AC，但额外\(O(n)\)空间，面试会被追问原地解法。
