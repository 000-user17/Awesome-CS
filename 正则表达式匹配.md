# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：10
- **题目名称**：正则表达式匹配
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/regular-expression-matching/
- **刷题日期**：2026\09\03
- **标签**：动态规划、字符串

## 📝 题目大意
实现正则匹配：
- `.` 匹配**任意单个字符**
- `*` 匹配**它前面字符零次或多次**
要求：**完整匹配整个字符串s，不是部分匹配**。

示例：
`s="aa", p="a*"` → true（a出现2次）
`s="ab", p=".*"` → true（.*代表任意字符任意次数）
`s="aab", p="c*a*b"` → true，c出现0次，a出现2次，b一次

> 注意：`*` 不能单独出现，一定依附前面一个字符。

## 💡 解题思路
**核心：二维动态规划 dp[i][j]**
`dp[i][j]`：`s` 的前 `i` 个字符，能否被 `p` 的前 `j` 个字符匹配。
> s下标：0~i‑1；p下标：0~j‑1；dp[0][0]代表空串匹配空模式，为True。

### 状态分两大类
1. **p[j‑1] 不是 `*`**（普通字符 or `.`）
需要当前字符匹配成功：`s[i‑1]==p[j‑1] or p[j‑1]=='.'`
`dp[i][j] = dp[i‑1][j‑1]`

2. **p[j‑1] 是 `*`**，`*`作用于 `p[j‑2]`，两种选择：
    - ① `*`匹配**0次**前面字符：直接舍弃 `x*` 这一段：`dp[i][j] = dp[i][j‑2]`
    - ② `*`匹配**至少1次**前面字符：当前s末尾字符要和`*`前面字符匹配，继承s前面结果：
      `dp[i][j] = dp[i‑1][j]`（条件：`s[i‑1]==p[j‑2] or p[j‑2]=='.'`）
> 两种情况只要一种成立，dp[i][j]=True。

### 初始化
- `dp[0][0]=True`：空s匹配空p。
- s为空，p不为空：处理 `x*`可以匹配0次。例如 `p="a*b*c*"`，`dp[0][j] = dp[0][j‑2]`。

### 填表顺序
i 从 0 ~ len(s)；j 从 0 ~ len(p)。
答案：`dp[len(s)][len(p)]`

> ❗ 不能用暴力递归，不记忆化会大量重复计算，容易超时。

## ⚙️ 复杂度分析
设 n = len(s), m = len(p)
- **时间复杂度**：$O(n*m)$，二维dp表，每个状态计算一次
- **空间复杂度**：$O(n*m)$；可以优化一维，但面试优先二维，可读性高。

## 💻 AC 代码（Python）
```python
from typing import List
class Solution:
    def isMatch(self, s: str, p: str) -> bool:
        n, m = len(s), len(p)
        # dp[i][j] s前i个，p前j个是否匹配
        dp = [[False]*(m+1) for _ in range(n+1)]
        dp[0][0] = True

        # s为空，p不为空初始化，处理 x* x*...
        for j in range(2, m+1, 2):
            if p[j-1] == '*':
                dp[0][j] = dp[0][j-2]

        for i in range(1, n+1):
            for j in range(1, m+1):
                if p[j-1] != '*':
                    # 普通字符或者 .
                    if s[i-1] == p[j-1] or p[j-1] == '.':
                        dp[i][j] = dp[i-1][j-1]
                else:
                    # p[j‑1]是*，作用 p[j‑2]
                    # 选择1：*匹配0次
                    dp[i][j] = dp[i][j-2]
                    # 选择2：*匹配>=1次，前提当前字符匹配*前面
                    if s[i-1] == p[j-2] or p[j-2] == '.':
                        dp[i][j] = dp[i][j] or dp[i-1][j]
        return dp[n][m]
```

## 🧩 关键知识点 & 技巧
- 技巧1：dp数组下标偏移：`dp[i][j]`对应`s[i‑1]`、`p[j‑1]`，方便处理空字符串i=0,j=0。
- 技巧2：`*`两种分支：**匹配0次**、**匹配多次**，二者取或。
- 技巧3：初始化 `dp[0][j]`：s是空串，`x*`可以消掉，所以隔两位判断`*`。
- 技巧4：`.`只能匹配单个字符；`*`修饰前一个字符，可以消掉前一个。

- 易错点：
  1. 混淆dp下标与原字符串下标，直接用`s[i]`而不是`s[i‑1]`，越界或者逻辑错误。
  2. 处理`*`忘记“匹配0次”分支；
  3. 把`*`当成可以匹配任意字符任意次数；`*`只作用于它前面那一个字符；`.*`才是任意字符任意次。
  4. 初始化漏掉s为空的case，例如`s="" p="a*"`本应返回True。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 直接模拟逐个遍历，`*`多种分支无法处理，很难写对；
2. dp下标没有偏移，i,j直接访问s[i],p[j]，空串处理混乱；
3. `*`只写匹配多次分支，漏掉匹配0次；
4. 误解语义：`*`不是独立符号，必须绑定前一个字符。

**修正思路：**
1. 本题是典型二维DP，不要硬模拟；
2. dp[i][j]代表前i、前j个字符；访问原字符串用i‑1,j‑1；
3. `*`分支：0次 `dp[i][j‑2]`；多次 `dp[i‑1][j]`，或运算。

## ✅ 总结
10 正则表达式匹配，困难DP。
> dp[i][j]：s前i、p前j是否匹配。
> 不是`*`：当前字符匹配，则继承`dp[i‑1][j‑1]`。
> 是`*`：两种选择：0次舍弃`x*`；多次则s回退一格；两者取或。
> 初始化处理s为空；返回`dp[len(s)][len(p)]`。

> 拓展：可以写记忆化递归（自顶向下DP），逻辑和迭代等价。
```python
# 记忆化递归版本（了解）
from functools import lru_cache
class Solution:
    def isMatch(self, s: str, p: str) -> bool:
        n,m = len(s),len(p)
        @lru_cache(maxsize=None)
        def dfs(i,j):
            if i >= n and j >= m:
                return True
            if j >= m:
                return False
            match = i < n and (s[i]==p[j] or p[j]=='.')
            if j+1 < m and p[j+1] == '*':
                # 0次 或者多次
                return dfs(i,j+2) or (match and dfs(i+1,j))
            if match:
                return dfs(i+1,j+1)
            return False
        return dfs(0,0)
```
