# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：136
- **题目名称**：只出现一次的数字 Single Number
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/single-number/
- **刷题日期**：2026\08\14
- **标签**：位运算 / 异或

## 📝 题目大意
给定一个**非空**整数数组 `nums`，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现一次的元素。
要求：线性时间复杂度，仅使用常量额外空间。

> 可行思路对比：
> 方案1：哈希集合/哈希表，空间 $\(O(n)\)$，不满足题目空间限制；
> 方案2：排序后相邻两两比对，时间 $O(n\log n)$，不满足线性时间；
> 方案3：**按位异或运算（最优解法）**，满足 $\(O(n)\)$ 时间、$\(O(1)\)$ 空间。

## 💡 解题思路
核心依靠异或三条基础性质：
1. $a \oplus a = 0$ 相同数字异或结果为0
2. $a \oplus 0 = a$ 任意数字与0异或等于自身
3. 异或满足交换律、结合律：$a \oplus b \oplus a = a \oplus a \oplus b = b$

算法逻辑：
初始化 `xor = 0`，遍历数组不断将变量与数组元素异或。
成对出现的数字相互抵消变成0，最终剩下的结果就是唯一只出现一次的数字。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，仅遍历一次数组
- **空间复杂度**：$\(O(1)\)$，只使用一个临时变量，满足题目要求

## 💻 AC 代码
```python
from typing import List
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        xor = 0
        for num in nums:
            xor ^= num
        return xor
```

## 🧩 关键知识点 & 技巧
1. 异或运算是处理「成对元素找唯一值」经典手段；
2. 不需要额外开辟容器，充分利用位运算特性节省空间；
3. 交换律意味着数组元素顺序不影响最终结果。

## ❌ 错题反思 / 踩坑记录
1. 第一反应优先使用哈希表，忽略题目常量空间硬性限制；
2. 想到排序解法，但排序无法达到线性时间要求；
3. 不熟悉位运算性质，很难第一时间想到异或方案。

## ✅ 总结
经典位运算入门题。
口诀记忆：**成对消除，找唯一值，优先异或**。
