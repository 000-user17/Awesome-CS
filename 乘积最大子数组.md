# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：152
- **题目名称**：乘积最大子数组
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/maximum-product-subarray/
- **刷题日期**：2026\08\12
- **标签**：动态规划 / 数组

## 📝 题目大意
给你一个整数数组 nums ，找出数组中乘积最大的**非空连续子数组**（子数组至少包含一个数字），返回对应乘积。
测试用例答案保证为32位整数。
> 重点难点：乘法存在「负负得正」，极小负数乘负数可以变成极大正数；
> 不能直接照搬【53.最大子数组和】只维护最大值的思路。

## 💡 解题思路
### 核心思想：动态规划，同时维护以当前位置结尾的最大、最小乘积
定义：
- `max_dp`：以 `nums[i]` 结尾的连续子数组最大乘积
- `min_dp`：以 `nums[i]` 结尾的连续子数组最小乘积

对于每个位置，存在三种选择：
1. 只取当前元素本身 `nums[i]`
2. 前一个位置最大乘积 × 当前元素
3. 前一个位置最小乘积 × 当前元素

$$
max\_dp = \max(nums[i], nums[i] \times max\_dp_{old},\; nums[i] \times min\_dp_{old})
$$
$$
min\_dp = \min(nums[i], nums[i] \times max\_dp_{old},\; nums[i] \times min\_dp_{old})
$$

⚠️ 关键：更新 `max_dp` 之后原值会被覆盖，必须先用临时变量保存旧的 `max_dp` 再计算 `min_dp`。
全局变量 `max_value` 持续记录遍历过程中出现过的最大值。

**步骤拆解**
1. 边界处理：数组为空直接返回0；
2. 初始化 `max_dp、min_dp、max_value` 均为数组第一个元素；
3. 从下标1开始遍历数组；
4. 临时变量保存旧max_dp，依次计算新max_dp、新min_dp；
5. 更新全局最大乘积；
6. 遍历结束返回 `max_value`。

示例推演：nums = [-2,3,-4]
初始：max_dp=-2，min_dp=-2，max_value=-2
i=1，num=3
temp_max=-2
max_dp = max(3, -2*3, -2*3) = 3
min_dp = min(3, -2*3, -2*3) = -6
max_value = max(-2,3)=3

i=2，num=-4
temp_max=3
max_dp = max(-4, 3*-4, -6*-4) = 24
min_dp = min(-4, 3*-4, -6*-4) = -12
max_value = max(3,24)=24
最终答案：24

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，仅一次线性遍历数组
- **空间复杂度**：$\(O(1)\)$，只用常数变量，没有开辟DP数组

## 💻 AC 代码（你提供版本）
```python
from typing import List
class Solution:
    def maxProduct(self, nums: List[int]) -> int:
        if not nums:
            return 0
        n = len(nums)

        max_dp = nums[0]
        min_dp = nums[0]
        max_value = max_dp
        for i in range(1, n):
            temp_max = max_dp
            max_dp = max(nums[i], nums[i] * max_dp, nums[i] * min_dp)
            min_dp = min(nums[i], nums[i] * min_dp, nums[i] * temp_max)
            max_value = max(max_value, max_dp)
        return max_value
```

## 🧩 关键知识点 & 技巧
1. 和最大子数组和（LeetCode53）区分：加法只需要维护最大值；乘法必须同时维护最大、最小值；
2. 必须使用临时变量 `temp_max` 保存更新前的max_dp，否则计算min_dp时使用的是新值，逻辑错误；
3. 每次都允许「舍弃前面子数组，从当前元素重新开始」，对应候选中的 `nums[i]`；
4. 全局最大值不能直接取最后一轮的max_dp，中间位置可能存在更大乘积，需要持续更新。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 只维护最大值，丢弃最小值，遇到负负得正场景直接算出错误答案；
2. 不使用临时变量，先更新max_dp，再计算min_dp，使用已经被覆盖的新数值；
3. 忘记候选 `nums[i]`，无法处理「前面乘积为负，不如单独选取当前数字」的场景；
4. 最后直接return max_dp，忽略遍历中途出现的最优解。

**避坑要点**
- 正负相乘会翻转大小关系，最小值随时可能变为最大值；
- 两个dp值的计算必须依赖**上一轮的原始状态**；
- 子数组要求连续，不能排序、不能自由挑选元素。

## ✅ 总结
本题是动态规划经典题型，属于最大子数组和的进阶变种。
核心矛盾来自乘法负数的特性，解决方案是同时保存历史最大、最小乘积。
代码采用空间优化写法，不需要DP数组，面试标准最优解法。
