# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：5
- **题目名称**：最长回文子串 Longest Palindromic Substring
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/longest-palindromic-substring/](https://leetcode.cn/problems/longest-palindromic-substring/)
- **刷题日期**：2026\09\04
- **标签**：字符串 / 双指针 / 中心扩展

## 📝 题目大意

给你一个字符串 `s`，找到 `s` 中最长的回文子串。
回文：正读与反读完全相同。**子串是连续字符片段，不是子序列**。

示例：
`s = "babad"`，输出：`"bab"` 或者 `"aba"`
`s = "cbbd"`，输出：`"bb"`

> 
> 分析：
> 
> 
> - 暴力枚举全部子串：枚举所有i,j，判断子串是否回文，时间复杂度 $O(n^3)$，大数据直接超时。
> - 回文分两类：奇数长度（单点为中心，如`aba`）；偶数长度（两字符缝隙为中心，如`bb`）。
> - **中心扩展法**：遍历字符串每一个位置，分别做奇数、偶数两种中心向外扩散，记录最大回文区间，面试手写首选。
> - DP可以做，但空间 $O(n^2)$；Manacher算法时间 $(O(n))$，实现复杂，一般不要求手写。

## 💡 解题思路

### 核心架构

1. 封装`expand(left, right)`扩散函数：给定初始左右指针，向两边扩展，直到越界或者字符不相等，返回合法回文的左右边界。
2. 遍历字符串每一个下标i：
   - 奇数回文：中心在i，`expand(i,i)`
   - 偶数回文：中心在i与i+1缝隙，`expand(i,i+1)`
3. 对比两次扩散得到回文长度，更新全局最优的起始位置、最大长度。
4. 最后通过起始位置+最大长度切片返回结果。

> 
> 关键细节：
> while循环退出时，left、right已经多向外走了一步；真实合法回文区间是 `[left+1, right‑1]`。

### 执行逻辑

**expand(left, right)**

1. 循环条件：left>=0，right<len(s)，并且`s[left]==s[right]`，则left左移，right右移。
2. 循环终止，返回 `left+1, right‑1`。

**主循环**

1. 边界预处理：字符串长度<=1直接返回自身。
2. 遍历每一个i，分别调用奇数、偶数扩展。
3. 比较回文子串长度，更新全局`start`起点、`max_len`最大长度。
4. python切片：`s[start : start + max_len]`返回答案。

## ⚙️ 复杂度分析

- **时间复杂度**：$O(n^2)$
一共n个中心点，每个中心点最多向外扩散n次。
- **空间复杂度**：$(O(1))$
只使用若干变量保存边界，没有额外开辟数组。

## 💻 AC代码（中心扩展）

```
class Solution:
    def longestPalindrome(self, s: str) -> str:
        n = len(s)
        if n <= 1:
            return s

        def expand(left: int, right: int):
            # 向外扩散，直到越界或者字符不等
            while left >= 0 and right < n and s[left] == s[right]:
                left -= 1
                right += 1
            # 退出循环已经多走一步，回文真实区间 left+1 ~ right‑1
            return left + 1, right - 1

        start = 0
        max_len = 1

        for i in range(n):
            # 奇数长度回文
            l1, r1 = expand(i, i)
            len1 = r1 - l1 + 1
            # 偶数长度回文
            l2, r2 = expand(i, i + 1)
            len2 = r2 - l2 + 1

            # 更新全局最优
            if len1 > max_len:
                max_len = len1
                start = l1
            if len2 > max_len:
                max_len = len2
                start = l2

        return s[start: start + max_len]
```

## 🧩 关键知识点 & 技巧

1. 回文两种形态：**奇数中心、偶数缝隙中心**，两个都要处理，容易漏掉偶数case，例如`cbbd`。
2. expand函数退出后，指针已经向外多偏移一格，不要直接拿left、right作为结果边界。
3. python切片左闭右开，`s[a:b]`取`[a,b‑1]`，所以截取`start : start+max_len`。
4. 区分**子串（连续）与子序列（可以不连续）**，本题是子串。
5. DP解法思路：`dp[i][j]`代表s[i…j]是否回文；转移：两端字符相等并且中间子串是回文；空间开销大。
6. Manacher马拉车可以做到$(O(n))$，竞赛向，面试一般只需要知道存在即可。

## ❌ 错题反思 / 踩坑记录

**高频错误**

1. expand返回直接使用left、right，没有+1、‑1，拿到错误区间，结果截断或者越界。
2. 只写奇数中心，漏掉偶数回文；输入`cbbd`输出`b`而不是`bb`。
3. 切片边界写错，`s[start:max_len]`，忘记`start+max_len`。
4. 初始`max_len`设0，单字符输入`s="a"`处理出错。
5. 循环i跑到n‑1，`expand(i,i+1)`会传入right=n；函数内部while有`right<n`判断，不会报错，无需特殊处理。

**避坑要点**

- expand函数返回一定要修正边界，这是本题最大坑；
- 奇数、偶数两种中心必须全部调用；
- 初始化max_len=1，兼容单字符输入；
- 不要混淆子串和子序列。

## ✅ 总结

字符串高频中等题，面试高频。
核心思想：利用回文对称特性，以每个位置作为中心双指针向外扩散。
时间$O(n^2)$，空间$(O(1))$，代码简洁适合手写。
注意两种回文形态、扩散结束后的边界修正。
