# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：17
- **题目名称**：电话号码的字母组合
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/letter-combinations-of-a-phone-number/
- **刷题日期**：2026\09\03
- **标签**：回溯、字符串、DFS

## 📝 题目大意
给定仅包含数字 `2‑9` 的字符串，按照电话键盘映射，返回所有可以组成的字母组合。顺序无要求。
键盘映射：
- 2 → abc
- 3 → def
- 4 → ghi
- 5 → jkl
- 6 → mno
- 7 → pqrs
- 8 → tuv
- 9 → wxyz

> 特殊边界：输入空字符串 `""`，直接返回空列表，不要返回 `[""]`。

## 💡 解题思路
**核心思想：回溯（深度优先DFS）**
本质是多轮枚举：每一位数字，选择它对应的其中一个字母，往下继续选下一位；全部数字选完，把这条结果收集。

1. 建立数字‑字母映射字典。
2. 回溯函数参数：`index` 当前处理到第几个数字；`path` 当前已经拼接的字符串。
3. 递归终止条件：`index == len(digits)`，代表所有数字处理完毕，把path加入结果集合，return。
4. 取出当前数字对应的全部字母，逐个遍历：
   - 将字母加到 path；
   - 递归处理下一位 `index+1`；
   - 回溯（撤销选择），继续尝试下一个字母。

> 举例 `"23"`
> 第一位2选a：第二位3分别选d/e/f → `"ad","ae","af"`
> 第一位2选b：第二位3分别选d/e/f → `"bd","be","bf"`
> 第一位2选c：第二位3分别选d/e/f → `"cd","ce","cf"`

步骤拆解：
1. 处理特殊情况：输入为空串直接返回`[]`。
2. 定义映射表。
3. 结果列表res保存全部答案。
4. 开启回溯，从index=0，path=""开始。
5. 递归结束返回res。

## ⚙️ 复杂度分析
假设每个数字最多4个字母，数字总长度为n
- **时间复杂度**：$O(3^m \times4^n)$，m是对应3个字母数字个数，n是对应4个字母数字个数，所有组合都要生成一次
- **空间复杂度**：$\(O(n)\)$，n为digits长度，递归栈深度等于字符串长度；res结果不计入算法额外空间。

## 💻 AC 代码（Python）
```python
from typing import List
class Solution:
    def letterCombinations(self, digits: str) -> List[str]:
        if not digits:
            return []
        phone = {
            "2":["a","b","c"],
            "3":["d","e","f"],
            "4":["g","h","i"],
            "5":["j","k","l"],
            "6":["m","n","o"],
            "7":["p","q","r","s"],
            "8":["t","u","v"],
            "9":["w","x","y","z"]
        }
        res = []
        
        def backtrack(index, path):
            # 全部数字处理完成，收集答案
            if index == len(digits):
                res.append(path)
                return
            num = digits[index]
            for char in phone[num]:
                # 选当前字符，往下递归
                backtrack(index + 1, path + char)
                # 字符串不可变，path+char生成新字符串，自动完成回溯，不需要手动pop
        
        backtrack(0, "")
        return res
```
> Python字符串是不可变类型：`path+char`生成新字符串，上层path不会被修改，**不用手动pop撤销**；
> 如果用list存path，则递归后需要 `path.pop()`。

list版本回溯写法（更标准的回溯模板）：
```python
def backtrack(index, path):
    if index == len(digits):
        res.append("".join(path))
        return
    num = digits[index]
    for char in phone[num]:
        path.append(char)
        backtrack(index+1, path)
        path.pop() # 回溯撤销
```

## 🧩 关键知识点 & 技巧
- 技巧1：回溯DFS模板：选择 →递归 →撤销选择。
- 技巧2：Python字符串不可变，拼接不需要pop；列表path需要append + pop。
- 技巧3：边界判断输入空串返回`[]`，很多人错返回`[""]`。
- 技巧4：index代表当前正在处理digits的第几位，终止条件`index == len(digits)`。

- 易错点：
  1. 输入`digits=""`，忘记特殊判断，返回`[""]`；
  2. 终止条件写错，index超过长度还继续递归；
  3. 使用list做path，递归完忘记pop，结果出现错误拼接；
  4. 映射字典7、9不要写错，是4个字母。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 未处理空输入，直接执行回溯，得到`[""]`错误结果；
2. 列表path递归后没有pop，状态没有回退；
3. 7和9的字母少写一个，映射表出错；
4. index含义搞混，递归传入index而不是index+1，死循环。

**修正思路：**
1. 开头判断if not digits return []；
2. list版本必须 append后 pop；字符串拼接版本不用；
3. index+1进入下一层递归，不要传原index。

## ✅ 总结
17 电话号码字母组合，回溯入门模板题。
> 按位DFS枚举每一位所有可选字母；index控制处理到第几位；到达字符串长度收集结果。
> 记住边界空输入返回空列表。
> 拓展：子集、组合、全排列，都是同一套回溯思想。
