# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：104
- **题目名称**：二叉树的最大深度
- **题目难度**：简单
- **题目链接**：[https://leetcode.cn/problems/maximum-depth-of-binary-tree/](https://leetcode.cn/problems/maximum-depth-of-binary-tree/)
- **刷题日期**：2026\09\08
- **标签**：二叉树 / DFS递归 / BFS层序遍历

## 📝 题目大意
给定一个二叉树 `root`，返回其最大深度。
二叉树的**最大深度**是指从根节点到最远叶子节点的最长路径上的节点数。
叶子节点是没有左右孩子的节点。

## 💡 解题思路
### 方法一：DFS递归（最简洁）
**核心思想：**一棵二叉树的最大深度 = max(左子树最大深度，右子树最大深度) + 1。+1代表当前根节点本身。

**步骤拆解：**
1. 递归边界：如果当前节点为空，深度为 0，返回0。
2. 求左子树深度：递归调用左孩子。
3. 求右子树深度：递归调用右孩子。
4. 当前节点深度 = max(左深度, 右深度) + 1。
5. 返回该深度。

### 方法二：BFS层序遍历（迭代，一层一层统计）
**核心思想：**层序遍历，每遍历完整一层，深度+1；最后得到总深度。
1. 队列保存每一层的节点；root不为空入队。
2. depth初始为0。
3. 队列不为空：获取当前层节点数量，循环把本层所有节点出队，把非空左右孩子入队；处理完一层 depth += 1。
4. 返回depth。

### 方法三：栈DFS迭代
用栈保存`(node, 当前深度)`，遍历所有节点，记录遇到的最大深度。

## ⚙️ 复杂度分析
- **递归DFS**
    - **时间复杂度**：$\(O(n)\)$，n为节点总数，每个节点访问一次。
    - **空间复杂度**：$\(O(h)\)$，h为树高度；最坏链表 $\(O(n)\)$；平衡树 $O(\log n)$。
- **BFS层序**
    - **时间复杂度**：$\(O(n)\)$
    - **空间复杂度**：$\(O(n)\)$，最坏一层存全部节点。

## 💻 AC 代码（Python）
### 版本1：递归DFS（面试首选）
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def maxDepth(self, root: TreeNode) -> int:
        if not root:
            return 0
        left_depth = self.maxDepth(root.left)
        right_depth = self.maxDepth(root.right)
        return max(left_depth, right_depth) + 1
```

### 版本2：BFS层序遍历
```python
from collections import deque
class Solution:
    def maxDepth(self, root: TreeNode) -> int:
        if not root:
            return 0
        q = deque([root])
        depth = 0
        while q:
            level_size = len(q)
            for _ in range(level_size):
                node = q.popleft()
                if node.left:
                    q.append(node.left)
                if node.right:
                    q.append(node.right)
            depth += 1
        return depth
```

### 版本3：栈迭代DFS
```python
class Solution:
    def maxDepth(self, root: TreeNode) -> int:
        if not root:
            return 0
        stack = [(root, 1)]
        max_d = 1
        while stack:
            node, d = stack.pop()
            max_d = max(max_d, d)
            if node.right:
                stack.append((node.right, d+1))
            if node.left:
                stack.append((node.left, d+1))
        return max_d
```

## 🧩 关键知识点 \& 技巧
- 技巧1：递归公式：`max(left_depth, right_depth) + 1`；空节点返回0。
- 技巧2：BFS层序，`len(q)`代表当前层节点数目，一定要提前保存`level_size`，不能循环内取len(q)。
- 技巧3：迭代DFS栈存储`(节点，当前深度)`，实时更新全局最大深度。
- 易错点：空树`root=None`最大深度是0，不是1。

## ❌ 错题反思 / 踩坑记录
**错误1：空节点返回1**
错误原因：混淆深度定义；空树没有节点，深度为0。
修正思路：`if not root: return 0`。

**错误2：忘记 +1**
错误原因：只取左右子树max，漏掉当前节点本身计数。
修正思路：`max(left, right) + 1`。

**错误3：BFS里面for循环直接用`range(len(q))`，不提前存level_size**
错误原因：循环中不断往队列加元素，`len(q)`会动态变大，不能代表本层节点数。
修正思路：进入while循环，先`level_size = len(q)`。

## ✅ 总结
1. 递归写法最简洁，后序的思想：先拿到左右子树结果，再计算当前节点结果。
2. BFS层序遍历按层计数，适合求树的深度、层数、层序输出。
3. 迭代DFS用栈保存节点+当前深度，记录遍历过程中的最大值。
4. 区分：最大深度是**节点数目**；有些题目定义为边数，结果会差1，读题要仔细。
