# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：21
- **题目名称**：合并两个有序链表
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/merge-two-sorted-lists/
- **刷题日期**：2026\09\03
- **标签**：链表、双指针

## 📝 题目大意
将两个已经升序排列的链表 `list1`、`list2`，合并成一个全新的升序链表并返回。直接拼接原有链表节点，**不要新建节点**。
> 链表可以为空。

## 💡 解题思路
**核心思想：虚拟头节点(dummy哨兵) + 双指针遍历**
1. 创建虚拟头结点 dummy，用来规避处理头节点为空的边界；tail指针指向结果链表的尾部。
2. 两个指针分别指向两个链表头部，循环比较两个指针节点的值：
   - 取值更小的节点接在 tail 的后面；
   - 被取走节点的链表向后移动；tail也向后走一步。
3. 当其中一条链表遍历完毕，直接把另一条链表剩下的全部节点接在 tail后面（链表本身有序，不需要继续比较）。
4. 返回 `dummy.next`，就是合并后链表真正的头。

> 为什么用dummy：不用单独判断 list1、list2谁是真正的头节点，统一处理所有节点。

### 步骤拆解
1. 建立虚拟哨兵节点 `dummy`，`tail = dummy`；
2. while循环条件：`list1 不为空 and list2 不为空`
   - 如果 `list1.val < list2.val`：tail.next = list1，list1 = list1.next
   - 否则：tail.next = list2，list2 = list2.next
   - tail = tail.next
3. 循环结束，至少一个链表为空，把剩下非空链表直接接到tail后面：`tail.next = list1 if list1 else list2`
4. return dummy.next

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(m+n)\)$，m、n分别是两个链表长度，每个节点访问一次
- **空间复杂度**：$\(O(1)\)$，只使用几个指针，没有开辟新链表节点

## 💻 AC 代码（Python）
```python
# Definition for singly‑linked list.
from typing import Optional
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(-1)
        tail = dummy
        while list1 and list2:
            if list1.val < list2.val:
                tail.next = list1
                list1 = list1.next
            else:
                tail.next = list2
                list2 = list2.next
            tail = tail.next
        # 接上剩余部分
        tail.next = list1 if list1 else list2
        return dummy.next
```

## 🧩 关键知识点 & 技巧
- 技巧1：**虚拟头节点 dummy**，链表合并类题目高频技巧，省去对头节点的特殊判断。
- 技巧2：循环只处理两个链表都不为空的情况；剩余链表直接整体拼接，不需要逐个循环。
- 技巧3：原地复用原有节点，不new新ListNode，空间\(O(1)\)。

- 易错点：
  1. 忘记移动 tail 指针，结果链表永远只在dummy后面覆盖同一个节点；
  2. 循环结束忘记拼接剩余链表，截断后面节点；
  3. 返回 dummy，而不是 `dummy.next`，把哨兵节点也返回；
  4. 输入链表为空，没有兼容处理（代码中and逻辑天然兼容空链表）。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 不使用dummy，手动判断list1、list2谁做头节点，大量if‑else，边界容易写错；
2. 循环内部只移动list1/list2，不移动tail指针，链表构建失败；
3. 循环结束直接return，没有接上剩下的链表；
4. 返回dummy，结果多出来一个值为‑1的哨兵节点。

**修正思路：**
1. 链表合并优先使用dummy哨兵；
2. 每接上一个节点，tail必须往后走；
3. while退出后一定把剩下链表接tail.next；
4. 返回的是dummy.next，不是dummy。

## ✅ 总结
21 合并两个升序链表，链表基础模板。
> dummy虚拟头 + tail尾指针；循环比较取小；剩余链表直接拼接；返回dummy.next。
> 这道题是23题合并k个升序链表的核心子函数，必须熟练默写。

> 拓展：本题也可以写递归版本，递归空间复杂度\(O(m+n)\)，面试优先写迭代版本。

### 递归版本
```python
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        if not list1:
            return list2
        if not list2:
            return list1
        if list1.val < list2.val:
            list1.next = self.mergeTwoLists(list1.next, list2)
            return list1
        else:
            list2.next = self.mergeTwoLists(list1, list2.next)
            return list2
```
