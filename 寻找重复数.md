# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：287
- **题目名称**：寻找重复数 Find the Duplicate Number
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/find-the-duplicate-number/
- **刷题日期**：2026\08\21
- **标签**：数组 / Floyd判圈算法 / 快慢指针 / 二分查找

## 📝 题目大意
给定包含 `n+1` 个整数的数组 `nums`，数组中数字范围为 `[1,n]`。数组中有且仅有一个数字重复（可重复多次），找出这个重复数字。
> 硬性约束：
> 1. **不能修改原数组**（禁止排序、原地正负标记）
> 2. **只能使用常数额外空间**（$\(O(1)\)$）
> 重点难点：很多直观解法会违反题目约束；本题最优思路把数组模拟成链表，复用环形链表II的Floyd判圈数学模型。
> 示例：`nums = [1,3,4,2,2]`，输出：`2`。

## 💡 解题思路
### 思路1：Floyd快慢指针（环的入口，满足题目全部约束⭐推荐）
**核心思想**：将数组抽象为虚拟链表
下标当作链表节点；`next[i] = nums[i]`，节点`i`的下一跳是`nums[i]`。
因为数值都在`1~n`，全部可以作为合法下标；存在重复数字等价链表存在环，**重复数字就是环的入口节点**。

**步骤拆解**
1. 快慢指针都从下标0出发；slow每次走一步：`slow = nums[slow]`；fast每次走两步：`fast = nums[nums[fast]]`，循环直到快慢指针相遇。
2. 一个指针从头节点0出发，另一个指针从相遇点出发，两个指针每次各走一步；相遇位置即为环入口，也就是重复数字。

### 思路2：二分查找（抽屉原理，满足约束，时间$O(n\log n)$）
统计数组中小于等于`mid`的元素数量`cnt`：
- 如果 `cnt > mid`：根据抽屉原理，重复数落在区间`[1, mid]`
- 否则：重复数落在区间`[mid+1, n]`
不断二分收缩区间，最后`left==right`就是答案。

### 思路3：集合Set（不满足约束，仅理解）
遍历数组存入set，如果当前数字已经在集合中，直接返回该数字。
> 缺陷：$\(O(n)\)$额外空间，不符合题目常数空间要求。

### 思路4：原地负数标记（不满足约束，仅理解）
遍历每个数字，取绝对值作为下标；把对应位置元素置为负数；
如果访问时该位置已经是负数，代表已经访问过，找到重复数字。
> 缺陷：修改原数组，违反题目条件。

## ⚙️ 复杂度分析
### Floyd快慢指针
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(1)\)$，不修改原数组，常数变量

### 二分查找
- **时间复杂度**：$O(n\log n)$，二分$\log n$轮，每轮遍历数组$\(O(n)\)$统计计数
- **空间复杂度**：$\(O(1)\)$

### Set集合
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(n)\)$，不满足题目要求

### 原地负数标记
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(1)\)$，但是修改输入数组，违反题目限制

## 💻 AC 代码（Python）
### ① Floyd快慢指针（最优，面试优先）
```python
from typing import List
class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        slow = 0
        fast = 0
        # 第一步：快慢指针相遇
        while True:
            slow = nums[slow]
            fast = nums[nums[fast]]
            if slow == fast:
                break
        # 第二步：头指针与相遇点同步前进，找环入口
        p1 = 0
        p2 = slow
        while p1 != p2:
            p1 = nums[p1]
            p2 = nums[p2]
        return p1
```

### ② 二分查找版本
```python
from typing import List
class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        left, right = 1, len(nums) - 1
        while left < right:
            mid = (left + right) // 2
            cnt = 0
            for x in nums:
                if x <= mid:
                    cnt += 1
            if cnt > mid:
                right = mid
            else:
                left = mid + 1
        return left
```

## 🧩 关键知识点 & 技巧
- 技巧1：数组模拟链表，下标为节点，`nums[cur]`代表next指针，复用142环形链表II的数学推导；
- 技巧2：抽屉原理：$\([1,mid]\)$最多容纳`mid`个不同数字，如果统计数量大于mid，则重复数一定落在这个区间；
- 技巧3：注意区分可行解法与暴力解法，读题优先看约束条件；
- 易错点：快慢指针初始化必须从下标`0`开始，不能直接赋值为`nums[0]`；返回的指针值就是重复数字。

## ❌ 错题反思 / 踩坑记录
**错误1**：直接使用set、原地负数标记，虽然能跑通样例，但是违反题目的硬性限制，提交会通过，但不符合题意，面试会扣分。
**错误2**：快慢指针初始化为`slow = nums[0], fast = nums[nums[0]]`，逻辑出错。
**错误3**：混淆链表题与本题：链表用`.next`，本题是数组模拟，使用`nums[x]`跳转。

**修正思路：**
1. 做题先阅读约束，判断哪些解法被禁止；
2. Floyd算法分两步：先找相遇点，再双指针同步找入口，两步不能合并。

## ✅ 总结
1. set、原地负数标记直观简单，但是受题目限制不能使用；
2. 二分可以通过，但时间复杂度更高；
3. Floyd快慢指针是本题最优解，$\(O(n)\)$时间、$\(O(1)\)$空间，不修改原数组；核心模型就是把数组映射成带环链表，重复数字等价环入口；
4. 该题和LeetCode142环形链表Ⅱ是同一套数学模型，一个操作链表，一个用数组模拟链表。
