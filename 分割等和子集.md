# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：416
- **题目名称**：分割等和子集 Partition Equal Subset Sum
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/partition-equal-subset-sum/
- **刷题日期**：2026\08\17
- **标签**：数组 / 动态规划 / 01背包

## 📝 题目大意
给定一个只包含**正整数**的非空数组 `nums`，判断能否将数组分割成两个子集，使得两个子集元素总和相等。
> 注意：子集不需要连续；每个元素只能分到其中一个集合，不能重复选取。

> 主流思路：
> 方案1：**一维DP压缩版01背包（当前实现）**
> 方案2：二维DP原始01背包（便于理解状态转移）

## 💡 解题思路
数学转化（最关键一步）
设数组全部元素总和为 `summ`
1. 如果 `summ` 是奇数 → 绝对无法平分，直接返回 `False`
2. 如果 `summ` 是偶数：令 `half = summ // 2`
题目等价于：**从数组选出若干数字，总和恰好等于 half**
👉 标准 01背包可行性问题：
物品：数组每个数字（只能选一次）
背包容量：`half`
目标：能否刚好装满背包。

### DP状态定义
`dp[i]`：是否可以选出若干数字，累加和等于 `i`
- `True`：可行
- `False`：不可行

### 初始化
`dp[0] = True`
和为0：不取任何元素，天然成立；其余初始为False。

### 状态转移
遍历每一个数字 `num`：
采用**逆序遍历背包容量**，避免同一个数字被多次选取（01背包核心特征）
```python
for i in range(half, num - 1, -1):
    dp[i] = dp[i] or dp[i - num]
```
含义：凑出和 `i` 有两种选择
1. 不选当前数字：维持原来 `dp[i]`
2. 选当前数字：看能否凑出 `i-num`

最终答案：`dp[half]`

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n \times half)$
  $n$ 为数组长度，half为总和一半
- **空间复杂度**：$\(O(half)\)$
  使用一维滚动数组进行空间优化

## 💻 AC 代码
```python
from typing import List
class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        n = len(nums)
        summ = sum(nums)
        # 总和奇数，直接无法平分
        if summ % 2 == 1:
            return False
        
        half = summ // 2
        dp = [False] * (half + 1)
        dp[0] = True

        for num in nums:
            # 01背包逆序遍历
            for i in range(half, num - 1, -1):
                dp[i] = dp[i] or dp[i - num]
        return dp[half]
```

## 🧩 关键知识点 & 技巧
1. 题意转化：分割两子集等和 → 寻找和等于总和一半的子集；
2. 区分 01背包 VS 完全背包：
   - 01背包（本题，元素只能选一次）：容量**从大到小遍历**
   - 完全背包（元素可重复选）：容量**从小到大遍历**
3. 一维数组是二维DP滚动优化而来，节省空间；
4. 数组全部是正整数，无需考虑负数带来的边界问题。

## ❌ 错题反思 / 踩坑记录
1. 忘记先判断总和奇偶，多余无效计算；
2. 背包容量写成正序遍历，变成完全背包，允许元素重复选取，答案错误；
3. 循环下限写错 `range(half, num, -1)`，漏掉 `i=num` 这个关键位置；
4. 混淆「子集」和「子数组」，试图用滑动窗口解题（滑动窗口只适合连续区间）；
5. 初始化错误，忘记设置 `dp[0]=True`。

## ✅ 总结
本题是01背包经典入门题型。
核心转化：**均分问题 → 子集和目标问题**；
核心模板：一维逆序DP实现01背包可行性判断。

> 拓展：同类背包题型区分
> 分割等和子集：01背包，是否可行（True/False）
> 零钱兑换Ⅱ：完全背包，统计方案数量
> 最后一块石头Ⅱ：01背包，求最小差值
