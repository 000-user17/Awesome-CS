# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：4
- **题目名称**：寻找两个正序数组的中位数 Median of Two Sorted Arrays
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/median-of-two-sorted-arrays/
- **刷题日期**：2026\09\04
- **标签**：数组 / 二分查找

## 📝 题目大意
给定两个升序数组 `nums1`（长度m）、`nums2`（长度n），找出两个数组的**中位数**。
要求时间复杂度严格 $\boldsymbol{\(\boldsymbol{\(O(log(m+n))\)}\)}$。

> 中位数定义：
总长度 `total = m + n`
- total 为奇数：合并后数组第 `k = total//2` 位置的元素（第k+1小）
- total 为偶数：合并后第 `(total‑1)//2` 和 `total//2` 两个数的平均值

示例1：
`nums1 = [1,3], nums2 = [2]` →合并`[1,2,3]`，总长度3奇数，中位数`2`
示例2：
`nums1 = [1,2], nums2 = [3,4]` →合并`[1,2,3,4]`，总长度4偶数，中位数`(2+3)/2 = 2.5`

> 分析：
1. 直接合并两个数组再取中位数：时间$\(O(m+n)\)$，不满足 log 复杂度要求，不能过题目硬性条件。
2. 题目强制 $\boldsymbol{\(\boldsymbol{\(O(log(m+n))\)}\)}$ → **二分思想**。
3. 核心转化：求两个有序数组中**第 k 小的元素**。
    - 奇数：直接取第k小
    - 偶数：求第k1小、第k2小，求平均。

## 💡 解题思路
### 核心思想：二分，求两个有序数组的第k小
> 函数 `findKth(a, b, k)`：在a、b两个升序数组，返回第k小的数字（k从1开始）

规则：
1. 保证 `a` 是更短的数组，如果a更长，交换a,b。（减少边界）
2. 如果其中一个数组为空，直接返回另一个数组的第`k‑1`下标元素。
3. 如果 `k == 1`：直接返回两个数组头部更小的值。
4. 取两个数组各自往前取 `k//2` 个元素：
    - `i = min(k//2, len(a)) -1`
    - `j = min(k//2, len(b)) -1`
    - 比较 `a[i]` 和 `b[j]`
      - 如果 `a[i] < b[j]`：a的前`i+1`个元素全部可以排除，它们一定不可能是第k小；在剩下的a[i+1:]、b中，找第 `k‑(i+1)` 小。
      - 否则：b的前`j+1`个元素全部排除，在a、b[j+1:]中找第 `k‑(j+1)` 小。

>为什么可以一次性扔掉k//2个？
两个数组都是升序；a[i]小，说明a[0…i]这些数全部小于b[j]，最多总共 `(i+1)+(j+1)-1` 个数比b[j]小，**这些数一定不是第k小，可以直接舍弃**。

### 主函数逻辑
```
total = m + n
k1 = (total + 1) // 2
k2 = (total + 2) // 2
# 奇数时k1 == k2；偶数k1、k2是中间两个位置
median = (findKth(nums1,nums2,k1) + findKth(nums1,nums2,k2)) / 2
return median
```
> 小技巧：不管奇偶，统一求k1,k2两个位置再平均，不用写两套分支。

## ⚙️复杂度分析
- **时间复杂度**：$\(\boldsymbol{\(O(log(m+n))\)}\)$
每次递归至少砍掉k/2个候选元素，k最大是m+n，对数级别。
- **空间复杂度**：$\(O(1)\)$；递归实现会有栈开销，可以改成迭代。

## 💻 AC代码
```python
class Solution:
    def findMedianSortedArrays(self, nums1: list[int], nums2: list[int]) -> float:
        def find_kth(a, b, k):
            # a,b 两个升序数组，找第k小，k从1开始
            if len(a) > len(b):
                return find_kth(b, a, k)
            # a为空，直接取b的第k‑1下标
            if not a:
                return b[k - 1]
            if k == 1:
                return min(a[0], b[0])
            # 各取k//2个元素
            i = min(len(a), k // 2) - 1
            j = min(len(b), k // 2) - 1
            if a[i] < b[j]:
                # 扔掉a前面i+1个，更新k
                return find_kth(a[i+1:], b, k - (i + 1))
            else:
                return find_kth(a, b[j+1:], k - (j + 1))

        total = len(nums1) + len(nums2)
        k1 = (total + 1) // 2
        k2 = (total + 2) // 2
        return (find_kth(nums1, nums2, k1) + find_kth(nums1, nums2, k2)) / 2.0
```

## 🧩关键知识点 &技巧
1. 题目强制 $\(\boldsymbol{\(O(log(m+n))\)}\)$，合并数组 $\(O(m+n)\)$ 思路只能做基础理解，**不能写在面试答卷**。
2. 问题转化：中位数等价求两个有序数组第k小元素，把奇偶两种情况统一处理。
3. 保证`a`是短数组，简化边界；切片`a[i+1:]`相当于把指针往后移动。
4. k是**从1开始计数**，数组下标是从0开始，下标要做 `-1`转换，极易搞错。
5. `min(len(a),k//2)`防止数组长度不足k//2，不能直接写`k//2‑1`，会越界。

>补充：另一种更难的二分：分割线法，在第一个数组上做二分切割，把两个数组切为左右两部分，满足左边全部≤右边，直接求中位数。面试上面两种都可以；**第k小递归思路更容易理解、更容易手写对**。

## ❌错题反思 /踩坑记录
1. k当成下标从0开始，全部逻辑错位。k代表第k小，起点是1。
2. 没有取`min(len(a),k//2)`，当数组长度小于k//2，索引越界报错。
3. 不交换a,b，a数组更长，边界处理复杂容易出错。
4. 直接合并数组，虽然能AC，但时间复杂度不满足题目log要求，面试直接扣分。
5. 偶数场景忘记求平均，直接返回中间某一个数。

**避坑要点**
- k从1开始，数组下标从0，两者时刻区分；
- 递归前保证a是短数组；
- 每次丢弃的元素数量是 `i+1`，不是i；
- 使用 `(total+1)//2` 与 `(total+2)//2` 统一奇偶，减少if分支。

## ✅总结
LeetCode经典困难题。
核心：把中位数转化为**两个有序数组找第k小元素**，二分每次舍弃一半候选元素，实现$\(\boldsymbol{\(O(log(m+n))\)}\)$。
面试优先掌握本套「第k小递归」写法，逻辑清晰好写。分割线版本理解难度更高。
