# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：15
- **题目名称**：三数之和
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/3sum/](https://leetcode.cn/problems/3sum/)
- **刷题日期**：2026\09\03
- **标签**：数组、排序、双指针

## 📝 题目大意

给整数数组 `nums`，找出全部三元组 `[nums[i], nums[j], nums[k]]`：

1. `i、j、k`下标互不相同；
2. `nums[i]+nums[j]+nums[k]==0`；
3. **结果不能包含重复的三元组**。

> 
> 重点难点：去重。不能简单用集合，要在遍历、双指针过程中跳过重复值。
> 返回的三元组内部元素可以任意顺序，整体不能出现重复组合。

## 💡 解题思路

**核心思想：排序 + 固定一个数，左右双指针找另外两个数**
暴力三层循环 O(n³)，会超时；排序后双指针降低到 O(n²)。

步骤拆解：

1. **数组先排序**。排序后方便双指针移动，也方便跳过重复元素。
2. 外层循环固定第一个数 `nums[i]`：
   - 如果 `nums[i] > 0`：后面全部数字都大于0，三数之和不可能等于0，直接break结束循环。
   - **i去重**：`i>0 and nums[i]==nums[i‑1]`，跳过，避免得到重复三元组。
3. 左指针 `left = i+1`；右指针 `right = len(nums)-1`
4. while `left < right`：
   - 计算三数总和 `s = nums[i]+nums[left]+nums[right]`
   - `s == 0`：找到一组答案，加入结果。> 
   > **left、right去重**：left向右跳过相同值；right向左跳过相同值。
   > 再执行 `left +=1；right -=1`
   - `s < 0`：总和偏小，需要变大，`left +=1`
   - `s > 0`：总和偏大，需要变小，`right -=1`

> 
> 去重逻辑理解：排序后相同数字挨在一起，跳过相同值，就不会产生重复组合。
> 注意：找到一组答案之后，才执行left/right的去重，不要放在最开头。

## ⚙️ 复杂度分析

- **时间复杂度**：$O(n^2)$，排序$O(n\log n)$；外层循环n次，内层双指针最多遍历n次。
- **空间复杂度**：$O(\log n)$，排序栈开销；不计返回结果数组。

## 💻 AC 代码（Python）

```
from typing import List
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        res = []
        nums.sort()
        n = len(nums)
        for i in range(n):
            # 当前数大于0，后面不可能凑0
            if nums[i] > 0:
                break
            # i去重，和上一个一样就跳过
            if i > 0 and nums[i] == nums[i-1]:
                continue
            left = i + 1
            right = n - 1
            while left < right:
                total = nums[i] + nums[left] + nums[right]
                if total == 0:
                    res.append([nums[i], nums[left], nums[right]])
                    # left去重
                    while left < right and nums[left] == nums[left + 1]:
                        left += 1
                    # right去重
                    while left < right and nums[right] == nums[right - 1]:
                        right -= 1
                    left += 1
                    right -= 1
                elif total < 0:
                    left += 1
                else:
                    right -= 1
        return res
```

## 🧩 关键知识点 & 技巧

- 技巧1：先排序，排序是本题的前提，兼顾双指针 + 去重。
- 技巧2：外层i的剪枝：`nums[i]>0`直接break，提升效率。
- 技巧3：三层去重：
  1. i层：`nums[i]==nums[i‑1]` continue；
  2. 找到答案后left跳过重复；
  3. 找到答案后right跳过重复。
- 技巧4：双指针循环条件 `left < right`，不能取等号，下标必须不同。
- 易错点：
  1. 不去重，结果出现大量重复三元组；
  2. i去重写成 `nums[i]==nums[i+1]`，错误；要和**前一个 i‑1**对比；
  3. left、right去重写在if total==0外面，逻辑出错；
  4. 忘记`nums[i]>0`剪枝；
  5. while条件少写`left < right`，发生数组越界。

## ❌ 错题反思 / 踩坑记录

**错误原因：**

1. 没有排序直接双指针，双指针逻辑完全失效；
2. i去重对比`i+1`，漏掉合法解；必须对比`i‑1`；
3. left/right去重while没有加`left < right`，发生索引越界；
4. 找到一组答案后，忘记移动left、right指针，死循环；
5. 使用暴力三层循环，大数据直接超时。

**修正思路：**

1. 排序是基础；固定i，双指针找另外两个；
2. i>0才做i的去重，对比前一位；
3. left、right的去重while内部一定要带上`left<right`保护；
4. 收集答案之后必须left+=1，right‑=1。

## ✅ 总结

15 三数之和，排序+双指针经典中等题。

> 
> 排序；外层固定i，i>0去重；left=i+1，right末尾；
> 和等于0收集答案，再对left、right内部去重；和小左移，和大右移。
> 重点：三处去重逻辑，防止重复三元组。

> 
> 拓展：18题四数之和，思路类似：固定两层，再双指针。
