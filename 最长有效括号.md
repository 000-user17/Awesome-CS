# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：32
- **题目名称**：最长有效括号
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/longest-valid-parentheses/
- **刷题日期**：2026\09\02
- **标签**：动态规划 / 栈 / 字符串

## 📝 题目大意
给你一个只包含 `'('` 和 `')'` 的字符串，找出最长有效（格式正确且连续）括号子串的长度。
有效括号定义：每个左括号都有对应的右括号完成闭合，子串要求连续。
示例：`")()())"` 输出4；`"(()"` 输出2；`""` 输出0。
> 重点难点：
> 1. 求**连续子串**，不是子序列；
> 2. 合法有效子串只能以右括号 `)` 结尾；
> 3. 匹配成功后，需要拼接前面相邻已经合法的括号片段。

## 💡 解题思路
### 方法一：栈解法
**核心思想：栈存储字符串下标，使用哨兵充当有效区间的左边界。**
栈初始化压入哨兵 `-1`，作为有效子串的基准左边界。
遍历每一个下标 `i`：
1. 遇到左括号 `'('`：将下标 `i` 入栈；
2. 遇到右括号 `')'`：执行栈顶弹出；
   - 如果栈变为空：把当前下标 `i` 压入栈，更新新的左边界哨兵；
   - 如果栈不为空：`i - stack[-1]` 得到当前有效括号长度，更新全局最大值。

**步骤拆解**
1. 初始化栈 `stack = [-1]`，结果变量 `res = 0`；
2. 遍历字符串每一个下标i；
3. 左括号直接压入下标；右括号弹出栈顶；
4. 弹出后栈空则压入当前i作为新哨兵；不为空则计算有效长度更新res；
5. 遍历结束返回res。

示例推演：`s = ")()())"`
初始栈：`[-1]`
i=0，字符`)`：pop，栈空，push 0 → stack=[0]
i=1，字符`(`：push 1 → stack=[0,1]
i=2，字符`)`：pop，栈不为空，`2‑0=2`，res=2
i=3，字符`(`：push3 → stack=[0,3]
i=4，字符`)`：pop，栈不为空，`4‑0=4`，res=4
i=5，字符`)`：pop，栈空，push5 → stack=[5]
最终结果 res=4。

### 方法二：动态规划
**核心思想：`dp[i]` 表示以 `s[i]` 结尾的最长有效括号子串长度。**
`s[i] == '('`，无法作为合法结尾，`dp[i]=0`。
分两种情况处理右括号：
1. `s[i]==')'` 且 `s[i‑1]=='('`，形成 `...()`：
`dp[i] = (i<2 ? 0 : dp[i‑2]) + 2`，加上i‑2位置之前的有效长度。
2. `s[i]==')'` 且 `s[i‑1]==')'`，形成 `...))`：
计算前一段匹配区间的前一位下标 `pre = i - dp[i‑1] -1`；
如果 `pre >=0` 并且 `s[pre] == '('`，代表 `s[pre]` 和 `s[i]` 配对；
`dp[i] = dp[i‑1] + 2 + (pre‑1 < 0 ? 0 : dp[pre‑1])`，叠加前面已经合法的子串长度。

遍历全程维护全局最大长度 `res`。

**步骤拆解**
1. n = len(s)，dp数组全部初始化为0，`res=0`；
2. i从1开始遍历字符串；
3. 当前字符为右括号，分两种场景更新dp[i]；
4. 每轮循环更新全局最长长度 res；
5. 返回 res。

示例推演：`s = ")()())"`
dp初始：`[0,0,0,0,0,0]`
i=1，s[1]='(' → dp[1]=0，res=0
i=2，s[2]=')'，s[1]='('，i<2 → dp[2]=0+2=2，res=2
i=3，s[3]='(' → dp[3]=0，res=2
i=4，s[4]=')'，s[3]='('，dp[4]=dp[2]+2=4，res=4
i=5，s[5]=')'，s[4]=')'，pre=0，s[0]=')'不匹配，dp[5]=0，res保持4。
最终答案4。

## ⚙️ 复杂度分析
- **栈解法**
  - 时间复杂度：$\(O(n)\)$，字符串一次遍历；
  - 空间复杂度：$\(O(n)\)$，最坏情况栈存储全部下标。
- **动态规划解法**
  - 时间复杂度：$\(O(n)\)$，字符串一次遍历；
  - 空间复杂度：$\(O(n)\)$，dp数组占用n大小空间。

## 💻 AC 代码
```python
# 栈实现
class Solution:
    def longestValidParentheses(self, s: str) -> int:
        n = len(s)
        stack = [-1]
        res = 0
        for i in range(n):
            if s[i] == '(':
                stack.append(i)
            else:
                stack.pop()
                if not stack:
                    stack.append(i)
                else:
                    res = max(res, i - stack[-1])
        return res
```
```python
# DP实现
class Solution:
    def longestValidParentheses(self, s: str) -> int:
        n = len(s)
        dp = [0] * n
        res = 0
        for i in range(1, n):
            if s[i] == ')':
                if s[i-1] == '(':
                    dp[i] = (0 if i < 2 else dp[i-2]) + 2
                else:
                    pre = i - dp[i-1] - 1
                    if pre >= 0 and s[pre] == '(':
                        dp[i] = dp[i-1] + 2 + (0 if pre - 1 < 0 else dp[pre-1])
            res = max(res, dp[i])
        return res
```

## 🧩 关键知识点 & 技巧
1. 栈技巧：栈保存**下标而不是字符**；哨兵 `-1` 用来计算第一组有效括号长度；遇到多余右括号，把当前下标压入充当新边界。
2. DP定义：`dp[i]` 是以i位置结尾的有效子串长度，不是0~i全局最长，全局最大值需要单独维护。
3. `...))`场景，要跳过i‑1已经匹配完成的区间，定位到pre位置判断是否存在左括号；匹配成功后必须叠加 `dp[pre‑1]`，拼接前面相邻合法片段。
4. 下标边界判断：i<2、pre‑1<0 时，对应的dp值取0，防止数组索引越界。

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. DP错误理解dp[i]为0~i全局最长，推导逻辑全部出错；
2. `...))`场景忘记叠加 `dp[pre‑1]`，无法拼接前面已经合法的括号子串；
3. 没有处理i‑2、pre‑1小于0的情况，直接访问dp数组，造成索引越界；
4. 栈解法忘记设置哨兵，或者多余右括号出现后没有更新栈的边界；
5. 混淆子串与子序列，本题必须连续，不能跳过字符。

**避坑要点**
- 有效括号子串结尾一定是右括号，左括号结尾dp值恒为0；
- 栈的哨兵作用：保证第一次匹配时可以正常计算长度；多余右括号会破坏连续性，更新哨兵边界；
- 每一轮循环都要更新全局最大值，不能返回dp最后一位。

## ✅ 总结
32最长有效括号属于字符串困难经典题，栈和动态规划两种主流解法。
栈利用下标+哨兵记录有效区间边界，逻辑直观；动态规划以当前下标结尾定义dp状态，分 `()`、`))` 两种场景处理，注意拼接前面合法片段。
> 拓展：还有双向扫描解法，可以把空间优化到 $\(O(1)\)$。
