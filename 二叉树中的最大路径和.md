# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：124
- **题目名称**：二叉树中的最大路径和 Binary Tree Maximum Path Sum
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/binary-tree-maximum-path-sum/
- **刷题日期**：2026\08\14
- **标签**：二叉树 / DFS / 后序遍历 / 递归

## 📝 题目大意
二叉树路径定义：相邻节点之间存在边，一条路径内同一个节点只能出现一次；路径至少包含一个节点，**不一定经过根节点**。
路径和为路径上所有节点数值之和，求二叉树中最大路径和。

> 可行思路对比
> 暴力枚举全部路径复杂度极高，无法通过；
> 最优方案：**后序深度优先搜索DFS**，区分「就地更新答案的分叉路径」与「向上传递的单向路径」。

## 💡 解题思路
定义 `dfs(node)`：以当前节点为起点，向下延伸，能够向上传递给父节点的**单向最大路径和**。
1. 递归终止条件：空节点返回0；
2. 递归获取左、右子树贡献值，如果子树贡献为负数，直接舍弃（取0）；
3. 构造分叉路径：左分支 + 当前节点值 + 右分支，这条路径无法向上继续延伸，用来更新全局最大值；
4. 向上只能返回单向路径：当前节点值 + 左右分支中较大的一支；
5. 全局变量持续记录遍历过程中所有路径的最大值。

> 核心关键点区分
> - `leftValue + node.val + rightValue`：分叉完整路径，仅用来更新答案，不能向上返回；
> - `max(leftValue, rightValue) + node.val`：单向路径，提供给上层父节点拼接。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，二叉树每个节点仅遍历一次
- **空间复杂度**：$\(O(h)\)$，$h$ 为二叉树高度，递归栈开销；树退化成单链表时复杂度 $\(O(n)\)$

## 💻 AC 代码
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
from typing import Optional
class Solution:
    def __init__(self):
        self.maxSum = 0

    def maxPathSum(self, root: Optional[TreeNode]) -> int:
        self.maxSum = float("-inf")

        def dfs(node):
            if not node:
                return 0

            leftValue = max(dfs(node.left), 0)
            rightValue = max(dfs(node.right), 0)
            self.maxSum = max(self.maxSum, leftValue + node.val + rightValue)

            return max(leftValue, rightValue) + node.val
        
        dfs(root)
        return self.maxSum
```

## 🧩 关键知识点 & 技巧
1. 子树返回值为负数时取0，代表放弃这条分支，不引入负收益；
2. 全局最大值初始化为负无穷，兼容全部节点为负数的边界用例；
3. 路径可以只包含单个节点；
4. 采用后序遍历：先处理左右子树，再计算当前节点的路径贡献。

## ❌ 错题反思 / 踩坑记录
1. 混淆分叉路径和单向路径，递归错误返回 `left+root+right`，上层无法拼接；
2. 不剔除负贡献分支，强行累加负数，导致路径和结果偏小；
3. `self.maxSum` 初始值设置为0，遇到全负数二叉树得到错误答案；
4. 思维定式，默认路径必须经过根节点。

## ✅ 总结
二叉树经典困难递归题型。
核心口诀：**分叉路径就地更新答案，单向路径向上传递。**
