# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：72
- **题目名称**：编辑距离 Edit Distance
- **题目难度**：困难
- **题目链接**：[https://leetcode.cn/problems/edit-distance/](https://leetcode.cn/problems/edit-distance/)
- **刷题日期**：2026\09\14
- **标签**：动态规划 DP

## 📝 题目大意
给两个单词 `word1`、`word2`，求把 word1 变成 word2 的**最少操作次数**。
允许三种操作：
1. 插入一个字符
2. 删除一个字符
3. 替换一个字符

示例：
`word1 = "horse", word2 = "ros"`
输出：`3`
解释：
horse → rorse（替换h为r）
rorse → rose（删除r）
rose → ros（删除e）

> DP经典题，字符串DP。定义dp[i][j]表示：**word1前i个字符，变成word2前j个字符，最少操作数**。

## 💡 解题思路
### 状态定义
`dp[i][j]`：`word1[0:i]`（前i个） → `word2[0:j]`（前j个）最小操作次数。
> 注意下标：i=0代表word1空串；j=0代表word2空串。

### 初始化（base case）
- `dp[i][0]`：word1前i个字符变成空串，只能全部删除 → `dp[i][0] = i`
- `dp[0][j]`：空串变成word2前j个字符，只能全部插入 → `dp[0][j] = j`

### 状态转移方程
两种情况：
1. `word1[i-1] == word2[j-1]`（当前字符相等，不用操作）
$$dp[i][j] = dp[i-1][j-1]$$
2. `word1[i-1] != word2[j-1]`，三种操作取最小值：
    - **替换**：把word1[i-1]替换成word2[j-1] → `dp[i-1][j-1] + 1`
    - **删除**：删掉word1[i-1]，看word1前i-1个匹配word2前j个 → `dp[i-1][j] +1`
    - **插入**：word1插入word2[j-1]，等价于word2去掉j-1，看word1前i匹配word2前j-1 → `dp[i][j-1] +1`
$$dp[i][j] = min(dp[i-1][j-1], dp[i-1][j], dp[i][j-1]) + 1$$

最终答案：`dp[m][n]`，m=len(word1), n=len(word2)

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(mn)\)$，m、n分别是两个字符串长度，填充m*n DP表
- **空间复杂度**：$\(O(mn)\)$；可以优化一维滚动数组降到 $\(O(min(m,n))\)$

## 💻 AC 代码（二维DP，方便理解，面试首选）
```python
class Solution:
    def minDistance(self, word1: str, word2: str) -> int:
        m, n = len(word1), len(word2)
        # dp[m+1][n+1]
        dp = [[0]*(n+1) for _ in range(m+1)]
        
        # base case
        for i in range(m+1):
            dp[i][0] = i
        for j in range(n+1):
            dp[0][j] = j
        
        for i in range(1, m+1):
            for j in range(1, n+1):
                if word1[i-1] == word2[j-1]:
                    dp[i][j] = dp[i-1][j-1]
                else:
                    dp[i][j] = min(
                        dp[i-1][j-1], #替换
                        dp[i-1][j],   #删除
                        dp[i][j-1]    #插入
                    ) + 1
        return dp[m][n]
```

## 🧩 关键知识点 & 技巧
1. 字符串DP习惯**dp下标从1开始**，0留给空串，简化边界判断；字符取的时候要i-1,j-1；
2. 三种操作对应DP三个方向，一定要理解：插入/删除互为镜像；
3. 编辑距离是LCS（最长公共子序列）的近亲，可以一起记忆；
4. 一维滚动数组优化：只保留上一行，空间压缩。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. dp下标直接用字符串下标i,j，没有预留空串i=0,j=0，边界崩掉；
2. 字符相等的时候，错误写`dp[i][j] = dp[i-1][j-1]+1`，多加一次操作；
3. min里面少写一种情况；
4. 混淆插入删除对应的dp子问题。

**避坑要点**
> word1[i-1] == word2[j-1]：不需要操作，直接继承左上角的值，不加1。

## ✅ 总结
72是字符串DP标杆困难题。核心：定义dp[i][j]为前i、前j字符的编辑距离，字符相等直接继承，不等取替换/删除/插入三者最小+1。
关联题目：1143最长公共子序列、583两个字符串的删除操作。
