# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：301
- **题目名称**：删除无效的括号 Remove Invalid Parentheses
- **题目难度**：困难
- **题目链接**：https://leetcode.cn/problems/remove-invalid-parentheses/
- **刷题日期**：2026\08\20
- **标签**：字符串 / BFS / DFS回溯 / 剪枝

## 📝 题目大意
给你一个由若干括号和字母组成的字符串 `s`，删除最小数量的无效括号，使得输入的字符串有效。
返回所有可能的结果，答案可以按任意顺序返回。
> 1. 删除括号数量要尽可能少；
> 2. 返回全部不同合法结果；
> 3. 字符串包含普通字母，字母必须保留；
> 4. 括号有效条件：任意前缀左括号≥右括号；整体左右括号总数相等。

## 💡 解题思路
### 思路1：BFS广度优先搜索（直观易懂）
**核心思想**：BFS每一层代表删除k个括号得到的字符串。
- 第0层原始字符串，如果合法直接返回；
- 每一层生成：删掉当前字符串每一个括号字符，得到下一层候选；
- **一旦某一层出现合法字符串，该层就是最小删除次数，收集本层所有答案直接终止，不再向下搜索**，天然保证删除括号数目最小。
- 使用visited集合去重，避免相同字符串重复入队。

**步骤拆解**
1. 辅助函数`is_valid`校验括号合法性；
2. 队列初始化存入原始字符串，visited集合记录已经处理过的字符串；
3. 逐层遍历队列，层内全部处理完成后判断是否找到解；
4. 如果找到合法解，收集结果并跳出循环。

### 思路2：DFS‑for‑start版本（无Set源头去重）
**核心思想**：先预处理，统计最少需要删除的左括号`rmL`、右括号`rmR`；递归枚举删除哪一个括号。
- `start`：本层只能从start下标之后选择要删除的位置，避免重复选择前面字符；
- 同层循环：`i != start and path[i]==path[i‑1]`跳过连续相同括号，**源头去重，不需要结果集合**；
- 剪枝：剩余待删括号数量 > 剩余字符，直接break；
- 当`rmL==0 and rmR==0`，再校验字符串是否合法，合法则加入答案。

### 思路3：传统DFS 逐位二分支（删 / 保留，Set结果去重）⭐
**核心思想**：按索引逐个遍历原字符串，每个括号字符两种选择：**删除、保留**；普通字母只能直接保留。
1. 预处理算出最少要删左括号`rmL`、右括号`rmR`，递归严格只允许删掉这么多括号，保证删除数目最小。
2. 参数：`idx`当前处理下标；`left/right`当前path内左右括号计数；`rmL/rmR`剩余还需要删除的括号；`path`当前拼接字符串。
3. 删除分支：满足还可以删该类型括号才执行，path不追加字符，idx前进。
4. 保留分支：左括号直接保留；保留右括号需要满足`left>right`，防止前缀非法。
5. 递归走到字符串末尾，如果已经删完需要删除的括号，把path加入`set`；利用集合对最终结果去重。

> ⚠️重要区分：
> 1. 逐位二分支DFS（删/保留二选一）：逻辑简单，但**无法优雅源头去重，只能用Set对最终结果去重**，强行用is_delete做连续括号跳过会误剪合法路径；
> 2. for‑start回溯版本：for循环枚举要删除的下标，同层跳过连续相同括号，可以做到不用Set。

## ⚙️ 复杂度分析
- **BFS**：时间 $O(n\cdot2^n)$，最坏每个括号删或不删；空间 $O(n\cdot2^n)$，队列存储候选字符串
- **for‑start DFS回溯**：时间 $O(n\cdot2^n)$，有剪枝实际远优于上界；空间 $\(O(n)\)$，递归栈深度为字符串长度
- **传统逐位二分支DFS**：时间 $O(n\cdot2^n)$，存在重复路径，依靠集合去重；空间 $\(O(n)\)$递归栈，$O(2^n)$集合存储结果

## 💻 AC 代码（Python）
### 版本1：传统DFS‑逐位二分支（Set去重）
```python
from typing import List
class Solution:
    def removeInvalidParentheses(self, s: str) -> List[str]:
        rmL, rmR = 0, 0
        balance = 0
        for c in s:
            if c == '(':
                balance +=1
            elif c == ')':
                if balance>0:
                    balance -=1
                else:
                    rmR +=1
        rmL = balance

        res = set()

        def dfs(idx, left, right, rmL, rmR, path):
            if idx == len(s):
                if rmL == 0 and rmR ==0:
                    res.add(path)
                return
            ch = s[idx]
            if ch not in "()":
                dfs(idx+1, left, right, rmL, rmR, path+ch)
                return
            
            # 删除当前括号
            if ch == '(' and rmL>0:
                dfs(idx+1, left, right, rmL-1, rmR, path)
            if ch == ')' and rmR>0:
                dfs(idx+1, left, right, rmL, rmR-1, path)
            # 保留当前括号
            if ch == '(':
                dfs(idx+1, left+1, right, rmL, rmR, path + '(')
            else:
                if left>right:
                    dfs(idx+1, left, right+1, rmL, rmR, path + ')')
        
        dfs(0,0,0,rmL,rmR,"")
        return list(res)
```

### 版本2：DFS‑for‑start（源头去重，无Set）
```python
from typing import List
class Solution:
    def removeInvalidParentheses(self, s: str) -> List[str]:
        balance = 0
        rmL, rmR = 0, 0
        for c in s:
            if c == '(':
                rmL += 1
            elif c == ")":
                if rmL <= 0:
                    rmR += 1
                else:
                    rmL -= 1

        def valid(t: str):
            bal = 0
            for ch in t:
                if ch == '(':
                    bal += 1
                elif ch == ')':
                    bal -= 1
                    if bal < 0:
                        return False
            return bal == 0

        res = []
        def dfs(start, rmL, rmR, path):
            if rmL == 0 and rmR == 0:
                if valid(path):
                    res.append(path)
                return
            for i in range(start, len(path)):
                # 同层跳过连续相同括号，源头去重
                if i != start and path[i] == path[i-1]:
                    continue
                # 剪枝：剩余待删括号大于剩下字符，不可能完成
                if rmL + rmR > len(path) - i:
                    break
                c = path[i]
                if c == '(' and rmL > 0:
                    # 删除i位置，下一轮start=i，不能回头删前面
                    dfs(i, rmL-1, rmR, path[:i] + path[i+1:])
                if c == ')' and rmR > 0:
                    dfs(i, rmL, rmR-1, path[:i] + path[i+1:])

        dfs(0, rmL, rmR, s)
        return res
```

### 版本3：BFS版本
```python
from typing import List
from collections import deque
class Solution:
    def removeInvalidParentheses(self, s: str) -> List[str]:
        def is_valid(t):
            bal = 0
            for ch in t:
                if ch == '(':
                    bal +=1
                elif ch == ')':
                    bal -=1
                    if bal < 0:
                        return False
            return bal == 0

        q = deque([s])
        visited = {s}
        res = []
        found = False
        while q:
            sz = len(q)
            for _ in range(sz):
                cur = q.popleft()
                if is_valid(cur):
                    res.append(cur)
                    found = True
                if found:
                    continue
                for i in range(len(cur)):
                    if cur[i] not in "()":
                        continue
                    nxt = cur[:i] + cur[i+1:]
                    if nxt not in visited:
                        visited.add(nxt)
                        q.append(nxt)
            if found:
                break
        return res
```

## 🧩 关键知识点 \& 技巧
- 技巧1：BFS天然得到最小删除次数，找到合法解直接停止向下层搜索；
- 技巧2：`for‑start`回溯模板，`i != start and path[i]==path[i‑1]`只在**同一层for循环**生效，可以源头去重，不要搬到逐位二分支递归；
- 技巧3：传统二分支DFS：每个括号分「删 / 保留」两条路径，逻辑最直白；重复路径交给`set`去重，不要强行做源头剪枝，避免剪丢合法解；
- 技巧4：剪枝条件：剩余待删除括号 > 当前剩余字符，直接break，提前终止循环；
- 技巧5：递归删除字符后下一层`start = i`，不能写`start = i+1`，允许后面递归删除当前i之后的相同括号；
- 易错点1：二分支递归模型不要使用`is_delete`实现连续括号去重，会误剪合法路径（例如连续多个左括号都需要删除的场景）；
- 易错点2：`start`参数含义：只能删除下标≥start的位置，避免重复组合；
- 易错点3：预处理计算`rmL、rmR`，代表最少必须删除多少括号，递归中不允许删更多。

## ❌ 错题反思 / 踩坑记录
**错误1：把同层for循环去重逻辑搬到逐位二分支递归，使用is_delete标记跳过删除分支**
- 错误原因：二分支递归允许多层递归连续删除相同括号；该逻辑会把合法路径剪掉，丢失正确答案。
- 修正思路：二分支递归老老实实使用Set集合对结果去重；想要源头去重改用`for‑start`回溯模板。

**错误2：dfs调用时start传i+1**
- 错误原因：删除i位置之后，后面还有和i相同括号需要删除，传i+1会跳过，漏掉方案。
- 修正思路：删除i之后下一层start = i。

**错误3：while循环在单次递归内部批量删除多个括号**
- 错误原因：破坏每次处理一个字符的状态，容易下标越界，丢失中间状态。
- 修正思路：使用for循环逐个枚举删除位置。

**错误4：传统二分支DFS忘记判断保留右括号时`left>right`**
- 错误原因：会生成前缀非法字符串，做很多无效递归。
- 修正思路：保留`)`必须保证当前左括号数量大于右括号。

## ✅ 总结
1. **BFS**：思路最简单适合面试快速写，利用层序保证删除括号最少；使用visited集合去重。
2. **DFS‑for‑start**：困难题最优回溯模板，同层循环跳过连续相同括号实现源头去重，无需Set，配合剩余字符剪枝提速。
3. **传统逐位二分支DFS**：代码逻辑最直观，按索引逐个处理，删/保留两条分支；缺点产生重复路径，需要`set`对最终结果去重。
4. 注意区分两种DFS递归模型：「逐位二分支删/保留」与「for循环枚举删除下标」，二者去重逻辑不能混用。
