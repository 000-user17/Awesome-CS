# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：239
- **题目名称**：滑动窗口最大值
- **题目难度**：困难
- **题目链接**：[https://leetcode.cn/problems/sliding-window-maximum/](https://leetcode.cn/problems/sliding-window-maximum/)
- **刷题日期**：2026\08\29
- **标签**：队列 / 单调队列 / 滑动窗口

## 📝 题目大意

给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。返回滑动窗口中的最大值数组。

> 
> 重点难点：暴力遍历每个窗口求最大值时间复杂度O(nk)会超时；需要使用单调递减队列维护窗口内候选最大值，实现O(n)线性复杂度。

## 💡 版本1：单循环单调队列（你提供的精简版本）

### 核心思想

使用双端队列保存数组下标，队列内下标对应的数值保持单调递减，队首永远是当前窗口最大值：

1. 新元素入队前，不断弹出队尾所有小于等于当前值的下标（这些元素不可能成为后续窗口最大值）；
2. 将当前下标i加入队列；
3. 如果队首下标已经脱离窗口左边界（`i - k == q[0]`），说明该下标滑出窗口，弹出队首；
4. 当窗口成型（`i - k +1 >=0`），将队首对应数值加入结果集。

**步骤拆解**

1. 初始化双端队列q、结果数组res；
2. 遍历数组下标i：
   - 维护单调递减队列，弹出尾部不大于nums[i]的下标；
   - 当前下标入队；
   - 判断队首是否超出窗口范围，超出则弹出队首；
   - 窗口成型时，取队首数值存入res；
3. 遍历结束返回res。

示例推演：nums = [1,3,-1,-3,5,3,6,7], k=3
i=0，num=1 → q=[0]，i-k+1=-2 不存结果
i=1，num=3 → 弹出0，q=[1]，i-k+1=-1 不存结果
i=2，num=-1 → q=[1,2]，i-k+1=0，res=[3]
i=3，num=-3 → q=[1,2,3]，i-k+1=1，res=[3,3]
i=4，num=5 → 依次弹出3,2,1，q=[4]，i-k+1=2，res=[3,3,5]
i=5，num=3 → q=[4,5]，i-k+1=3，res=[3,3,5,5]
i=6，num=6 → 弹出5,4，q=[6]，i-k+1=4，res=[3,3,5,5,6]
i=7，num=7 → 弹出6，q=[7]，i-k+1=5，res=[3,3,5,5,6,7]
最终答案：[3,3,5,5,6,7]

## ⚙️ 复杂度分析

- **时间复杂度**：$(O(n))$，每个元素最多入队、出队各一次
- **空间复杂度**：$(O(k))$，队列最多保存k个下标

## 💻 AC 代码

```
from typing import List
from collections import deque
class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        n = len(nums)
        res = []
        q = deque()
        for i in range(n):
            while q and nums[q[-1]] <= nums[i]:
                q.pop()
            q.append(i)
            if i - k == q[0]:
                q.popleft()
            if i - k + 1 >= 0:
                res.append(nums[q[0]])
        return res
```

## 🧩 关键知识点 & 技巧

1. 队列存储下标而非数值，方便判断元素是否滑出窗口；
2. 单调递减队列，队首为当前窗口最大值；
3. `i - k == q[0]` 精准判断队首元素刚好离开窗口；
4. `i - k +1 >=0` 作为窗口成型判断条件。

## ❌ 错题反思 / 踩坑记录

**常见错误**

1. 单调方向写反，使用单调递增队列；
2. 队列直接存数值，无法判断元素是否滑出窗口；
3. 窗口未成型就收集结果；
4. 弹出队首的边界条件写错。
**避坑要点**

- 只有刚好队首下标等于i-k时才弹出，不要用`q[0] < i-k`；
- 每次新元素都要维护队列单调性，不能遗漏。

## ✅ 总结

简洁单循环单调队列写法，代码简短精炼，是面试最常写的标准版本，逻辑清晰，时间复杂度最优。

---

## 💡 版本2：双指针单调队列版本

### 核心思想

用i代表窗口左边界，j代表窗口右边界，配合单调递减队列维护窗口最大值：

1. j不断向右扩张，新元素入队前清理队尾较小元素，维持队列单调递减；
2. 当窗口长度达到k（`j-i+1 ==k`），窗口成型，队首即为当前窗口最大值，存入结果；
3. 准备收缩左边界i前，如果队首下标等于i，代表最大值即将滑出窗口，弹出队首；
4. i、j同时右移，继续滑动窗口直到遍历结束。

**步骤拆解**

1. 初始化i=0，j=0，双端队列q、结果数组res；
2. j循环遍历数组：
   - 维护单调递减队列，弹出尾部不大于nums[j]的下标；
   - 当前下标j入队；
   - 窗口未满（`j-i+1<k`）：仅j右移；
   - 窗口已满：收集队首数值，判断队首是否等于左边界i，相等则弹出队首，i和j同时右移；
3. 遍历结束返回res。

示例推演：nums = [1,3,-1,-3,5,3,6,7], k=3
i=0,j=0 → q=[0]，窗口不足3，j++
i=0,j=1 → q=[1]，窗口不足3，j++
i=0,j=2 → q=[1,2]，窗口满，res=[3]，q[0]==0，不弹出，i=1,j=3
i=1,j=3 → q=[1,2,3]，窗口满，res=[3,3]，q[0]==1，弹出，i=2,j=4
i=2,j=4 → q=[4]，窗口满，res=[3,3,5]，q[0]!=2，i=3,j=5
i=3,j=5 → q=[4,5]，窗口满，res=[3,3,5,5]，q[0]!=3，i=4,j=6
i=4,j=6 → q=[6]，窗口满，res=[3,3,5,5,6]，q[0]!=4，i=5,j=7
i=5,j=7 → q=[7]，窗口满，res=[3,3,5,5,6,7]
最终答案：[3,3,5,5,6,7]

## ⚙️ 复杂度分析

- **时间复杂度**：$(O(n))$，每个元素最多入队、出队各一次
- **空间复杂度**：$(O(k))$，队列最多保存k个下标

## 💻 AC 代码

```
from typing import List
from collections import deque
class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        n = len(nums)
        i, j = 0, 0
        res = []
        q = deque()
        while j < n:
            # 维护单调递减队列
            while q and nums[q[-1]] <= nums[j]:
                q.pop()
            q.append(j)
            # 窗口还没达到k，右边界扩张
            if j - i + 1 < k:
                j += 1
            else:
                # 窗口成型，记录最大值
                res.append(nums[q[0]])
                # 左边界要移动，如果队首是即将移除的元素则弹出
                if q[0] == i:
                    q.popleft()
                i += 1
                j += 1
        return res
```

## 🧩 关键知识点 & 技巧

1. i、j双指针清晰区分窗口左右边界，窗口成型逻辑直观；
2. 队列依旧维持单调递减，保存下标；
3. 仅当队首下标等于窗口左边界i时才弹出，避免误删；
4. 适合初学者理解滑动窗口扩张、收缩全过程。

## ❌ 错题反思 / 踩坑记录

**常见错误**

1. 嵌套while错误构建窗口，导致窗口长度异常；
2. 单调队列维护只写在初始化窗口阶段，滑动新增元素不处理；
3. 误将队尾作为窗口最大值；
4. 没有判断队列是否为空直接访问q[0]，引发索引异常。
**避坑要点**

- 所有新增元素都必须执行单调队列维护逻辑；
- 窗口满之后才收集结果；
- 只有左边界要丢弃的元素恰好是队首最大值时，才弹出队首。

## ✅ 总结
双指针版本可以直观体现滑动窗口「扩张 - 收集 - 收缩」的完整流程，适合面试时一步步口述思路，和单循环版本底层原理完全一致，只是代码组织形式不同。

双指针版本可以直观体现滑动窗口「扩张-收集-收缩」的完整流程，适合面试时一步步口述思路，和单循环版本底层原理完全一致，只是代码组织形式不同。
