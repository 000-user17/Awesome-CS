# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：236

- **题目名称**：二叉树的最近公共祖先

- **题目难度**：中等

- **题目链接**：https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/description/?envType=problem-list-v2&envId=2cktkvj&

- **刷题日期**：2026\08\07

- **标签**：二叉树/DFS

## 📝 题目大意

给定一个二叉树, 找到该树中两个指定节点的最近公共祖先。

百度百科中最近公共祖先的定义为：“对于有根树 T 的两个节点 p、q，最近公共祖先表示为一个节点 x，满足 x 是 p、q 的祖先且 x 的深度尽可能大（一个节点也可以是它自己的祖先）。”

## 💡 解题思路

**核心思想：**递归寻找左右子树是否存在指定节点，如果存在则记录，最终取同时存在于左右子树根/根为最终答案

**步骤拆解：**

1. 确定方法为递归查找

2. 递归函数含义确定，入参为根节点，搜寻节点p和q。目标是寻找到p和q节点的最近公共祖先。可能当前根节点是最近公共祖先，也可能存在于左子树和右子树。
   
   a. 左子树和右子树同时存在p或q，则最近公共祖先只能是root；
   
   b. 左子树存在p和q，最近公共祖先在左子树，左子树继续执行递归查找
   
   c. 右子树存在p和q，最近公共祖先在右子树，右子树继续执行递归查找


3. 内部递归边界条件：
    
   a. 根节点空，代表寻找失败，返回空；

   b. 特殊终止条件：root等于p或q，返回root，代表当前root只可能是/不是最近公共祖先，中断递归查询，不可能在其子树中查找。如果找到了p，q只可能存在于p的子树中，或存在于p的根节点的另外一颗子树，所以需要提前退出查看根节点另外一颗子树情况

4. 返回：
   
   a. 查找完左右子树是否存在p或q后，如果左右子树都有p和q，则返回root

   b. 左子树或右子树如果单独存在p和q，返回左/右子树查询结果存在p和q

   c. 都不存在，返回None，无公共祖先

## ⚙️ 复杂度分析

- **时间复杂度**：O\( \)

- **空间复杂度**：O\( \)


## 💻 AC 代码（Python）

```python

# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None

class Solution(object):
    def lowestCommonAncestor(self, root, p, q):
        """
        :type root: TreeNode
        :type p: TreeNode
        :type q: TreeNode
        :rtype: TreeNode
        """
        if not root:
            return None
        if root == p or root == q:
            return root
        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)
        if left and right:
            return root
        if left:
            return left
        elif right:
            return right
    
```

## 🧩 关键知识点 \& 技巧

- 技巧1：

- 技巧2：

- 易错点：

## ❌ 错题反思 / 踩坑记录



**错误原因：**

**修正思路：**

## ✅ 总结
递归相关题目需要明确递归的目标和边界条件，对于本题目目标为找到最近公共祖先，递归函数的目标为找到最近公共祖先或p或q；题目保证了p和q一定存在，因此递归函数的目标=最终目标。

证明如下：
1. 最初的根节点是p或q，则一定为最近公共祖先
2. 查找过程中，某一根节点的左子树为p，则另外一个节点q一定在p的子树或p的祖先节点的另外一个子树上，而q一定会在祖先节点的另外一个子树上寻找到。所以此祖先节点就是最近公共祖先

所以以寻找p或q或最近公共祖先为目标时，一定能找到最近公共祖先

