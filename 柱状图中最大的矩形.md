# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：84
- **题目名称**：柱状图中最大的矩形 Largest Rectangle in Histogram
- **题目难度**：困难
- **题目链接**：[https://leetcode.cn/problems/largest-rectangle-in-histogram/](https://leetcode.cn/problems/largest-rectangle-in-histogram/)
- **刷题日期**：2026\09\09
- **标签**：数组 / 单调栈

## 📝 题目大意

给定 n 个非负整数，用来表示柱状图中各个柱子的高度。每个柱子彼此相邻，且宽度为 1。
求在该柱状图中，能够勾勒出来的矩形的最大面积。

> 
> 分析：
> 暴力思路：枚举每一根柱子作为矩形高度，向左右扩散直到遇到比它矮的柱子，计算宽度。两层循环 $O(n^2)$，数据量大超时。
> ✅ 核心思想：**单调递增栈**
> 对于每一根柱子 `heights[idx]`，想以这根柱子的高度作为矩形的高，向左右尽可能延伸。
> 需要找到：
> 
> 
> 1. **左侧第一个高度小于当前柱子的位置**（左边界）
> 2. **右侧第一个高度小于当前柱子的位置**（右边界）
> 左右边界中间的区间，就是以 `heights[idx]` 为高能扩展的最大宽度；面积 = 高度 × 宽度。
> 栈保存柱子下标，栈中下标对应的柱子高度保持递增。一旦遇到更小柱子，栈顶找到右边界，可以弹出结算面积。
> 末尾追加哨兵0，强制把栈内剩下的元素全部结算。

## 💡 解题思路

### 核心架构

1. 初始化空栈，栈内存放柱子**下标**，维持栈内对应高度单调递增；
2. 在原数组末尾追加一个高度0的哨兵柱子，用来清空栈内剩余元素；
3. 遍历每一根柱子下标 i：
   - 当栈不为空，并且当前柱子高度 `h[i] < h[栈顶下标]`：
     - 弹出栈顶下标 `idx`（当前要结算的柱子）
     - 取高度 `height = h[idx]`
     - 右边界就是 i；弹出后新栈顶就是左边界；
     - 计算宽度：栈空则左边界=-1，宽度=i；否则 `width = i - stack[-1] -1`
     - 更新全局最大面积
   - 满足递增，直接把当前下标i压入栈
4. 遍历结束，返回最大面积

> 
> 样例 heights = [2,1,5,6,2,3]
> 追加哨兵后 h = [2,1,5,6,2,3,0]
> 最终最大面积 = 10

### 模拟简要流程

- i=0，h=2，栈空，push 0 → stack [0]
- i=1，h=1 < h[0]=2，触发结算，弹出idx=0，面积=2*1=2；push1 → stack [1]
- i=2，h=5>1，push2 → stack [1,2]
- i=3，h=6>5，push3 → stack [1,2,3]
- i=4，h=2 < h[3]=6，弹出idx3，面积6*1=6；继续h=2<h[2]=5，弹出idx2，面积5*2=10；push4 → stack [1,4]
- i=5，h=3>2，push5 → stack [1,4,5]
- i=6，h=0，依次弹出5、4、1，结算剩余柱子面积。> 
> 最大面积10

## ⚙️ 复杂度分析

- **时间复杂度**：$(O(n))$，每个元素最多入栈一次、出栈一次
- **空间复杂度**：$(O(n))$，栈最多存储n个下标

## 💻 AC 代码

```
class Solution:
    def largestRectangleArea(self, heights: list[int]) -> int:
        stack = []
        max_area = 0
        # 末尾增加哨兵0，强制清算栈内剩余柱子
        h = heights + [0]
        for i in range(len(h)):
            # 遇到比栈顶更小的柱子，栈顶找到右边界，结算
            while stack and h[i] < h[stack[-1]]:
                idx = stack.pop()
                height = h[idx]
                if not stack:
                    # 左边没有更小柱子，左边界=-1，宽度 = i
                    width = i
                else:
                    # 左边界：弹出之后新的栈顶
                    width = i - stack[-1] - 1
                max_area = max(max_area, height * width)
            stack.append(i)
        return max_area
```

## 🧩 关键知识点 & 技巧

1. 单调递增栈的作用：**快速找到每个元素左右第一个更小元素**；
2. 栈存下标，不是高度！方便通过下标回查高度，同时计算宽度；
3. 哨兵0：不用循环结束后单独处理栈内残留元素，代码简化；
4. 条件 `h[i] < h[stack[-1]]`：h[i]只是**右边界触发器**，结算的高度是被弹出柱子`h[idx]`，不是h[i]；
5. 宽度公式：区间 `(stack[-1],i)`，左右边界都不包含，所以要 `-1`。

## ❌ 错题反思 / 踩坑记录

**高频错误**

1. 把判断条件写成 `h[i] > h[stack[-1]]`，整个逻辑颠倒，无法正确结算；
2. 栈里面直接存高度而不是下标，无法计算宽度；
3. 宽度公式忘记 `-1`，算出宽度偏大，答案错误；
4. 没有哨兵，遍历结束栈里还有元素没有结算，漏掉一部分矩形；
5. 混淆单调递增栈/单调递减栈：找左右更小元素用递增栈；接雨水找更大元素用递减栈；
6. 误以为h[i]是当前矩形高度：h[i]只是障碍物，结算高度是被弹出的栈顶柱子。

**避坑要点**

- 栈存下标，不要存高度；
- 触发条件：`h[i] < h[stack[-1]]`；
- 宽度：`i - stack[-1] -1`；栈空时宽度=i；
- 哨兵0必不可少，简化边界；
- 结算对象：**被pop出去的idx对应的柱子**，i只是标记右边界。

## ✅ 总结

84是单调栈经典模板题，也是85最大矩形的底层依赖。核心：**每根柱子以自身高度作为矩形高，单调栈快速锁定左右第一个更矮的边界，计算最大宽度**。
面试可以先讲暴力的缺陷，再引入单调栈思路，配合样例模拟。

> 
> 关联题目：85 最大矩形（二维矩阵逐行转为柱状图，调用本题函数）

---

要不要继续按这个模板写**接雨水 42** 的刷题笔记？
