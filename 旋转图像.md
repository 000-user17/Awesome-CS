# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：48
- **题目名称**：旋转图像 Rotate Image
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/rotate-image/](https://leetcode.cn/problems/rotate-image/)
- **刷题日期**：2026\08\31
- **标签**：数组 / 矩阵 / 原地算法

## 📝 题目大意

给定一个 `n × n` 的二维矩阵 `matrix`，将矩阵**顺时针旋转90度**。
👉 **要求：原地修改输入矩阵，不能新建另一个矩阵做中转。**

示例：

```
输入：
[[1,2,3],
 [4,5,6],
 [7,8,9]]

顺时针旋转90°后
[[7,4,1],
 [8,5,2],
 [9,6,3]]
```

> 
> 分析：

1. 如果允许开新矩阵：旋转公式 `new[j][n‑1‑i] = matrix[i][j]`，直接复制；但题目强制原地，不能用。
2. 原地两种主流思路：
   - 思路A：**先上下翻转，再沿主对角线（左上‑右下）转置**（最容易记，面试首选）
   - 思路B：分层循环，直接旋转每一层四个角的元素，直接交换4个位置。

---

## 💡 解题思路

### 方法1：翻转 + 转置（最推荐，好写好记）

**顺时针旋转90度 = 上下翻转 → 主对角线转置**

举例子 n=3
原始矩阵

```
1 2 3
4 5 6
7 8 9
```

1）**上下翻转**：第0行 和第2行交换，中间行不动

```
7 8 9
4 5 6
1 2 3
```

2）**主对角线转置**：`i,j` 和 `j,i`交换（只处理上三角，i<j）

```
7 4 1
8 5 2
9 6 3
```

✅完成顺时针90°旋转。

> 
> 补充记忆：
> 
> 
> - 顺时针90°：上下翻转 → 主对角线转置
> - 逆时针90°：左右翻转 → 主对角线转置
> - 旋转180°：上下翻转 + 左右翻转

### 方法2：分层旋转，四层轮换交换（纯原地，不开额外空间）

把矩阵看成一圈一圈的环。
外层一圈，内层一圈。
对于每一圈，一组四个位置循环交换数值。
对于位置 `(i,j)`，顺时针90的四个对应坐标：

```
(i, j)
(j, n‑1‑i)
(n‑1‑i, n‑1‑j)
(n‑1‑j, i)
```

四个位置循环赋值，用临时变量保存一个值，完成四轮交换。

> 
> 缺点：下标非常绕，容易写错位，面试容易写错。

## ⚙️ 复杂度分析

n 是矩阵边长

- **时间复杂度**：$O(n^2)$，每个元素被访问有限次
- **空间复杂度**：$(O(1))$，原地，只使用少量临时变量

## 💻 AC 代码

### 解法1：上下翻转 + 转置（优先背这个）

```
from typing import List

class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        """
        Do not return anything, modify matrix in-place instead.
        """
        n = len(matrix)
        # 1. 上下翻转：i行 和 n‑1‑i行交换
        for i in range(n // 2):
            matrix[i], matrix[n-1-i] = matrix[n-1-i], matrix[i]
        
        # 2. 主对角线转置，只遍历上三角 i < j
        for i in range(n):
            for j in range(i + 1, n):
                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
```

### 解法2：分层四位置轮换交换（了解）

```
from typing import List

class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        n = len(matrix)
        for i in range(n // 2):
            for j in range(i, n - 1 - i):
                # 保存左上角
                temp = matrix[i][j]
                # 左下 → 左上
                matrix[i][j] = matrix[n-1-j][i]
                # 右下 → 左下
                matrix[n-1-j][i] = matrix[n-1-i][n-1-j]
                # 右上 → 右下
                matrix[n-1-i][n-1-j] = matrix[j][n-1-i]
                # temp → 右上
                matrix[j][n-1-i] = temp
```

## 🧩 关键知识点 & 技巧

1. **原地算法**：不新建同等大小矩阵，直接修改输入；可以用少数临时变量。
2. 矩阵操作记忆套路：旋转、翻转大多可以拆解成「翻转 + 转置」组合，避免复杂下标。
3. 转置操作：只交换上三角 `i<j`，不要全部遍历，否则交换两遍还原回去。
4. `n//2`：上下翻转只需要交换前一半行，否则交换完又换回来了。
5. 题目函数不需要 return，直接修改原 matrix。

## ❌ 错题反思 / 踩坑记录

**高频错误**

1. 转置的时候写双重循环全部 i,j，没有限制 `j>i`，元素交换两次变回原矩阵。
2. 上下翻转循环写成 `range(n)`，全部行互相交换，等于没有变化，必须 `range(n//2)`。
3. 想直接开新矩阵保存结果再赋值，违反原地要求，面试直接扣分。
4. 分层写法下标写错，四个坐标对应关系搞混，出现元素覆盖丢失（一定要先存temp）。
5. 试图 return matrix，本题不需要返回，OJ看传入的matrix是否被修改。

**避坑要点**

- 翻转+转置思路下标简单，面试优先写这套；
- 转置：只处理上三角 i<j；
- 上下翻转只循环一半行数。

## ✅ 总结

旋转图像是矩阵高频题。
核心套路：**矩阵旋转不要硬推复杂下标，优先拆解成翻转+转置的组合**。

> 
> 顺时针90：上下翻转 → 主对角线转置。

> 
> 拓展思考：如果题目允许使用额外矩阵，那一行公式就完成：
> `res[j][n‑1‑i] = matrix[i][j]`，但本题禁止。
