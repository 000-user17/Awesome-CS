```
# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：560
- **题目名称**：和为 K 的子数组
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/subarray-sum-equals-k/
- **刷题日期**：2026\09\03
- **标签**：数组、前缀和、哈希表

## 📝 题目大意
给你一个整数数组 `nums` 和一个整数 `k`，统计并返回该数组中和为 `k` 的子数组的个数。
子数组是数组中元素的**连续非空序列**。
> 注意：数组元素可以包含负数、0；只统计数量，不需要输出具体子数组。

## 💡 解题思路
**核心思想：前缀和 + 哈希表优化**
前缀和 `preSum`：从数组起点累加到当前位置的总和。
子数组 `[l, r]` 的连续和 = `preSum[r] − preSum[l‑1]`。
我们希望子数组和等于 k：
`preSum[r] − preSum[l‑1] = k`
变形：`preSum[l‑1] = preSum[r] − k`。

含义：遍历到当前位置前缀和 `preSum`，查找历史上 `preSum‑k` 出现过多少次，就代表有多少个左端点可以构成和为k的子数组，累加到结果。

哈希表记录：key=前缀和数值，value=该前缀和出现的次数。
> 初始化 `preSums[0] = 1`：处理前缀和本身就等于k，子数组从数组第一个元素开始的情况。

步骤拆解：
1. 哈希表初始 `{0:1}`，`preSum=0`，结果`res=0`；
2. 遍历数组每一个数字：
   1. 更新当前前缀和 `preSum += num`；
   2. 哈希表查询 `preSum‑k` 的出现次数，加到结果；
   3. 将当前前缀和存入哈希表，计数+1；
3. 遍历完成返回结果 res。

> 顺序不能颠倒：**先查询，再存入哈希表**，避免把当前位置重复统计。
> ❌本题不能滑动窗口：数组存在负数，窗口扩大缩小总和没有单调性。

## ⚙️ 复杂度分析
- **时间复杂度**：$O(n)$，n为数组长度，遍历一次数组，哈希表增删查O(1)
- **空间复杂度**：$O(n)$，最坏所有前缀和互不相同，哈希表存储n个键值对

## 💻 AC 代码（Python）
```python
from typing import List
from collections import defaultdict

class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        preSums = defaultdict(int)
        preSums[0] = 1
        preSum = 0
        res = 0 
        for num in nums:
            preSum += num
            res += preSums[preSum-k]
            preSums[preSum] += 1
        return res
```

## 🧩 关键知识点 & 技巧
- 技巧1：区分概念：**子数组必须连续；子序列可以不连续**，本题是连续子数组。
- 技巧2：公式 `preSum[r] - preSum[l‑1] = k`，变形 `preSum[l‑1] = preSum[r]‑k` 是本题核心。
- 技巧3：初始化 `preSums[0]=1`，处理从数组开头就满足条件的子数组。
- 技巧4：`defaultdict(int)` 计数字典，key不存在自动默认0，省去if‑else判断。

- 易错点：
  1. 忘记初始化 `preSums[0]=1`，统计结果偏小；
  2. 颠倒顺序：先存前缀和再查询，会错误统计自身；
  3. 数组有负数仍然使用滑动窗口，逻辑错误；
  4. 混淆子数组、子序列概念。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 直接套用滑动窗口模板，没有考虑数组存在负数，窗口无法收缩扩张；
2. 缺少前缀和0的初始化，漏掉从数组起始位置的合法子数组；
3. 先把当前preSum加入哈希表，再查询preSum‑k，造成多算；
4. 使用普通字典，key不存在触发KeyError。

**修正思路：**
1. 数组含负数求连续子数组和，优先前缀和+哈希；全正数才适合滑动窗口；
2. 记住初始化`{0:1}`；严格遵守：**先查询，后存入哈希**；
3. 计数场景使用`defaultdict(int)`简化代码。

## ✅ 总结
560 和为K的子数组是前缀和+哈希经典模板题。
利用前缀和差值公式，哈希保存历史前缀和出现频次，一趟遍历完成统计。
> 记忆公式：`preSum[r] − k`；初始化`preSums[0]=1`；先查询，后存值。
> 拓展：和可被K整除的子数组，同样是前缀和+哈希，存储前缀和取模后的频次。
```
