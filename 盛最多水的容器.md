# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：11
- **题目名称**：盛最多水的容器
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/container-with-most-water/
- **刷题日期**：2026\09\03
- **标签**：数组、双指针、贪心

## 📝 题目大意
给定数组 `height`，数组每个元素代表垂线高度。下标代表横坐标。选两条垂线，和x轴形成容器，不能倾斜。
容器容量公式：`容量 = 两条垂线的距离 × min(左边高度，右边高度)`。
求可以装下的最大水量。

> 暴力思路：枚举所有两两组合，$O(n^2)$，大数据会超时。

## 💡 解题思路
**核心思想：左右双指针 + 贪心**
左指针在最左端 `left = 0`，右指针在最右端 `right = len(height)-1`。
- 计算当前容量，更新最大值。
- **移动规则：移动高度更小的那一侧指针**
  - 如果 `height[left] < height[right]`：left向右移动
  - 否则：right向左移动

> 贪心原理：
容器的高度受限于较短的板。
假设左板矮、右板高：此时如果移动右指针，宽度减小，而容器高度最多只能等于左板高度，**容量只会变小，不可能得到更大值**。所以只能移动矮的那一侧，才有可能遇到更高的板子，得到更大容量。

步骤拆解：
1. 初始化 `left = 0`，`right = len(height)-1`，`max_area = 0`。
2. while `left < right`：
   1. 计算当前宽度 `width = right‑left`
   2. 当前面积 `area = width * min(height[left], height[right])`
   3. 更新 `max_area = max(max_area, area)`
   4. 比较两侧高度，移动高度较小的指针；相等时任意移动一侧即可。
3. 循环结束返回 `max_area`。

> ⚠️ 不要同时移动两个指针；不要固定移动左/右。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，双指针最多遍历一遍数组
- **空间复杂度**：$\(O(1)\)$，仅常数变量

## 💻 AC 代码（Python）
```python
from typing import List
class Solution:
    def maxArea(self, height: List[int]) -> int:
        left = 0
        right = len(height) - 1
        max_area = 0
        while left < right:
            width = right - left
            current = width * min(height[left], height[right])
            if current > max_area:
                max_area = current
            if height[left] < height[right]:
                left += 1
            else:
                right -= 1
        return max_area
```

## 🧩 关键知识点 & 技巧
- 技巧1：面积由**宽度和短板高度**共同决定。
- 技巧2：贪心策略：**每次移动高度更小的指针**；移动高的一侧没有机会得到更大面积。
- 技巧3：左右高度相等，left++或者right‑‑都可以，不影响最终结果。

- 易错点：
  1. 错误移动高度更高的指针，无法得到最优解；
  2. 把面积写成 `width * max(height[left], height[right])`；容器高度取短板 min；
  3. 循环条件写成 `left <= right`，同一个点计算面积，面积为0不影响结果，但逻辑无意义。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 使用双重循环暴力枚举，大数据超时；
2. 取 `max` 计算高度，混淆木桶原理；
3. 指针移动规则写反，移动高的那一侧，直接错过最优解。

**修正思路：**
1. 本题最优是双指针贪心，不要暴力；
2. 容器高度取两块板较小值 min；
3. 永远移动矮板那一端。

## ✅ 总结
11 盛最多水的容器，经典双指针贪心题。
> 左右分别放两端；算面积更新最大值；**移动高度较小的指针**；相等随便移一侧。
> 记忆：木桶装水看短板，移动短板才有希望变大。

> 区分：接雨水（42）是单调栈；本题是双指针贪心，不要混淆两道题。
