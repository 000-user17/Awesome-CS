# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：79
- **题目名称**：单词搜索 Word Search
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/word-search/
- **刷题日期**：2026\09\04
- **标签**：二维矩阵 / DFS / 回溯

## 📝 题目大意
给定 `m x n` 的字符网格 `board`，字符串 `word`。判断 word 是否存在网格中。
规则：
1. 单词字母顺序，只能上下左右**相邻格子**（不能斜向）；
2. **同一个格子不能重复使用**，走过的位置不能再次选。

示例：
```
board = [
  ["A","B","C","E"],
  ["S","F","C","S"],
  ["A","D","E","E"]
]
word = "ABCCED" → true
word = "SEE" → true
word = "ABCB" → false
```

>分析：
1. 需要从网格每一个单元格作为起点，开始深度优先搜索DFS；
2. DFS往上下左右四个方向走；
3. 需要标记已经访问过的格子，防止同一轮递归重复走；
4. 回溯：递归返回之后恢复现场，其他起点搜索还可以使用这个格子。
>注意：不能直接开visited二维数组，每次DFS新建数组开销很大；常用技巧：**原地修改board，搜索结束改回来**。

## 💡解题思路
### 核心架构：DFS + 回溯
1. 遍历矩阵每一个位置 `(i,j)`，如果 `board[i][j] == word[0]`，以此位置作为起点启动DFS。
2. DFS参数：当前坐标`i,j`，当前匹配到word的下标`index`。
    - 终止条件：
      ① i,j越界，返回False
      ② board[i][j] != word[index]，字符不匹配，返回False
      ③ `index == len(word)-1`：全部字符匹配完成，返回True
    - 标记当前格子已经访问：把board[i][j]临时改成特殊符号（比如`'#'`），避免本轮递归重复访问。
    - 向上下左右四个方向递归搜索，只要任意一条路径返回True，直接向上返回True。
    - **回溯恢复**：递归回来，把`#`改回原来字符，恢复现场，给别的分支使用。

>原地修改的好处，不需要额外visited数组，空间O(递归深度)，递归深度最大等于word长度。

### 执行逻辑
1. m=行数，n=列数
2. 双重循环遍历矩阵每一个点，如果等于word首字符，调用dfs(i,j,0)
3. dfs一旦返回True，整体直接return True；全部起点都失败返回False。

## ⚙️复杂度分析
- **时间复杂度**：$O(m \cdot n \cdot 4^L)$
m*n是网格单元格数量；L是word长度；每个位置最多4个方向分支，4的L次方。
- **空间复杂度**：$\(O(L)\)$，递归栈深度，最大为单词长度L。

## 💻 AC代码
```python
from typing import List

class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        m = len(board)
        n = len(board[0])
        dirs = [[-1,0],[1,0],[0,-1],[0,1]] #上下左右

        def dfs(i: int, j: int, idx: int) -> bool:
            #越界或者字符不匹配
            if i < 0 or i >= m or j <0 or j >= n or board[i][j] != word[idx]:
                return False
            #匹配到最后一个字符，成功
            if idx == len(word)-1:
                return True
            #标记已经访问，原地修改
            temp = board[i][j]
            board[i][j] = '#'

            #四个方向搜索
            for dx, dy in dirs:
                ni = i + dx
                nj = j + dy
                if dfs(ni, nj, idx + 1):
                    return True
            #回溯，恢复现场
            board[i][j] = temp
            return False

        for i in range(m):
            for j in range(n):
                if board[i][j] == word[0]:
                    if dfs(i,j,0):
                        return True
        return False
```

## 🧩关键知识点 &技巧
1. **回溯必做恢复现场**，原地修改`#`，递归返回复原；如果不复原，后续其他起点搜索会被错误拦截。
2. 四个方向用方向数组统一写，不要写4份重复dfs代码。
3. 提前终止：只要某一条dfs路径返回True，直接层层向上return True剪枝，不继续搜别的分支。
4. 不能斜着走，只有上下左右。
5. 也可以额外开辟visited二维数组记录访问；但原地修改更省空间，面试常用。

## ❌错题反思 /踩坑记录
1. **忘记回溯恢复board[i][j]**，标记为#之后不改回来，后面搜索全部失败。本题最高频错误。
2. 终止条件写错：`idx == len(word)`，而不是`len(word)-1`，会索引越界。
3. 没有先判断边界，直接访问`board[i][j]`，i,j越界直接报错。
4. 找到一条路径之后没有及时return True，继续循环，做无效搜索。
5. 允许不同起点重复用同一个格子，**只是同一条搜索路径不能重复**，不要全局把格子永久禁用。
6. 方向数组写错，加入对角线方向。

**避坑要点**
- 匹配到最后一位下标是`len(word)-1`；
- 原地标记后，无论四个方向成功失败，**必须回溯复原字符**；
- 越界判断写在dfs最开头。

## ✅总结
矩阵回溯经典中等题。
遍历每一个起点，DFS向四个方向探索；原地标记访问，递归结束回溯恢复现场；匹配到单词末尾返回true。
时间是指数级，依靠剪枝优化。

>补充：小优化：可以提前统计board字符计数，如果word里面有某个字符board完全没有，可以直接返回False做剪枝。

如果你需要，我可以给你手推一遍样例`ABCCED`的递归过程。
