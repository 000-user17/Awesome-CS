# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：448
- **题目名称**：找到所有数组中消失的数字 Find All Numbers Disappeared in an Array
- **题目难度**：简单
- **题目链接**：https://leetcode.cn/problems/find-all-numbers-disappeared-in-an-array/
- **刷题日期**：2026\08\14
- **标签**：数组 / 原地标记

## 📝 题目大意
给定长度为 n 的数组 nums，数组元素取值范围 `[1, n]`。找出区间 `[1, n]` 中所有未出现在数组中的数字，并以列表形式返回。

> 思路对比
> 方案1：哈希集合，实现简单，需要额外 \(O(n)\) 空间；
> 方案2：原地正负标记（当前代码实现），\(O(1)\) 额外空间（不计返回数组），修改原数组。

## 💡 解题思路
利用数组下标与数字的映射关系：数字 x 对应下标 `x-1`。
1. 遍历数组，取出当前元素绝对值 `abs(nums[i])`，得到对应下标 `idx-1`；
2. 如果下标位置数值大于0，将其变为负数，用来标记数字 `idx` 已经存在；
3. 完成一轮标记后再次遍历数组：
   下标 `i` 处数值仍然大于0 → 代表数字 `i+1` 在原数组中没有出现，加入结果。

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(n)\)$，两轮线性遍历数组
- **空间复杂度**：$\(O(1)\)$，仅常数临时变量，原地修改原数组（返回结果数组不计入空间开销）

## 💻 AC 代码
```python
from typing import List
class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        n = len(nums)
        ret = []
        for i in range(0, n):
            idx = abs(nums[i])
            if nums[idx - 1] > 0:
                nums[idx - 1] *= -1
        
        for i in range(0, n):
            if nums[i] > 0:
                ret.append(i + 1)
        return ret
```

## 🧩 关键知识点 & 技巧
1. 值域 `[1,n]` 和数组下标范围 `[0,n-1]` 天然匹配，适合使用下标映射做原地标记；
2. 计算下标必须使用 `abs()`，避免前面被修改为负数的元素造成下标错误；
3. 负数仅作为“存在标记”，不参与数值大小计算；
4. 最终收集答案时，下标 `i` 对应的数字是 `i+1`，注意偏移量。

## ❌ 错题反思 / 踩坑记录
1. 忘记取绝对值，直接使用负数计算下标，出现负下标报错；
2. 收集答案时直接存入 `i`，遗漏 `+1` 偏移；
3. 不理解原地标记思想，只会使用哈希集合，无法满足空间优化要求；
4. 重复多次翻转正负，增加不必要运算，只需第一次见到时置负即可。

## ✅ 总结
原地映射经典数组题。
核心要点：**下标映射数字，正负符号充当存在标记，不额外开辟空间。**
