# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：53
- **题目名称**：最大子数组和 Maximum Subarray
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/maximum-subarray/](https://leetcode.cn/problems/maximum-subarray/)
- **刷题日期**：2026\09\14
- **标签**：动态规划 DP / 贪心 / 分治

## 📝 题目大意
整数数组 `nums`，找出**连续子数组**的最大和，子数组至少包含一个元素。

示例：
`nums = [-2,1,-3,4,-1,2,1,-5,4]`
输出：6
解释：连续子数组 `[4,-1,2,1]` 的和最大为6

> 经典Kadane（卡登）算法，本质动态规划，也可以贪心理解。

## 💡 解题思路
### 思路1：动态规划（Kadane算法）
#### 状态定义
`dp[i]`：以 `nums[i]` **结尾**的连续子数组的最大和。
> 重点：必须以i结尾，保证子数组连续性。

#### Base Case
`dp[0] = nums[0]`，第一个元素结尾的子数组只能是自己。

#### 状态转移方程
$$dp[i] = \max(nums[i],\ dp[i-1]+nums[i])$$
两种选择：
1. 把当前元素接在前一个子数组后面：`dp[i-1]+nums[i]`
2. 前面的子数组拖累总和，**丢弃前面，从当前元素重新开始**：`nums[i]`

答案就是整个dp数组的最大值。

> 空间优化：dp[i]只依赖dp[i-1]，不需要完整数组，只用一个变量保存上一个值，\(O(1)\)空间。

### 思路2：贪心（和Kadane等价）
维护当前子数组和`cur_sum`：
- 如果 `cur_sum < 0`：前面的和是负数，舍弃，`cur_sum = nums[i]`
- 如果 `cur_sum >=0`：继续累加 `cur_sum += nums[i]`
- 每一步更新全局最大`max_sum`

### 思路3：分治（了解即可，面试一般不写）
把数组二分，最大子数组只有三种情况：
1. 在左半区间
2. 在右半区间
3. 跨左右中点
递归求解三部分取最大值，时间\(O(n\log n)\)，代码复杂。

## ⚙️ 复杂度分析
Kadane（DP空间压缩版）
- **时间复杂度**：$\(O(n)\)$，遍历一次数组
- **空间复杂度**：$\(O(1)\)$

完整DP数组版本
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(n)\)$

分治版本
- **时间复杂度**：$O(n\log n)$
- **空间复杂度**：$O(\log n)$，递归栈

## 💻 AC 代码
### 空间压缩DP / Kadane（面试首选）
```python
class Solution:
    def maxSubArray(self, nums: list[int]) -> int:
        cur = max_sum = nums[0]
        for num in nums[1:]:
            cur = max(num, cur + num)
            max_sum = max(max_sum, cur)
        return max_sum
```

### 完整DP数组版本（便于理解DP思想）
```python
class Solution:
    def maxSubArray(self, nums: list[int]) -> int:
        n = len(nums)
        dp = [0]*n
        dp[0] = nums[0]
        for i in range(1,n):
            dp[i] = max(nums[i], dp[i-1]+nums[i])
        return max(dp)
```

## 🧩 关键知识点 & 技巧
1. dp定义**必须以i结尾**，是这道DP的核心，这样才能保证子数组连续；
2. 全负数数组依然可以正确处理，例如`[-3,-1,-2]`，答案取最大的单个元素-1；
3. Kadane是DP的空间优化写法，很多地方直接叫贪心，底层是DP；
4. 子数组=连续；子序列=不连续，不要搞混。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. dp定义写错：`dp[i]`表示0~i区间最大和，这样无法写出正确转移方程；
2. 初始化max_sum=0，遇到全负数组直接出错；必须初始化为nums[0]；
3. 分不清子数组（连续）和子序列（不连续）。

**避坑要点**
> dp[i]：以i结尾的连续子数组最大和；
> 如果前面累加和是负数，不如从头重新开始。

## ✅ 总结
53是DP经典模板题，Kadane算法。核心：对每个位置，选择接上前面子数组或者单独新开子数组，记录全局最大值。
关联题目：918 环形子数组的最大和
