# LeetCode 刷题记录

## 📌 题目信息

- **题目编号**：234

- **题目名称**：回文链表

- **题目难度**：简单

- **题目链接**：https://leetcode.cn/problems/palindrome-linked-list/description/?envType=problem-list-v2&envId=2cktkvj&

- **刷题日期**：2026\08\10

- **标签**：链表

## 📝 题目大意

给你一个单链表的头节点 head ，请你判断该链表是否为回文链表。如果是，返回 true ；否则，返回 false 。

## 💡 解题思路

**核心思想：** 寻找到回文链表的中间位置，朝两侧有依次检查是否满足会问条件（回文：1-2-3-2-1 从左到右和从右到左完全相同 ）

**步骤拆解：**

1. 寻找到回文链表的中间位置

2. 逆转左边的链表

3. 确认左右链表的起始点，依次查询是否满足回文条件

## ⚙️ 复杂度分析

- **时间复杂度**：O\( \)

- **空间复杂度**：O\( \)


## 💻 AC 代码（Python）

```python

# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def isPalindrome(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: bool
        """
        if not head or not head.next:
            return True

        pre = None
        slow = head
        fast = head
        while fast.next and fast.next.next:
            pre = slow
            slow = slow.next
            fast = fast.next.next
        
        first = None
        second = slow.next
        if not fast.next:
            first = self.reverse(head, pre)
        else:
            first = self.reverse(head, slow)
        while first or second:
            if not first or not second or first.val != second.val:
                return False
            first = first.next
            second = second.next
        return True
    

    def reverse(self, head, tail):
        p = head
        q = head.next
        r = None
        while r != tail:
            p.next = r
            r = p
            p = q
            q = q.next
        return r
    
```

## 🧩 关键知识点 \& 技巧

- 技巧1：逆转单链表，固定方法 p, q, r滑动

- 技巧2：奇数和偶数情况区别，快慢节点，奇数节点的逆转链表的起始节点应该是慢节点的前一个，后半部分的链表的起始节点时慢节点的下一个节点

- 易错点：空节点的特殊情况满足回文条件

## ❌ 错题反思 / 踩坑记录



**错误原因：**

**修正思路：**

## ✅ 总结
链表操作需要谨慎 可以一个个打印出来或者手画来检查是否和算法思路一致

