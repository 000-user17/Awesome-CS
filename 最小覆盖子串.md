# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：76
- **题目名称**：最小覆盖子串 Minimum Window Substring
- **题目难度**：困难
- **题目链接**：[https://leetcode.cn/problems/minimum-window-substring/](https://leetcode.cn/problems/minimum-window-substring/)
- **刷题日期**：2026\09\14
- **标签**：滑动窗口 / 双指针 / 哈希表

## 📝 题目大意
给两个字符串 `s`、`t`，在 `s` 中找到一个最短子串，**包含 t 的所有字符（字符出现次数也要满足）**。
找不到返回 `""`；题目保证如果存在解，答案唯一。

示例：
`s = "ADOBECODEBANC", t = "ABC"`
输出：`"BANC"`

> 核心：不是只要出现字符，**每个字符的数量要 ≥ t 里的数量**。
> 这是典型滑动窗口（双指针）模板题。

## 💡 解题思路
### 滑动窗口核心思想
用左右指针 `left`、`right` 表示窗口 `s[left...right]`
1. `right` 不断向右扩张窗口，直到窗口内字符满足：包含 t 全部字符、数量达标。
2. 满足条件后，尝试移动 `left` 收缩窗口，**尽可能缩小窗口长度**，同时持续检查是否仍然满足条件；收缩过程记录最小窗口。
3. 窗口一旦不满足条件，再次移动 right 扩张。

### 两个哈希表
- `need`：记录 t 中每个字符需要多少个。
- `window`：记录当前窗口里各个字符的数量。
- `valid`：计数器，表示**满足数量要求的字符个数**。`valid == len(need)` 代表窗口已经覆盖 t。

### 流程步骤
1. 构建 need 字典，统计 t 的字符频次。
2. left=0，valid=0，记录最小子串起始索引和长度。
3. right 遍历 s：
    - 取出字符 c = s[right]
    - 如果 c 在 need：window[c] += 1；当 window[c] == need[c]，valid +=1
    - 当 valid == len(need)：窗口满足条件，进入收缩阶段
        - 更新最小窗口（当前窗口更小就更新）
        - 尝试左移 left：d = s[left]
        - 如果 d 在 need：window[d] -=1；若 window[d] < need[d]，valid -=1，窗口不再满足，退出收缩循环
        - left += 1
4. 遍历结束，根据记录的最小窗口截取字符串；无满足窗口返回 ""

## ⚙️ 复杂度分析
- **时间复杂度**：$\(O(m+n)\)$，m是s长度，n是t长度；左右指针最多各走一遍s
- **空间复杂度**：$O(|\Sigma|)$，字符集大小，最多英文字母，常数级

## 💻 AC 代码
```python
class Solution:
    def minWindow(self, s: str, t: str) -> str:
        from collections import defaultdict
        need = defaultdict(int)
        window = defaultdict(int)
        for c in t:
            need[c] += 1
        
        left = 0
        valid = 0
        # 记录最小子串起始位置和长度
        start = 0
        min_len = float('inf')

        for right in range(len(s)):
            c = s[right]
            if c in need:
                window[c] += 1
                if window[c] == need[c]:
                    valid += 1
            
            # 窗口满足条件，收缩左边界
            while valid == len(need):
                cur_len = right - left + 1
                if cur_len < min_len:
                    min_len = cur_len
                    start = left
                # 移出左指针字符
                d = s[left]
                if d in need:
                    if window[d] == need[d]:
                        valid -= 1
                    window[d] -= 1
                left += 1
        
        if min_len == float('inf'):
            return ""
        return s[start: start + min_len]
```

## 🧩 关键知识点 & 技巧
1. `valid` 是优化点：不用每次循环对比两个字典，只统计满足数量的字符数量；
2. **扩张用right，满足条件才收缩left**，滑动窗口通用模板；
3. 记录最小窗口不要直接存字符串，只存起始下标+长度，减少字符串切片开销；
4. 要考虑重复字符，t="AABC"，窗口必须至少2个A，1个B，1个C。

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 只判断字符是否存在，不判断字符计数，t="AA"，s="AB"，误以为匹配；
2. valid 更新时机写错：window[c] > need[c] 时不能增加 valid；只有相等时 valid+1；
3. 收缩的时候，window[d] 减少之后，只有 window[d] < need[d]，valid才减一；
4. 更新最小窗口位置写在while外面，漏掉收缩阶段更小的窗口；
5. 边界：s长度小于t，直接返回空串。

**避坑要点**
> valid增减条件是这道题最容易写错的地方：
> - window[c] == need[c] → valid +=1
> - window[d] 从等于 need[d] 变成小于 → valid -=1

## ✅ 总结
76是滑动窗口经典困难模板题。固定套路：右指针扩窗口，满足条件左指针缩窗口，用valid减少哈希表比对开销。
同类滑动窗口题：3.无重复字符的最长子串、438.找到字符串中所有字母异位词、567.字符串的排列。
