# LeetCode 刷题记录（补充修正，重点：入参 m,n）
## 📌 题目信息
- **题目编号**：62
- **题目名称**：不同路径 Unique Paths
- **题目难度**：中等
- **题目链接**：[https://leetcode.cn/problems/unique-paths/](https://leetcode.cn/problems/unique-paths/)
- **刷题日期**：2026\09\14
- **标签**：动态规划 DP / 组合数学

## 📝 题目大意
函数入参 `m` = **行数**，`n` = **列数**，网格 m行n列。
机器人位于网格左上角 `(0,0)`，目标右下角 `(m-1, n-1)`。每次只能向下、向右走一步，求总路径数量。
> 重点：入参 m,n 代表**行数、列数**，不是坐标！

示例：`m = 3, n = 7` → 3行7列网格，输出 28
> 总共要向下走 `m-1 = 2` 次，向右走 `n-1 =6` 次，总步数 `(m-1)+(n-1) = m+n-2`

## 💡 解题思路
### 思路1：动态规划
#### 状态定义
`dp[i][j]`：走到第 i 行，第 j 列格子的路径总数
i范围：`0 ~ m-1`；j范围：`0 ~ n-1`

#### Base Case
- 第0行（i=0）：只能一路向右，全部 = 1
- 第0列（j=0）：只能一路向下，全部 = 1

#### 转移方程
$$dp[i][j] = dp[i-1][j] + dp[i][j-1]$$
`dp[i-1][j]`：上面一行（i-1,j）向下走到当前
`dp[i][j-1]`：同一行左边（i,j-1）向右走到当前

### 思路2：组合数学
总步数 $total = m+n-2$，从中选 `m-1` 步向下：
$$C_{m+n-2}^{m-1}$$
> 等价 $C_{m+n-2}^{n-1}$，两个结果一样

## ⚙️ 复杂度分析
二维DP：
- 时间：$\(O(mn)\)$
- 空间：$\(O(mn)\)$

一维DP压缩版：
- 时间：$\(O(mn)\)$
- 空间：$\(O(n)\)$

组合数：
- 时间：$O(\min(m,n))$
- 空间：$\(O(1)\)$

## 💻 AC 代码（函数入参 m,n）
### 一维DP（推荐）
```python
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        # m行，n列
        dp = [1] * n
        # i从1到m-1行
        for i in range(1, m):
            for j in range(1, n):
                dp[j] = dp[j] + dp[j-1]
        return dp[-1]
```

### 二维DP
```python
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        dp = [[1]*n for _ in range(m)]
        for i in range(1, m):
            for j in range(1, n):
                dp[i][j] = dp[i-1][j] + dp[i][j-1]
        return dp[m-1][n-1]
```

### 组合数学
```python
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        from math import comb
        return comb(m + n - 2, m - 1)
```

## 🧩 关键知识点 & 技巧
1. **入参坑点：m是行数，n是列数**
    - 右下角坐标是 `(m-1, n-1)`，不要写成 `(m,n)`
    - 总向下步数：`m-1`，总向右：`n-1`
2. 一维dp数组长度等于**列数n**，因为一行有n个格子；
3. 循环 i 遍历**行数**：i ∈ [1, m-1]；j遍历**列数**：j ∈ [1, n-1]

## ❌ 错题反思 / 踩坑记录
**高频错误**
1. 混淆m,n含义：误以为m是列，n是行，组合数下标写反（虽然C(a,b)=C(a,a−b)结果不变，但逻辑错）；
2. 循环边界写成 `range(1,m+1)` 越界；
3. 把终点坐标写成(m,n)，不是(m-1,n-1)；
4. DFS暴力递归，m,n偏大直接超时。

**避坑要点**
> m=行数，n=列；
> 向下次数 = m-1；向右次数 = n-1。

## ✅ 总结
入参 m 行 n 列。DP核心：每个位置路径 =上方+左方。可以一维压缩。数学本质组合数。
关联题目：63 不同路径II（增加障碍物）

要不要继续整理 63 不同路径II？
