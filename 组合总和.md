# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：39
- **题目名称**：组合总和 Combination Sum
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/combination-sum/
- **刷题日期**：2026\08\31
- **标签**：回溯 / DFS / 组合问题
## 📝 题目大意
给你一个**无重复元素**的整数数组 `candidates` 和一个目标整数 `target`，找出 `candidates` 中可以使数字和为目标数 `target` 的所有不同组合，并以列表形式返回。
- `candidates` 同一个数字可以**无限制重复选取**；
- 组合顺序无关：`[2,3]`、`[3,2]`视为同一个组合，不能重复输出；
- 只要选取元素数量不同，就视为不同组合；
- 保证合法组合数量少于150个。

> 重点难点：
> 1. 元素可无限复用，区别于普通0‑1组合；
> 2. 需要避免生成顺序不同但元素相同的等价组合；
> > 解法采用二叉树选/不选模型：对当前下标`start`分出两条分支
> > - 选当前元素：下一轮递归下标不变，允许再次选取自己；
> > - 不选当前元素：下标+1，向后搜索，不再回头看前面元素，天然去重。
> 3. 数组排序后可做剪枝：当前数字加上已有总和已经超过target，后面数字更大，直接返回剪枝。

示例：
`candidates = [2,3,6,7], target = 7`
输出：`[[2,2,3],[7]]`

## 💡 解题思路
### 核心思想：二叉树DFS回溯（选 / 不选模型）
参数定义：
- `path`：当前正在收集的组合路径；
- `start`：当前处理的数组下标；
- `_sum`：path当前累加总和。

递归终止条件：
1. `_sum == target`：找到合法组合，拷贝path加入结果集，return；
2. `_sum > target`：总和超过目标，直接return；
3. `start >= len(candidates)`：下标越界，搜索完毕，return。

剪枝逻辑：
数组升序排序后，如果 `_sum + candidates[start] > target`，后续数字只会更大，直接return剪枝。

递归分支：
1. **选当前数字**：path追加`candidates[start]`；递归时start不变（允许重复选）；递归返回执行pop回溯；
2. **不选当前数字**：不修改path，直接递归 `start+1`，跳过当前元素向后搜索。

> 示例推演：`candidates=[2,3,6,7], target=7`
1. start=0，_sum=0，path=[]
    - 选2：path=[2], _sum=2，继续start=0；
        - 选2：path=[2,2], _sum=4，继续start=0；
            - 选2：_sum+2=6，path=[2,2,2],_sum=6；再选2总和>7剪枝返回；不选2，start=1；
            - start=1选3：path=[2,2,3],_sum=7，保存答案`[2,2,3]`；回溯；
    - 不选2，start=1；
2. start=1，选3，总和达不到7，全部剪枝返回；不选3，start=2；
3. start=2，6>7‑0，剪枝；不选6，start=3；
4. start=3，选7，path=[7],_sum=7，保存答案`[7]`。

最终结果：`[[2,2,3],[7]]`

## ⚙️ 复杂度分析
- **时间复杂度**：$O(2^M)$，M为target大小，属于子集树搜索；题目保证组合数小于150，不会超时。
- **空间复杂度**：$\(O(target)\)$，递归栈深度最多为`target/min(candidates)`，不计输出结果集。

## 💻 AC 代码
```python
class Solution:
    def combinationSum(self, candidates: List[int], target: int) -> List[List[int]]:
        n = len(candidates)
        candidates.sort()
        res = []
        def dfs(path: List[int], start: int, _sum: int):
            if _sum == target:
                res.append(path[:])
                return
            if _sum + candidates[start] > target:
                return
            path.append(candidates[start])
            dfs(path, start, _sum + candidates[start])
            path.pop()
            if start + 1 < n:
                dfs(path, start + 1, _sum)
        dfs([], 0, 0)
        return res
```

> for循环标准回溯模板
```python
from typing import List
class Solution:
    def combinationSum(self, candidates: List[int], target: int) -> List[List[int]]:
        res = []
        candidates.sort()
        def backtrack(start: int, remain: int, path: List[int]):
            if remain == 0:
                res.append(path[:])
                return
            for i in range(start, len(candidates)):
                num = candidates[i]
                if num > remain:
                    break
                path.append(num)
                backtrack(i, remain - num, path)
                path.pop()
        backtrack(0, target, [])
        return res
```

## 🧩 关键知识点 & 技巧
1. 二叉树模型两条分支：**选（递归start不变）、不选（递归start+1）**；
    - 允许重复选取：选分支递归传`start`；
    - 不能重复选取：选分支递归传`start+1`；
2. `start`下标向后搜索，禁止回头选前面元素，天然避免`[2,3] [3,2]`等价重复组合；
3. path存入结果必须`path[:]`拷贝副本，直接append(path)会被后续pop回溯修改；
4. 数组先sort升序，利用`_sum + candidates[start] > target`提前剪枝，减少无效递归；
5. dfs内部必须处理`start >= n`边界，防止访问`candidates[start]`发生下标越界；
6. 两套模板性能等价：二叉树选不选、for循环回溯，只是搜索树表达方式不同。

> 同源题目对比记忆
> - 39组合总和：元素无重，可无限选；递归选分支传`i`
> - 40组合总和Ⅱ：元素有重复，每个只能选一次；递归选分支传`i+1`，还要去重剪枝
> - 46全排列：无start下标，使用used数组标记，元素选一次，允许打乱顺序

## ❌ 错题反思 / 踩坑记录
**曾经写代码出现的错误**
1. 第二个dfs写在pop之前，path还带着刚append的元素，并且`_sum`重复叠加当前数字，path真实总和和参数_sum不一致；
2. dfs函数内部没有`start >= n`终止条件，依靠外层if判断下标，函数鲁棒性差，容易下标越界；
3. 缺少`_sum>target`终止条件，仅靠剪枝判断，遇到不排序输入会出现死递归；
4. 选分支错误传入`start+1`，变成每个元素只能选一次，无法生成重复选取的组合；
5. 保存答案直接`res.append(path)`，没有拷贝，结果列表全部为空；
6. 忘记排序就做剪枝判断，数组无序时剪枝逻辑错误，剪掉合法路径。

**避坑要点**
- 选分支递归完必须pop，再执行不选分支；两条分支互斥；
- dfs内部一定要写越界、超和终止条件，不要完全依赖外部判断；
- 剪枝的前提条件是数组升序sort；
- path保存结果务必拷贝副本。

## ✅ 总结
39组合总和是回溯经典模板题，二叉树「选/不选」模型逻辑直观。
核心两点：
1. start下标控制只向后搜索，消除等价重复组合；
2. 选当前数字分支递归保持start不变，实现元素无限复用。
> 面试中for循环版本写的更多，不容易下标越界；二叉树版本适合理解决策树逻辑，两套都要掌握。
