# LeetCode 刷题记录

## 📌 题目信息
- **题目编号**：200
- **题目名称**：岛屿数量
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/number-of-islands/description/?envType=problem-list-v2&envId=2cktkvj&
- **刷题日期**：2026\08\12
- **标签**：网格 DFS / 连通块 / 深度优先搜索

## 📝 题目大意
给你一个由 `'1'`（陆地）和 `'0'`（水域）组成的二维网格。
一座岛屿由相邻的陆地构成，相邻指水平或垂直方向（上下左右），斜向不算。
求网格中一共有多少座岛屿。

核心理解：遇到一块未访问的陆地，启动一次DFS，把整块连通陆地全部标记为已访问，启动DFS的次数就是岛屿总数。

## 💡 解题思路
**核心思想：网格连通块 DFS（淹岛思路，原地修改，无需额外visited数组）**
**步骤拆解：**
1. 获取网格行数m、列数n；
2. 遍历网格中每一个单元格；
3. 如果当前单元格是陆地 `'1'`：
   - 岛屿计数 +1；
   - 启动DFS，将这块连通区域所有陆地修改为水域 `'0'`（相当于标记已访问）；
4. DFS逻辑：
   - 边界判断：坐标超出网格范围直接返回；
   - 当前是水域 `'0'`，直接返回；
   - 将当前陆地修改为 `'0'`；
   - 递归向上下左右四个方向继续搜索。

> 注意：本题是无向连通块遍历，**不需要三色标记**！三色标记只用于有向图判环，照搬会造成多余判断、超时。

## ⚙️ 复杂度分析
- **时间复杂度**：O(m * n)，m网格行数，n列数，每个格子最多访问一次
- **空间复杂度**：O(m * n)，最坏情况全是陆地，递归栈深度达到 m*n

## 💻 AC 代码（Python）
```python
from typing import List

class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        m = len(grid)
        if m == 0:
            return 0
        n = len(grid[0])
        dirs = [(-1, 0), (1, 0), (0, -1), (0, 1)]

        def dfs(x, y):
            # 越界或者当前是水域，直接返回
            if x < 0 or x >= m or y < 0 or y >= n or grid[x][y] == '0':
                return
            # 原地淹掉陆地，替代visited数组
            grid[x][y] = '0'
            for dx, dy in dirs:
                dfs(x + dx, y + dy)

        count = 0
        for i in range(m):
            for j in range(n):
                if grid[i][j] == '1':
                    count += 1
                    dfs(i, j)
        return count
```

## 💻 AC 代码优化空间（Python）
```python
class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        m = len(grid)
        n = len(grid[0])
        if m == 0 or n == 0:
            return 0
        dirs = [(-1, 0), (1, 0), (0, -1), (0, 1)]

        def dfs(x, y):
            if x >= m or x < 0 or y >= n or y < 0:
                return
            
            if grid[x][y] == '0':
                return
            
            grid[x][y] = '0'
            for dx, dy in dirs:
                dfs(x + dx, y + dy)
        
        cnt = 0
        for i in range(m):
            for j in range(n):
                if grid[i][j] == '0':
                    continue
                dfs(i, j)
                cnt += 1
        return cnt


```

## 🧩 关键知识点 & 技巧
- 技巧1：网格遍历标准方向数组 `dirs = [上下左右]`，不要手写多组dfs，代码简洁高效
- 技巧2：连通块题目可以**原地修改网格**，省去开辟visited二维数组，节省内存
- 技巧3：区分场景：无向连通块只用二值标记/原地覆盖；有向图判环才使用三色标记
- 易错点：
  1. 错误复用「三色标记」逻辑，增加大量无用判断，引发超时；
  2. 方向遍历写法混乱，产生大量无效递归调用；
  3. 忘记判断网格为空的边界情况；
  4. 混淆 `'1'` 字符串和数字 `1`，判断条件出错。

## ❌ 错题反思 / 踩坑记录
**错误原因：**
1. 照搬课程表有向图三色标记写法到连通块问题，多余 `visited = [[0,1,2]]` 判断，造成大量无效运算超时；
2. 方向遍历写法简陋，无条件执行四次dfs，大量无效函数调用；
3. DFS缺少提前拦截水域的逻辑，函数进入后多一层判断开销。

**修正思路：**
1. 区分题型：连通块遍历不需要三色标记；
2. 使用标准四方向数组简化遍历；
3. 采用原地修改网格代替visited数组，优化运行速度；
4. 在dfs入口统一完成越界、水域判断。

## ✅ 总结
岛屿数量属于经典**二维网格连通块问题**。
核心模式：发现未访问陆地 → DFS/BFS清除整块连通陆地，统计启动搜索次数。
DFS写法简洁，但超大网格存在递归栈溢出风险；遇到超大测试用例可改用BFS队列实现。
原地修改网格是本题最优写法，省去额外空间开销，也是LeetCode网格类题目常用优化手段。
