# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：309
- **题目名称**：最佳买卖股票时机含冷冻期
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-with-cooldown/
- **刷题日期**：2026\08\20
- **标签**：动态规划 / 数组

## 📝 题目大意
给定数组 `prices`，`prices[i]` 表示第 i 天股票价格。可以无限次完成交易，但是**卖出股票后，第二天无法买入股票（冷冻期 1 天）**。
可以不进行任何交易，求能获取的最大利润。
> 重点难点：卖出之后下一天是冷冻期，不能买入；冷冻期过后恢复正常；可以同时持有最多一股股票。
> 示例：`prices = [1,2,3,0,2]`，交易：1买入，2卖出，冷冻，0买入，2卖出，最大利润 3。

## 💡 解题思路
### 思路：动态规划
定义三种状态，表示**第 i 天结束之后的状态**
- `f[i][0]`：第 i 天结束，**持有股票**的最大利润
- `f[i][1]`：第 i 天结束，**今天刚卖出，处于冷冻期**的最大利润
- `f[i][2]`：第 i 天结束，**不持有股票，且不在冷冻期**的最大利润

**状态转移**
1. 今天持有股票：
要么前一天就持有；要么前一天不在冷冻期，今天买入。
$$f[i][0]=\max(f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][0],\; f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][2]\(f[i][1]=f[i‑1][0] + prices[i]\)prices[i])$$
> ❗不能从冷冻期`f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][1]`买入，卖出次日禁止买。

2. 今天刚卖出（冷冻期）：
只能是昨天持有，今天卖出。
$$f[i][1]=f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][0] + prices[i]$$

3. 今天不持有、非冷冻：
要么昨天就非冷冻；要么昨天是冷冻期，今天解冻。
$$f[i][2]=\max(f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][2],\; f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][1])$$

**初始化（第0天）**
- `f[0][0] = -prices[0]`：第一天买入，持有股票
- `f[0][1] = 0`：第一天不可能卖出
- `f[0][2] = 0`：第一天不操作，不持股非冷冻

**答案**
最后一天不能持有股票，取刚卖出、非冷冻两者最大值：$\max(f[n\(f[i][1]=f[i‑1][0] + prices[i]\)1][1],f[n\(f[i][1]=f[i‑1][0] + prices[i]\)1][2])$

> 空间优化观察：每一步只依赖 i\(f[i][1]=f[i‑1][0] + prices[i]\)1 的全部状态，可以用滚动变量代替二维数组，把空间由 $\(O(n)\)$ 降到 $\(O(1)\)$。
> 变量：`hold`持有；`freeze`今日卖出冷冻；`un_freeze`不持股非冷冻。

## ⚙️ 复杂度分析
### 二维DP常规版本
- **时间复杂度**：$\(O(n)\)$，遍历一次价格数组
- **空间复杂度**：$\(O(n)\)$，DP二维数组

### 滚动变量优化版本
- **时间复杂度**：$\(O(n)\)$
- **空间复杂度**：$\(O(1)\)$，常数变量

## 💻 AC 代码
### ① 常规二维DP版本
```python
from typing import List
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        if not prices:
            return 0
        n = len(prices)
        # f[i][0]持有；f[i][1]今日卖出冷冻；f[i][2]不持股非冷冻
        f = [[0] * 3 for _ in range(n)]
        f[0][0] = -prices[0]

        for i in range(1, n):
            f[i][0] = max(f[i-1][0], f[i-1][2] - prices[i])
            f[i][1] = f[i-1][0] + prices[i]
            f[i][2] = max(f[i-1][2], f[i-1][1])
        
        return max(f[-1][1], f[-1][2])
```

### ② 滚动变量空间优化版本
```python
from typing import List
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        if not prices:
            return 0
        hold = -prices[0]
        freeze = 0
        un_freeze = 0

        for p in prices[1:]:
            # 必须全部先算新状态，不能原地覆盖旧值
            new_hold = max(hold, un_freeze - p)
            new_freeze = hold + p
            new_unfreeze = max(un_freeze, freeze)
            hold, freeze, un_freeze = new_hold, new_freeze, new_unfreeze
        
        return max(freeze, un_freeze)
```

## 🧩 关键知识点 & 技巧
1. 状态定义是**当天结束后的状态**，不是当天一定发生买卖；
2. 冷冻期规则：**卖出操作完成后的下一天禁止买入**；卖出当天记为冷冻；隔一天解冻；
3. 从非冷冻状态才能执行买入；不能从冷冻状态买入；
4. 滚动优化时，一定要全部计算`new_xxx`新状态，再批量赋值，不要一边算一边覆盖旧变量；
5. 最终结果**一定不能取hold**，手上持有股票没有卖出，不算实际利润。

> 股票系列对比记忆
> - 121：最多1次交易 hold / not_hold
> - 122：无限次交易 hold / not_hold
> - 309：无限次+冷冻期 hold / freeze / un_freeze 三状态

## ❌ 错题反思 / 踩坑记录
**常见错误**
1. 状态转移`f[i][0]`写成 `max(f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][0], f[i\(f[i][1]=f[i‑1][0] + prices[i]\)1][1]\(f[i][1]=f[i‑1][0] + prices[i]\)prices[i])`，允许冷冻期买入，逻辑错误；
2. 滚动DP直接原地修改hold，后面计算使用已经被覆盖的旧值，结果错乱；
3. 返回结果包含hold状态，把持有股票当成利润；
4. 初始化`f[0][1]`设为`\(f[i][1]=f[i‑1][0] + prices[i]\)prices[0]`，第一天不可能卖出。

**避坑要点**
- 分清：冷冻是卖出之后带来的约束，不是买入；
- DP数组每个状态代表当天结束；转移只依赖前一天；
- 允许全程不交易，利润最小为0。

## ✅ 总结
本题属于股票DP中等变种，由二元状态扩展为三元状态。
二维DP逻辑直观适合理解；面试写滚动变量版本空间最优。
核心约束：**冷冻期只能从非冷冻状态买入**，这点是本题和普通无限交易股票题最大区别。答案只能在不持有股票的两个状态中取最大值。
