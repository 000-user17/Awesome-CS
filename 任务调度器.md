# LeetCode 刷题记录
## 📌 题目信息
- **题目编号**：621
- **题目名称**：任务调度器
- **题目难度**：中等
- **题目链接**：https://leetcode.cn/problems/task-scheduler/
- **刷题日期**：2026\09\07
- **标签**：贪心/堆/数学

## 📝 题目大意
给你一个用字符数组 `tasks` 表示的 CPU 需要执行的任务列表，用字母 A 到 Z 表示，以及一个冷却时间 n。每个周期或时间间隔允许完成一项任务。任务可以按任何顺序完成，但有一个限制：两个**相同种类**的任务之间必须有长度为 n 的冷却时间。
返回完成所有任务所需要的**最短时间间隔**。

> 注意：时间片可以空闲(idle)，空闲也占用时间。

## 💡 解题思路
本题有两种主流解法：**数学公式法（最优）**、**大顶堆模拟调度法（仿真过程）**

### 方法一：数学公式法
**核心思想：**
整个调度的瓶颈来自**出现次数最多的任务**，用高频任务构建调度骨架，得到冷却约束带来的理论下界；同时总时间不能小于任务总数量，最终取两者最大值。

**步骤拆解：**
1. 统计各个任务出现频次，得到最大频次 `max_cnt`；统计有多少种任务达到最大频次 `max_task_num`。
2. 计算骨架长度：`frame = (max_cnt - 1) * (n + 1) + max_task_num`
    - `max_cnt‑1`：前 max_cnt‑1 轮完整块；每块长度 `n+1`（执行一次高频任务 + n个冷却槽）
    - `+ max_task_num`：最后一轮，只摆放全部最高频任务，不再需要后面的冷却空位。
3. 两个下界：
    - L1：`frame`，冷却规则约束的最小时间（允许idle空闲）
    - L2：`len(tasks)`，所有任务至少各占一个时间片
4. 最终答案取两者较大值：`ans = max(len(tasks), frame)`
    - 如果 `frame > len(tasks)`：骨架有空idle，答案为 frame
    - 如果 `frame ≤ len(tasks)`：间隙被其他任务填满，无空闲，答案等于任务总数

### 方法二：大顶堆贪心模拟
**核心思想：**
贪心：每次优先执行剩余次数最多的任务；每一轮最多执行 `n+1` 个不同任务（对应n冷却）；堆中保存任务剩余频次（存负数模拟python大顶堆）；一轮结束后把还没做完的任务放回堆，模拟冷却。
**步骤拆解：**
1. 统计任务频次，把频次取负存入堆，构建大顶堆。
2. 循环：堆不为空代表还有任务未完成
    1. 每一轮最多跑 `n+1` 个时间槽；用临时列表保存本轮执行完还剩余的任务。
    2. 逐个从堆取出任务，执行一次，次数减一；次数大于0则放入临时列表。
    3. 每一个时间槽，无论执行任务还是idle，时间+1；全部任务做完提前跳出循环。
    4. 一轮结束，把临时列表任务放回堆，进入下一轮。
3. 返回总时间。

## ⚙️ 复杂度分析
- **数学公式法**
    - **时间复杂度**：$\(O(N)\)$，N为tasks长度，统计频次。最多26种任务，后续计算常数。
    - **空间复杂度**：$\(O(1)\)$，最多26个大写字母，空间常数。
- **堆模拟法**
    - **时间复杂度**：$\(O(N)\)$，只有26种任务，堆操作是常数级别。
    - **空间复杂度**：$\(O(1)\)$。

## 💻 AC 代码（Python）
### 版本1：数学公式（面试优先）
```python
from collections import Counter
from typing import List

class Solution:
    def leastInterval(self, tasks: List[str], n: int) -> int:
        freq = list(Counter(tasks).values())
        max_cnt = max(freq)
        max_task_num = sum(1 for v in freq if v == max_cnt)
        frame = (max_cnt - 1) * (n + 1) + max_task_num
        return max(len(tasks), frame)
```

### 版本2：堆模拟版本
```python
import heapq
from collections import Counter
from typing import List

class Solution:
    def leastInterval(self, tasks: List[str], n: int) -> int:
        freq = [-v for v in Counter(tasks).values()]
        heapq.heapify(freq)
        time = 0
        while freq:
            tmp = []
            for _ in range(n + 1):
                if freq:
                    cur = -heapq.heappop(freq)
                    cur -= 1
                    if cur > 0:
                        tmp.append(-cur)
                time += 1
                if not freq and not tmp:
                    break
            for t in tmp:
                heapq.heappush(freq, t)
        return time
```

## 🧩 关键知识点 \& 技巧
- 技巧1：Python `heapq` 只有小顶堆；大顶堆通过存储负数模拟。
- 技巧2：本题两个下界，答案必须大于等于冷却骨架，同时大于等于任务总数，取`max`。
- 技巧3：堆模拟中，`tmp`临时列表很关键：本轮执行过的任务不能立刻放回堆，模拟冷却，一轮结束再放回。
- 易错点1：`list * -1` 是列表重复，不是每个元素取负；逐个元素运算要用列表推导式。
- 易错点2：堆模拟中时间计数位置：**先time +=1，再判断是否全部任务结束**，否则会少算时间片。

## ❌ 错题反思 / 踩坑记录
**错误1：`freq = -1 * [Counter(tasks).values()]`**
错误原因：`*`对列表是容器重复，不是元素逐个相乘；列表乘负数直接得到空列表。
修正思路：使用列表推导式 `[-v for v in Counter(tasks).values()]`。

**错误2：堆模拟时`time +=1`写在break之后**
错误原因：任务全部完成时，当前时间片没有计数，总时间偏小。
修正思路：每轮循环先`time +=1`，再判断任务是否全部完成，执行break。

**错误3：直接返回frame，不与len(tasks)取max**
错误原因：当任务数量足够多，把所有冷却空位填满，此时不需要idle，总时间等于任务长度。
修正思路：`return max(len(tasks), frame)`。

## ✅ 总结
1. 数学解法核心：**频次最高的任务决定调度骨架**。`(max_cnt‑1)*(n+1)+max_task_num` 是冷却约束下的最小框架；和任务总长度取max得到答案。
2. 堆模拟是仿真调度过程，适合理解调度逻辑；由于只有26个大写字母，堆的开销是常数。
3. 两个下界思想：问题有多个约束条件，最终解必须大于等于所有约束给出的下界，取最大值。
4. 堆模拟注意：本轮执行过的任务存入临时列表，本轮不能再次选取，一轮结束再放回堆，实现冷却效果。
